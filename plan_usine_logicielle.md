# Planification : Usine Logicielle Autonome (Graph Engineering)

Ce document traduit l'audit des références (`references_audit.md`) en une feuille de route concrète, triée par ordre d'importance critique. L'objectif est d'atteindre le "Fire and Forget" complet.

> **🎯 Principe directeur (filtre qualité des références)** — Chaque feature à venir s'appuie sur une référence qui a **fait le job en production** :
> - **Références production-éprouvées** (transposables avec confiance) : `aider` (PyPI top OpenRouter), `crush` (Charm/Go), `deer-flow` (ByteDance), `qm` (plateforme Slack/Web), `open-swe` (dérivé SWE-agent), `pi` (agent interactif TS).
> - **Référence mature mais collectée** (prompts, à adapter) : `claude-code-unified-agents`.
> - **Références recherche/POC** ( RepoGraph, axon, graphify) : **non retenues** comme base de feature — trop académiques, ROI faible sur l'usage actuel (création de code de zéro, pas exploration de gros dépôts).
> Le détail des références est dans [`docs/references-audit/`](./docs/references-audit/INDEX.md).
> 🔄 **Note de mise à jour** : Consulte toujours [`references/update_report.md`](./references/update_report.md) pour voir les dernières nouveautés ajoutées aux dépôts amonts (nouvelles "Skills", outils MCP, workflows, etc.) avant de concevoir une fonctionnalité.
> 🧠 **Mémoire et Directives** : Le comportement des agents, la gestion d'état (`feature_list.json`, `progress.md`, event stream DuckDB) et la topologie du graphe sont définis dans [`AGENTS.md`](./AGENTS.md) et [`docs/NODES_AND_SKILLS.md`](./docs/NODES_AND_SKILLS.md). Ce sont les sources de vérité absolues pour le runtime.
> 🗺️ **Cartographie du projet** :
> - `docs/` : Architecture (`NODES_AND_SKILLS.md`) et audit des références (`references-audit/`).
> - `debug/` : Scripts de test isolés (ex: `run_web_tester_standalone.py`) pour exécuter/déboguer un nœud spécifique hors graphe.
> - `scripts/` : Outils de maintenance, de préparation (ex: DL de modèles) et d'analyse (`run_analyzer.py`).
> - `skills/` : Fonctionnalités modulaires chargeables par les agents (Lazy/Eager E-57).
> - `prompts/` / `graph_orchestrator/prompts.py` : Définition des blocs et rôles comportementaux de l'orchestrateur.

> **📊 ÉTAT D'AVANCEMENT (2026-07-31)**
> | Priorité | Statut |
> |---|---|
> | 🔴 0. Cadre système & prompts | **Partiel** (Coder autonome ✅, Architect Read-Only ✅ via DSPy, CodeAgent ❌ ; **Spécialisation + Invariants universels ✅ (F-44)**) |
> | 🔴 1. Édition sécurisée | **✅ TERMINÉE** (PR #3) — la plus critique, validée au run #12 |
> | 🟠 2. Auto-correction (stderr) | **✅ TERMINÉE** — troncature + Tester polyvalent multi-techno (web+python) |
> | 🟡 3. Graphe autonome (breaker/checkpoints) | **✅ TERMINÉE** — `max_iter=3` ✅, checkpoints ✅, nœud d'escalade ✅ (F-23) |
> | 🟢 4. Cartographie de code (Repo Map + KG structural) | ⏸️ **HORS-SCOPE** (références académiques/POC + usage = création de code de zéro). Palliatif conservé : tags de confiance sur claims DuckDB. |
> | 🔵 5. Auto-dépendances | **✅ TERMINÉE** (F-26) — auto-install `pip` non-persistante + relance sur `ModuleNotFoundError` |
> | 🟣 6. Feedback & Évaluation Avancée | **PARTIEL** 🟡 *(Nettoyage DOM ✅ F-37 ; **Code Review Rubric + Findings ✅ (F-44)** ; **consolidation claims KG + oubli ✅ (F-68 Phase 1)** ; reste TDD persistant/Findings lifecycle/Plan lifecycle/mémoire cross-run scratch-notebook/extraction L0-L3 — Phases 2+3 F-68 cycles futurs)* |
> | 🟤 7. Le "Shift Left" (Linter-as-a-Reviewer) | **✅ TERMINÉ** — `linter.py` (317 lignes) existe et est branché dans `workflows.py` (court-circuite le Tester si erreur de syntaxe). *Cohérence doc/code corrigée le 2026-07-31.* |
> | ⚪ 8. Middlewares d'Auto-Réparation | ❌ à faire |
> | 🔧 8-bis. Robustesse Runtime (Sandbox + Idempotence) | **PARTIEL** ❌→🟡 *(Guard bash denylist ✅ F-38 ; Idempotence replays ✅ F-43 ; reste Sandbox Docker)* |
> | 🟢 9. Optimisation de l'État (Reducers/Compaction) | ❌ à faire *(compaction qm/pi + whitelist loopx + branch summarization pi + **skeleton libcst RepoGraph** planifiés)* |
> | 🔵 10. Contexte à la Demande (Skills) | **PARTIEL 🟡 (Phase 1 ✅ F-57, Phase 3 en attente F-82)** — Coder lazy loading TERMINÉ (Phase 1). Phase 2 (nœuds DSPy via `methodology_context`) planifiée conditionnellement. Phase 3 (F-82) : Chercheur de skills à la volée (Skill Finder) pour installer dynamiquement les skills via skills.sh en s'appuyant sur les audits de sécurité (Socket/Snyk) (planifié). **F-72 ✅** (Prompt Offloading : outils Python dédiés `check_js_syntax` / `clean_dom` / `add_visual_tags` / `fuzz_click_all_buttons` encapsulent les snippets JS/algorithmes hors des prompts Coder/Tester ; répare le « modèle » Puppeteer cassé → DevTools). |
> | 🟠 11. Observabilité (Event Stream) | ❌ à faire |
> | ✨ 12. Meta-prompt (PromptRefiner avant Architect) | **✅ TERMINÉ (Phase 1)** — nœud DSPy F-39 (Kilo/Cline "Enhance Prompt" pattern). Phase 2 MIPROv2 écartée (signal biaisé mono-modèle 6 Go VRAM). |
> | 📁 13. Output daté par run (isolation des artefacts) | **✅ TERMINÉ** — `chdir` vers `runs/YYYY-MM-DD_HHMM_slug/` (F-40). Reprise via checkpoint (même dossier). DB DuckDB stable (kg_path absolu avant chdir). Restoration cwd auto (`_scoped_chdir` finally). |
> | 🎯 14. Durcissement des prompts de nœuds (procédure concrète) | **✅ TERMINÉ** (F-56) — durci les 4 prompts DSPy (procédure ordonnée + patterns concrets + biais nommés) + Linter warning fichier absent. 587 passed / 0 failed. F-57 (P10 lazy loading) planifié ensuite pour dégonfler. |
> | 🔴 17. Jeux de tests par nœud (Isolation LLM) | **🟢 FAIT** — 6 scripts d'isolation créés (Router/PromptRefiner/Architect/Drafter/Security/Judge) + Coder/WebTester existants. Chacun appelle la VRAIE fonction de production (0 mock) avec fixtures figées. Dossier `debug/` versionné (retiré du .gitignore). **FAIT (F-89)**. |
>
> **Levier hors-plan majeur découvert et appliqué** : température du Coder 0.2 (vs 1.0 serveur) — a éliminé la moitié de la corruption.

---

## 🔴 Priorité 0 : Le Cadre Système & L'État d'Esprit (La Fondation)
*Ce sont les réglages natifs de smolagents et des prompts qui dictent le comportement global.*

> **🔎 État Actuel :** Le prompt du Coder a été amélioré, mais l'Architecte utilise la même base d'outils et peut être tenté d'écrire du code. On utilise probablement l'agent par défaut de smolagents.
> **🚀 Pourquoi ce sera mieux :** En interdisant à l'Architecte d'écrire via ses prompts système (Read-Only) et en forçant le format `CodeAgent` (génération de scripts Python pour appeler les outils au lieu de JSON), on gagne massivement en précision avec les petits LLM locaux.

- [ ] **Durcissement des Prompts (Testeur & Architecte)** :
  - Ajouter une règle stricte (Invariant) dans la system_prompt du `Web Tester` (Ornith) pour la Sandbox : `You MUST strictly use the "read_file" tool to read files. Native Python open() is forbidden and will crash.`
  - Cadrer l'Architecte pour qu'il soit plus directif dans ses specs. Il n'a aucune permission d'utiliser des outils d'édition. Son unique livrable est le `contract.md` (TDD). — *FAIT via DSPy : l'Architect (dspy_nodes.py) n'a aucun tool d'écriture, il ne fait que planifier en JSON (ArchitectOutput).*
- [x] **Directive d'Autonomie Pure (Coder)** : Ajouter au prompt du Coder : "Ne t'arrête jamais pour poser une question. Effectue l'action suivante." — *FAIT : prompt Coder avec plan d'action strict + règle anti-boucle ("une passe unique").*
- [ ] **Transition vers `CodeAgent`** : S'assurer que tous les agents dans `dspy_nodes.py`/`nodes.py` instancient un `CodeAgent` de *smolagents* (beaucoup plus robuste que le `ToolCallingAgent` JSON classique). — *REPORTÉ. À nuancer : les 5 « Brain » (Router/Architect/Security/Judge/Escalation) ont migré en **DSPy** (meilleur pour le raisonnement pur, pas besoin d'outils) ; le **Coder** est déjà un `CodeAgent` (`nodes.py:437`). Restent en `ToolCallingAgent` : Worker/Judge/Synth/Adversary (modes one-shot/exploration) et le Web Tester. CodeAgent exécute du Python (risque `bash_command` non-sandboxé, voir Priorité 0-bis), nécessite réécriture des prompts JSON→Python. **Prérequis : nettoyer d'abord les ~400 lignes de nœuds smolagents dépréciés dans `nodes.py` (l.306-688).***
- [x] **Spécialisation des Agents (Inspiré de claude-code-unified-agents + learn-claude-code + system-prompts)** : Étendre la flotte d'agents actuels (Coder, Tester, Judge) avec des profils hautement spécialisés (ex: `frontend-specialist`, `security-auditor`) en s'inspirant des prompts de la fiche **15-claude-code-unified-agents**. — *Précision (audit refondu 2026-07-31) : il y a **53** agents (et non 54), et la valeur est concentrée dans ~8 prompts purs : `python-pro` (Coder), `code-reviewer` (Judge), `security-auditor` (Security), `test-engineer` (Tester), `frontend-specialist` (Coder web), `backend-architect` (Architect), `orchestrator` (Router). Le schéma `AgentCapabilitySchema` de `agent-generator.md` fournit un modèle pour déclarer formellement un rôle. Les autres fichiers sont du code TS non portable.* — *Patron d'instanciation (Python natif) : fiche **16-learn-claude-code** → `s06_subagent/code.py` (`spawn_subagent` : messages frais, hard cap 30 tours, pas de récursion = le subagent n'a pas l'outil `task`). C'est le patron direct pour instancier Coder/Tester spécialisés sans polluer le contexte parent.* — ***Bibliothèque de patterns** : fiche **17-system-prompts-and-models-of-ai-tools** → ~15 prompts d'agents de coding vérifiés, bases concrètes par rôle (Codex CLI→Coder/Tester, Manus→topologie multi-agent Planner/Knowledge/Datasource, Augment→Router, Claude Code 2.0→Coder/Judge, Gemini CLI→workflow 5 étapes, Devin→`<think>` reasoning). ⚠️ Préférer les prompts open-source (Codex CLI, Gemini CLI, Cline) pour citation verbatim ; les prompts commerciaux sont leakés (inspiration des patterns uniquement).* — *FAIT (F-44) : module `graph_orchestrator/prompts.py` exporte `ROLE_BLOCKS` (9 rôles spécialisés : router/architect/prompt_refiner/coder/coder_frontend/web_tester/judge/security/escalation) inspirés des fiches 15+17. Chaque rôle aligné avec un prompt pur de référence (python-pro→coder, code-reviewer→judge, security-auditor→security, test-engineer→web_tester, frontend-specialist→coder_frontend, backend-architect→architect, orchestrator→router). Doctrine open-source citable respectée (patterns seulement pour les leakés).*
- [x] **Contextualisation par package (AGENTS.md localisés)** : Ajouter la capacité à lire un fichier `AGENTS.md` local au dossier cible de la sous-tâche pour injecter des directives spécifiques au composant (ex: règles i18n, design system) dans le prompt du Coder. (F-76) — *Référence production : fiche **opencode** → `packages/*/AGENTS.md` (règles de localisation spécifiques par paquet).*
- [ ] **Gestion dynamique des prompts système** : Gérer dynamiquement les instructions du prompt système selon le modèle (Anthropic vs autres, flash vs thinking) avec support du caching. — *Référence : fiche **27-browser-use** → `references/browser-use/browser_use/agent/prompts.py` (`SystemPrompt`, `cache=True`).* — *Assemblage par sections ordonnées (audit fiche **46-deepseek-harness**, 2026-08-14) : fiche **46-deepseek-harness** → `packages/core/system-prompt/src/index.ts` (`PromptSection {name, order, complete?}` avec convention -100 identité / 0 persona / 100-199 guidance outils, `PromptAssembly` + interpolation `{{variable}}`, flag `complete` = un seul gagnant remplace tout). Modèle direct pour des presets de prompt par nœud (P0 spécialisation) — chaque nœud du graphe = un preset avec sa section persona.*

### P0-bis : Invariants universels des system prompts
> 📚 **Nouvelle sous-section** (issue de la fiche **17-system-prompts-and-models-of-ai-tools** et de la fiche **29-system-prompts-leaks**). L'audit croisé de ~12 prompts d'agents de coding (fiche 17) et des prompts de production de Claude Code Opus 5, Cursor, et Gemini CLI (fiche 29) révèle **10 patterns universels** qui reviennent partout (read-before-edit, tool usage policy, git safety). Ils doivent devenir une **section partagée** injectée dans TOUS les system_prompts du graphe (Router, Architect, Coder, Tester, Judge, Security), au-delà des spécificités de chaque rôle.

- [x] **Section « Invariants universels » partagée** : codifier et injecter dans chaque system_prompt les 10 patterns ci-dessous (vérifiés par grep croisé sur Claude Code 2.0, Codex CLI, Cline, Cursor, Gemini CLI, Devin, Augment…) : — *FAIT (F-44) : constante `UNIVERSAL_INVARIANTS` dans `graph_orchestrator/prompts.py`. Les 10 patterns sont numérotés et actionnables. Injectés dans TOUS les nœuds : 6 Signatures DSPy via `__doc__ = with_invariants(role, doc)` (mécanisme validé — DSPy lit `__doc__` via metaclass) + 2 prompts smolagents (Coder/WebTester) via `build_role_header(role)`. Test `test_prompts.py::TestDSPySignaturesHaveInvariants` vérifie les 6 signatures. PythonTester inchangé (déterministe, aucun prompt).*
  1. **Read-before-write / read-before-edit** — l'outil Edit doit échouer si le fichier n'a pas été lu récemment ; re-lire si >5 messages. *(Déjà partiellement en P1 Read-Before-Write Gate.)*
  2. **Pas de whole-file rewrite** — privilégier SEARCH/REPLACE / `apply_patch` / substring replace ; toujours des edits ciblés. *(Déjà en P1.)*
  3. **Format d'édition formel** — un format canonique réduit les erreurs (SEARCH/REPLACE, `apply_patch`, etc.). *(Déjà en P1.)*
  4. **NEVER assume library available** — vérifier `requirements.txt`/imports voisins avant d'utiliser une lib (le plus récurrent, 9+ prompts).
  5. **Test-first / verify-after** — exécuter tests + lint + typecheck après chaque edit, jamais supposer le framework de test. *(Relie à P6 et P7.)*
  6. **Approval gating sur actions destructives** — jamais commit/push/install sans permission ; `requires_approval`/`is_dangerous`/sandbox modes. *(Relie à P8-bis Sandbox.)*
  7. **Anti-boucle / rabbit-hole** — max 3 itérations linter puis ask user ; « going in circles → escalate ». *(Relie à P3 anti-loop et P8 LoopDetection.)*
  8. **No comments / no preamble / concision** — « DO NOT ADD COMMENTS unless asked », réponses courtes. *(Économie de tokens pour petits LLM locaux.)*
  9. **Todo/Task tracking obligatoire** — TodoWrite/update_plan, marquer completed immédiatement, pas de batch. *(Relie à P6 cycle de vie plan.)*
  10. **Parallel tool calls par défaut** — batch les reads/recherches indépendantes (« 3-5x faster, expected behavior »).
- *Bonus (base de rôles spécifiques)* : **Professional objectivity** (truth > validation = base du Judge, Claude Code 2.0) ; **Defensive security only** (jamais logger/exposer secrets = base du Security Reviewer).*

- [ ] *(audit fiches 34 + 35, 2026-08-12)* **Enrichissement de la section `UNIVERSAL_INVARIANTS`** (prompts.py) : (a) fiche **34-brooklyn-skills** → `defaults.md` (UI first, pas de code sans confirmation, clean avant PR) + skill `no-tropes` (liste de tics d'écriture LLM à prohiber dans les sorties — "prompt the positive", rejoint l'invariant n°8 concision) ; (b) fiche **35-Understand-Anything** → `packages/core/src/schema.ts` (`EdgeTypeSchema`, modèle invariant de nœuds/arêtes génériques — utile si on modélise un jour des graphes métiers). — *Références : fiche **34-brooklyn-skills** → `references/brooklyn-skills/defaults.md` + `no-tropes` ; fiche **35-Understand-Anything** → `references/Understand-Anything/understand-anything-plugin/packages/core/src/schema.ts`.*

- [ ] *(audit fiches 45 + 46, 2026-08-14)* **Enrichissement P0-bis : Guardrails gradués + charte d'invariants de développement** : (a) fiche **45-OpenSandbox** → `AGENTS.md` racine = AGENTS.md **hiérarchiques** en production (« root router » : « Prefer the nearest `AGENTS.md` in the directory tree », résolution « le plus proche gagne ») + Guardrails en **3 niveaux Always / Ask first / Never** (compléments : `server/AGENTS.md` Key Paths, `specs/AGENTS.md` Contract Map) — le modèle pour nos invariants universels injectés par nœud ET par skill ; (b) fiche **46-deepseek-harness** → `AGENTS.md` racine = **charte d'invariants** exigés de tout plugin (« Registrations are effects », « Model-visible ⟺ logged », « Plugins, not loop changes », « Misconfiguration fails loud », waterfalls MUST `next()`, tags discriminants + `assertNever`) — chaque package a en plus son `invariant.ts` **exécutable** (invariants vérifiés en test, pas seulement déclarés). Sources directes pour durcir `UNIVERSAL_INVARIANTS` (F-44) et nos AGENTS.md contextualisés (F-76 : la maj incrémentale added/updated/removed après chaque outil fs de `packages/context/agent-instructions/` est le détail qui nous manque). — *Références : fiche **45-OpenSandbox** → `references/OpenSandbox/AGENTS.md` (+ `server/AGENTS.md`, `specs/AGENTS.md`) ; fiche **46-deepseek-harness** → `references/deepseek-harness/AGENTS.md` + `packages/context/agent-instructions/README.md`.*
- [x] *(update références 2026-08-14)* **Linter de prompts + budgets de guidance (F-103)** : (a) Anthropic a publié `prompt-audit` — framework d'audit des prompts contre les patterns datés pour vieux modèles, avec signaux GREPPABLES : langage de pression (« CRITICAL: You MUST » → sur-déclenchement sur modèles récents ; les hedges « try to/if possible » sont lus littéralement comme permission de sous-livrer), sur-spécification (chorégraphies STEP 1/2, clusters de prohibitions qui *ancrent* vers l'échec, « grader vocabulary »), tool descriptions = man pages (sous-description = failure n°1), « count the model-call sites » (routing/tallying/formatting retournent en code pur) + règle de deletion « could the model already know this? » (garder le contexte auteur, retirer les trained defaults). Applicable à nos 9 ROLE_BLOCKS + docstrings DSPy + skills — nos petits modèles llama.cpp suivent les instructions plus littéralement, mêmes risques de sur-contrainte. (b) deer-flow a ajouté des budgets soft/hard de taille par fichier de guidance (racine 12/16 Ko, module 24/32, local 40/48, **chaîne cumulée 80/96**) avec job CI + annotations PR — gate applicable à `prompts.py` et `skills/` (aucun budget aujourd'hui). — *Références : fiches **38-skills** → `references/skills/skills/claude-api/shared/prompt-audit.md` ; **08-deer-flow** → `references/deer-flow/scripts/check_agent_guidance.py`.* — *FAIT (2026-08-16) : (b) livré intégralement — `scripts/check_agent_guidance.py` (port fidèle : budgets racine/module/local + CHAÎNE CODER cumulée composée depuis `ALWAYS_SKILLS_CODER` = source de vérité unique, sémantique incrémentale base/head, annotations GitHub, durcissement repo-sans-commit) + 37 tests + run LIVE (AGENTS.md 20 433 o > hard = vrai signal consigné, réduction non exécutée ; chaîne Coder 30.7 Ko OK). (a) livré en volet greppable opt-in `--audit-signals` (AG101 densité pression + AG102 hedges, findings agrégés, caveat « pression légitime pour petits modèles ») ; le volet LLM-jugé complet de prompt-audit est REPORTÉ — nos 4B/9B sous-déclencheurs rendent la pression load-bearing, l'audit de suppression ne se fera que si migration vers un modèle plus littéral.*
- [ ] *(update références 2026-08-14)* **Nouveaux invariants Claude Code v2.1.224 → v2.1.232** : 3 règles candidates pour `UNIVERSAL_INVARIANTS`/middlewares : (1) anti « cross-session permission laundering » (ne JAMAIS redemander via un autre agent/session une action refusée — « route blocked work back to your user ») ; (2) un refus de content-safety est une FRONTIÈRE, pas une erreur d'infra — ne pas retenter via un autre outil/reformulation/découpage (anti-loop de retry) ; (3) « never fabricate or predict a pending agent's results » (subagents désormais background). — *Référence : fiche **29-system-prompts-leaks** → `references/system_prompts_leaks/Anthropic/Claude Code/claude-code-fable-5.md`.*

## 🔴 Priorité 1 : L'Édition Sécurisée des Fichiers (Le socle critique) ✅ TERMINÉE
*Si l'agent corrompt les fichiers, tout le reste de la boucle plantera. C'est l'urgence absolue pour les petits modèles locaux.*

> **🔎 État Actuel (`tools.py`) :** L'outil `edit_file` exige une correspondance exacte (`content.replace(old, new)`) au caractère et à l'espace près. De plus, il n'y a aucun verrou lors du Fan-out asynchrone (`asyncio.gather` dans `workflows.py`).
> **🚀 Pourquoi ce sera mieux :** Les petits LLMs échouent constamment sur l'indentation stricte. Un format `SEARCH/REPLACE` tolérant résout ce problème d'hallucination de format. Le Mutex (verrou) évitera que deux sous-tâches n'écrasent le même fichier en même temps.

- [x] **Outil "SEARCH/REPLACE" textuel** : Supprimer l'édition basée sur le remplacement strict/JSON. Créer un outil Python qui parse le format texte brut `<<<< ==== >>>>` (inspiré de `aider`). — *FAIT (PR #3) : `search_replace_utils.py` (portage Aider) + outil `@tool search_replace(path, search, replace)` avec matching tolérant (exact → indentation → ellipses).*
- [x] **Verrou d'accès (Mutex)** : Implémenter un verrou asynchrone par fichier (inspiré de `openfox`) dans `tools.py` pour empêcher l'exécution en parallèle de deux opérations d'écriture sur le même fichier. — *FAIT : `_FILE_LOCKS` (threading.Lock) dans tools.py.*
- [x] **Validation Anti-Vide** : Rejeter immédiatement toute édition qui remplace le code par des placeholders (`// ... code ici ...`). — *FAIT : garde anti-placeholder + anti-effacement dans `search_replace` ET `write_file`.*
- [x] **Read-Before-Write Gate (Inspiré de Deer Flow / Crush)** : Implémenter un middleware qui bloque toute tentative de `write_file` ou `edit_file` si l'agent n'a pas préalablement lu le fichier dans la session courante (suivi via un hash SHA256). Cela évite les écrasements aveugles ou les corruptions de fichiers. — *FAIT (F-67) : `graph_orchestrator/read_gate.py` (100% Python natif, 0 LLM). `ReadGate` (dict thread-safe {norm_path: hash}) + `compute_content_hash` (SHA256 contenu complet UTF-8) + `_normalize_path` (Windows-safe `..`/mixed sep). Mode STRICT fidèle Deer Flow issue #3857 : un write RÉUSSI invalide la mark → force re-read avant chaque édition (corrige le bug « édition à partir d'une représentation mentale stale »). Fail-open garanti (fichier absent = création OK, read impossible = on laisse passer). 2 proxies `BaseTool` (template copié SanitizedTool F-42) : `_GatedWriteTool` (bloque write_file/search_replace/edit_file/multi_replace/append_file sans déléguer si check_write False, sinon délègue puis record_write) + `_ReadTrackingTool` (stamp le hash du contenu COMPLET après read_file, re-lit le disque peu importe offset/limit). Branché dans `execute_coder_node` entre wrap_screenshot_tools et sanitize_tools (gate AVANT sanitizer → kwargs déjà typés). Opt-out `READ_BEFORE_WRITE_ENABLED=false`. 35 tests PASS (test_read_gate.py). Suite pytest 651 passed / 0 failed (0 régression). Écart consciencieux vs Deer Flow : mark en RAM (notre CodeAgent = unique séquentiel, pas LangGraph multi-threads) plutôt que sur ToolMessage.additional_kwargs.* — *Source du pattern gate stateless (write sauf si lu) : fiche **08-deer-flow** → `references/deer-flow/docs/superpowers/plans/2026-07-02-read-before-write-gate.md`.*
- [ ] *(évolution edit-format, audit fiche 02-aider, 2026-08-04)* **Format V4A / `apply_patch` multi-fichiers + move** : Notre `search_replace` (F-19, mono-bloc) et `multi_replace` (F-59) couvrent l'édition ciblée, mais le fan-out Coder (P0) écrit plusieurs fichiers. Le format V4A / `apply_patch` moderne (GPT-5 era) d'aider supporte nativement le multi-fichiers + move/rename dans un seul bloc, avec niveaux de fuzz (exact → rstrip → strip). À évaluer comme évolution si le multi-fichiers devient un point de friction. — *Référence : fiche **02-aider** → `references/aider/aider/coders/patch_coder.py` (`PatchCoder`, `Patch`, `PatchAction`, `ActionType`, `_parse_patch_text`, `_apply_update`) + `patch_prompts.py`.* Précision source : le moteur `search_replace.py` existant (F-19 « portage Aider ») s'inspire d'`editblock_coder.py` mais ne nomme pas `RelativeIndenter`/`dmp_apply`/`git_cherry_pick_osr_onto_o` de `references/aider/aider/coders/search_replace.py` (moteur multi-stratégies DMP + cherry-pick, 🟢 Haute) — à crediter si on durcit le matching.
- [x] *(bonus hors-plan)* **Température du Coder 0.2** — *FAIT (PR #3) : le défaut serveur était 1.0 (chat créatif) → corruption aléatoire. Config `CODER_TEMPERATURE=0.2` + Modelfile. Levier découvert par l'utilisateur, a éliminé la moitié du problème de corruption.*

## 🟠 Priorité 2 : La Boucle d'Auto-Correction (Le feedback)
*Un modèle fera toujours des erreurs de syntaxe. L'autonomie passe par sa capacité à se réparer seul.*

> **🔎 État Actuel :** Le graphe ne filtre pas intelligemment la sortie d'erreur (`stderr`) du nœud Tester.
> **🚀 Pourquoi ce sera mieux :** Si un script Python crache 500 lignes de Traceback, le LLM va les avaler à chaque itération. Au bout du 3ème essai, la fenêtre de contexte explosera ("Context Overflow"), provoquant un "oubli" des instructions de l'Architecte. La troncature garantit une boucle infinie stable en mémoire.

- [x] **Capture robuste du `stderr`** : Dans le nœud `Tester`, exécuter le code dans un sous-processus isolé et capturer la sortie d'erreur exacte. — *FAIT : `PythonTestRunner` (package `testers/`) lance `pytest` en subprocess via `sys.executable`, capture stdout+stderr+exit code. Le `WebTestRunner` capture les erreurs console JS via MCP Puppeteer (skill enrichi exigeant un rapport structuré).*
- [x] **Troncature anti "Context Overflow"** : Créer une fonction utilitaire qui coupe les grosses Tracebacks Python (ex: garder les 20 premières et 20 dernières lignes) avant de les renvoyer au `Coder`, pour sauver la mémoire du LLM. — *FAIT : `feedback_utils.py` (`truncate_output` head+tail + `truncate_history` plafonné) branché aux 3 points de la boucle (Tester→Judge, Judge→DuckDB→Coder, `bash_command`). Troncature à la LECTURE (contenu DuckDB intégral). 4 settings config.*
- [x] *(bonus hors-plan, décision utilisateur)* **Tester polyvalent multi-techno** : le nœud Tester n'est plus cloisonné au web. `execute_tester_node` est un dispatcher qui route selon la techno détectée (redondance Router + extensions) vers N runners dédiés (web, python, rust/ts futurs). Architecture extensible : ajouter une techno = 1 module + 1 skill + 1 ligne registre.
- [ ] *(audit fiches 07-llamabot + 18-awesome-claude, 2026-08-04)* **Capture web + cycle de vie serveur** : La capture Playwright (`capture_page_and_img_src` + `trim_html_for_llm`, source non créditée de `dom_filter.py` F-37) et le cycle de vie serveur (`wait_for_port` pour démarrer/arrêter un serveur local autour des tests) sont des compléments au Web Tester actuel (Puppeteer + `file:///`). À évaluer si le passage à un serveur HTTP local devient nécessaire (tests fetch/CORS/EHP). — *Références : fiche **07-llamabot** → `app/agents/utils/playwright_screenshot.py` ; fiche **18-awesome-claude-skills** → `webapp-testing/scripts/with_server.py`.*

- [ ] *(audit fiches 31 + 41, 2026-08-12)* **Outils web d'automatisation/navigation pour le Tester** : Évaluer en complément de Puppeteer + Chrome DevTools (pas en remplacement). (a) fiche **31-Scrapling** → `scrapling/core/ai.py` (serveur MCP Scrapling pilotant navigateurs/extraction furtive) + `scrapling/parser.py` (parseur lxml adaptatif, retrouve les éléments après restructuration du DOM) + `fetchers/stealth_chrome.py` (`StealthyFetcher`, contournement anti-bot type Cloudflare Turnstile) + `spiders/throttle.py` (`AutoThrottle`, ajustement dynamique des délais sur 429). (b) fiche **41-stagehand** → SDK browser-agent LLM avec primitives `act`/`observe`/`extract` (actions self-healing + DOM tronqué optimisé tokens). À réserver au cas où le Web Tester actuel bute sur de l'anti-bot ou des sélecteurs fragiles. — *Références : fiche **31-Scrapling** ; fiche **41-stagehand**.*

## 🟡 Priorité 3 : Le Graphe Autonome et ses Sécurités (L'Orchestration)
*Sans limites, un agent bloqué bouclera à l'infini et videra vos ressources.*

> **🔎 État Actuel (`workflows.py`) :** Il y a déjà un concept de `max_iter = 3` (bravo !), et les découvertes sont stockées dans `DuckDB`. Cependant, l'exécution elle-même reste une boucle Python en RAM.
> **🚀 Pourquoi ce sera mieux :** En cas de micro-coupure réseau (API) ou de plantage inattendu, une boucle Python en RAM est détruite. L'ajout de Checkpoints (comme dans *LangGraph*) permet de sauvegarder l'état sur disque à chaque nœud et de reprendre l'usine exactement là où elle a planté, économisant du temps et de l'argent.

- [x] **Le Coupe-Circuit (Circuit Breaker)** : Renforcer le compteur `retry_count` existant dans l'état global du graphe (`GraphState`). — *FAIT (préexistant) : `max_iterations=3` dans le workflow coding, la boucle s'arrête et passe à la sous-tâche suivante.*
- [x] **Anti-Loop Cryptographique (Inspiré de Crush)** : Ajouter un anti-loop déterministe qui hache en SHA256 les interactions d'outils (ToolName + Input + Output). Si un Coder répète exactement la même action X fois, le circuit-breaker s'active immédiatement pour stopper l'hémorragie financière et computationnelle. — *FAIT (F-36) : `loop_guard.py` — `compute_tool_call_fingerprint` (SHA256 de ToolName + Input normalisé via `sort_keys` + strip whitespace) ; `LoopGuard.record/repeated_action/reset` ; `extract_tool_calls_from_step` (ToolCallingAgent `tool_calls` + CodeAgent `code_action`). Branché dans `run_with_retry` (param optionnel `loop_guard=None`, non-cassant) via `execute_coder_node` (seul nœud d'écriture). Reset aligné sur purge `agent.memory.steps`. Config `LOOP_GUARD_ENABLED`/`LOOP_GUARD_THRESHOLD` (défaut 3). 16 tests PASS. Nuance : on hashe `ToolName + Input` (pas l'Output — l'Output varie même en bouclant sur la même action fausse).*
- [x] **Nœud d'Escalade (Judge/Summarizer)** : Si le Coupe-Circuit s'active au bout de 3 essais, router vers un nouveau nœud qui résume les échecs ou fait appel à une API distante (modèle lourd) pour corriger l'erreur vicieuse. — *FAIT (F-23) : nœud DSPy `execute_escalation_node` qui synthétise les réfutations accumulées dans le KG en un diagnostic post-mortem structuré (cause racine + leçon + severity), le persiste (kind="escalation" + arêtes ESCALATES vers les réfutations), marque la sous-tâche "escalated". Stratégie "diagnostic seul" (pas de retry/modèle distant — réutilise le modèle de raisonnement local). Dégradation gracieuse (repli "max_iterations_reached" si désactivé ou LLM down). 8 tests PASS.*
- [x] **Persistance d'État (Checkpoints)** : Connecter la base `DuckDB` existante au cycle de vie du graphe pour écrire l'état d'exécution à chaque changement de nœud (Reprise sur erreur). — *FAIT : table `checkpoint` dans knowledge_graph.py (save/load/clear) + run_id stable (hash du contenu de tâche) + branchement dans run_coding_workflow (skip Architect + skip sous-tâches completed + reprise à l'itération). Granularité "début d'itération" (sûre/idempotente). Config FRESH_START. 12 tests PASS.* **Besoin critique résolu** : avec ~10-15 min/fichier en CPU-only, une coupure ne perd plus 40 min — la reprise est automatique.

- [ ] *(audit fiche 40-waku-agent, 2026-08-12)* **Boucle & DAG déterministes en Python pur** : S'inspirer de `waku/loop/agent.py` (`run_loop`, boucle pure Python avec guardrails intégrés) et `waku/graph/engine.py` (`run_graph`, DAG déterministe par vagues avec détection `GraphStateCollision` et exécution ordonnée). Résonance directe avec notre boucle Coder→Tester et la gestion d'état — patterns Python natifs adaptables (contrairement aux références TS qm/pi). — *Référence : fiche **40-waku-agent** → `references/waku-agent/waku/loop/agent.py` + `waku/graph/engine.py`.*

### P3-bis : Anti-loop déterministe — stall detector + hash d'output matériel + vocabulaire de turn
> 📈 **Évolution** (audit référence 19-loopx, 2026-08-03). Notre F-36 ne hashe que `ToolName + Input` (pas l'Output). loopx apporte la **matière déterministe qui manquait** pour distinguer un turn productif d'un turn gratuit, et détecter le stall au-delà de la simple répétition.

- [x] **Stall detector (turns sans transition matérielle)** : Compter les itérations Coder→Tester consécutives « sans changement matériel » (l'Output du Coder ne change pas entre deux itérations), en **ignorant le bookkeeping** (accounting/state_refreshed). Au-delà d'un seuil (loopx utilise `=2`), déclencher l'arbitration/escalade plutôt que de laisser spinner. — *Référence : fiche **19-loopx** → `loopx/control_plane/quota/recent_runs.py` (`consecutive_unchanged_monitor_observations`, `MONITOR_DEBT_UNCHANGED_TURN_THRESHOLD=2`, `_run_is_controller_bookkeeping` qui exclut le bookkeeping du comptage).* — *FAIT (F-88) : module `graph_orchestrator/stall_detector.py` — `StallDetector(threshold=2, enabled=True)` port de `consecutive_unchanged_monitor_observations` + `build_monitor_debt_arbitration`. Compte les steps consécutifs "sans changement matériel". L'exclusion du bookkeeping loopx (classification control plane) est remplacée par `classify_turn` (outils d'écriture = ACCOUNTABLE = matériel, le reste = non-matériel). Branché dans `run_with_retry` après le bloc LoopGuard, priorité `validated` conservée. 26 tests PASS (test_stall_detector.py).*
- [x] **Hash d'output matériel** : Étendre notre LoopGuard (F-36) pour hasher aussi l'**output** (le résultat du write_file / le diff produit), pas seulement `ToolName + Input`. Un Coder qui rejoue le même `write_file` avec un contenu identique doit être détecté même si l'input normalisé change subtilement. — *Référence : fiche **19-loopx** → `loopx/capabilities/issue_fix/pr_monitor_materialization.py` (`_group_fingerprint` → `result_hash`, comparaison `previous_hash != result_hash` → `material_change`, sinon `consecutive_no_change++`). C'est le pattern exact de hash d'output.* — *FAIT (F-88) : `compute_material_fingerprint(tool_name, arguments)` dans `stall_detector.py` — port de `_group_fingerprint`. Hashe le CONTENU que l'outil cherche à écrire (content pour write_file/append_file, old+new pour search_replace/edit_file, concaténation pour multi_replace), tronqué à 16 hex chars comme loopx. **Écart consciencieux** : la case disait "Étendre notre LoopGuard (F-36)" — décision contraire prise (module ORTHOGONAL, LoopGuard intact). Raison : le docstring `loop_guard.py:10-12` dit explicitement "on n'inclut pas l'Output" et le test `test_valid_final_answer_wins_over_loop_guard` (test_coder_hardening.py:289) est fragile (asserte `guard.repeated_action() is not None` après `return validated`). Modifier LoopGuard cassait sa cohérence ET ce test. Le stall detector opère à un autre niveau (step/turn) que le LoopGuard (tool call isolé) — gardes complémentaires, pas remplacement.*
- [x] **Vocabulaire de delivery_outcome** : Classifier chaque itération Coder→Tester en `accountable` (livrable matériel) / `progress` (avance sans livrable) / `idle`. Un turn sans outcome matérielle = pas de progression = graine de stall. Cette métrique alimente à la fois le stall detector et le circuit breaker. — *Référence : fiche **19-loopx** → `loopx/control_plane/work_items/delivery_outcome.py` (`DeliveryOutcome` enum, `ACCOUNTABLE_DELIVERY_OUTCOMES`) + `loopx/control_plane/turn_driver/transaction.py` (`LoopXTurnResultKind`, `NO_SPEND_RESULT_KINDS`/`STOP_RESULT_KINDS` pour distinguer turns productifs vs gratuits).* — ***Anti-loop de compaction (audit fiche 25-hermes-agent, 2026-08-05)** : `references/hermes-agent/agent/context_compressor.py` (`ContextCompressor.record_timeout_failure` + `get_active_compression_failure_cooldown` + `_persist_ineffective_compression_count`). Garde-fous persistés en DB (cooldown après échec, "fallback streak", "ineffective counter") pour empêcher la compaction de spinner. Exactement l'anti-loop de compaction qui manque au projet pour P9.* — *FAIT (F-88) : `DeliveryOutcome` enum (port simplifié 3 valeurs ACCOUNTABLE/PROGRESS/IDLE vs 4 chez loopx — fusion surface_only+outcome_gap en IDLE, non pertinent pour un agent de coding) + `ACCOUNTABLE_OUTCOMES` frozenset + `classify_turn(tool_calls)` dans `stall_detector.py`. Alimente le stall detector (record par step). 26 tests PASS.*

- [ ] **Queue de runs à leases / reaper** *(audit fiche 14-qm, 2026-08-04)* : Modéliser la reprise après crash comme une queue à leases (`claim` + `heartbeat` + `reapExpired` pour les runs abandonnés + `errorParks` pour les runs en échec). Complète notre checkpoint (F-24) qui ne gère que la reprise d'un run *en cours*, pas la détection/nettoyage des runs *abandonnés* (ex: process tué, VRAM saturée). À reloger sur DuckDB (déjà utilisé). — *Référence : fiche **14-qm** → `references/qm/src/runs/run-store.ts` (`RunStore`, `errorParks`, `claim`, `heartbeat`, `reapExpired`). Attention : les leases typés par rôle `LeaseHolder` (`turn`/`compaction`/`fork`, `sessions/session-store.ts`) deviendront pertinents quand la compaction async (P9) sera implémentée — pour qu'une compaction en tâche de fond n'écrase pas un turn en cours. Ajout de fiabilité pour l'accès concurrent SQLite/DuckDB : référence **pi** → `packages/session-backends/sqlite-node/src/sqlite/storage/writer-leases.ts`.*
- [x] *(update références 2026-08-14)* **Anti-loop comportemental : enforcement de but (F-99)** : les couches déterministes (LoopGuard F-36, stall detector F-88) ne voient pas les faux « j'ai fini ». qm apporte la garde comportementale manquante : continuation prompt avec audit de complétion (« treat completion as unproven », preuve = état autoritaire pas la mémoire), statut `blocked` accepté seulement après 3 tours de MÊME impasse (`GOAL_BLOCKED_MIN_ROUNDS`), auto-waiver après 5 rounds de continuation sans aucun tool call (escape anti-deadlock), token cap = wind-down (« un budget épuisé n'est pas une complétion »). — *Références : fiche **14-qm** → `references/qm/src/harness/goal.ts` (`enforceGoal`, `goalContinuationPrompt`, `GOAL_BLOCKED_MIN_ROUNDS=3`) + `src/harness/grind.ts` (`GrindBudget` floor minTurns/minMs/minTokens/minUsd) ; complément : fiche **34-brooklyn-skills** → `skills/babysit/SKILL.md` (boucle de poll bornée dans le shell + table react-by-cause + reporting sur changement d'état seulement + sortie fail-closed) ; fiche **36-prime-agent** → `packages/coding-agent/src/core/kernel/index.ts` (`HostRequestContext.isCurrent()` — rejeter le travail d'un handler dont l'autorité a été révoquée).* — *FAIT (F-99, 2026-08-14) : module `goal_enforcer.py` (0 LLM) branché `run_with_retry`/`execute_coder_node` — continuation prompt + preuves manquantes déterministes, blocked 3 même impasse, auto-waiver 5, cap wind-down. Fix annexe : `loop_guard.known_tools` étendu (multi_replace/check_js_syntax/DevTools). 22 tests, suite 1035/0.*
- [ ] *(audit fiches 45 + 46, 2026-08-14)* **Anti-loop couche douce (détection de répétitions) + reprise par agents frais** : compléments aux couches dures existantes (LoopGuard F-36 = fingerprint `ToolName+Input`, stall detector F-88 = hash d'output matériel). (a) **Détection douce sans veto** : fiche **46-deepseek-harness** → `packages/guard/repeat-tool-reminder/src/index.ts` (détecte les répétitions **consécutives** d'appels d'outils identiques — clé = nom + arguments **canonisés par tri récursif des clés JSON** (subtilité que notre normalisation `sort_keys` a déjà, mais leur escalade graduée nous manque) ; seuils `[3,5,8]` : rappel générique au 1er seuil, rappel détaillé (outil, longueur de série, arguments tronqués) ensuite ; injecté via contexte additionnel **sans veto** ; chaîne par agent `WeakMap`, reset dès qu'un message utilisateur interjecte. Portage Python ~150 lignes, branchable sur le Coder AVANT le veto LoopGuard — nudge préventif au lieu de coupe-circuit). (b) **Reprise par agents frais** : fiche **46-deepseek-harness** → `packages/workflow/tool-ralph/README.md` (`ralph({objective, maxRounds})` : objectif immuable + contexte remis à zéro chaque round + workspace partagé comme mémoire + handoff structuré borné `status/summary/evidence/nextSteps/blocker` `maxHandoffChars=16384` = seul transfert inter-rounds. Contre-mesure radicale quand l'escalade F-23 échoue : réinstancier le Coder avec un contexte neuf plutôt que de continuer à polluer un contexte saturé). (c) **Anti-récursion** : fiche **46-deepseek-harness** → `packages/subagent/subagent/src/depth.ts` (`delegationDepthOf`, profondeur monotone persistée — un child repris ne peut pas se faire passer pour top-level). (d) **Self-loop bornée illustrée** : fiche **45-OpenSandbox** → `examples/langgraph/main.py` (`WorkflowState` avec `attempt/max_attempts/fallback_command` : à chaque retry, substituer une commande de **fallback différente** au lieu de rejouer l'identique + cleanup garanti en `finally` — le pattern exact de notre boucle Coder→Tester quand l'itération n apporte la même erreur qu'en n-1). — *Références : fiche **46-deepseek-harness** → `references/deepseek-harness/packages/guard/repeat-tool-reminder/src/index.ts` + `packages/workflow/tool-ralph/README.md` + `packages/subagent/subagent/src/depth.ts` ; fiche **45-OpenSandbox** → `references/OpenSandbox/examples/langgraph/main.py`.*
- [ ] **Traçabilité de bifurcation (Fork Origin)** : Tracer formellement lorsqu'un run est un fork d'un checkpoint précédent pour conserver l'arbre d'héritage des expérimentations et afficher l'historique complet. (F-77) — *Référence production : fiche **qm** → `plugins/web-ui/src/fork-origin.ts`.*


## 🟢 Priorité 4 : ⏸️ Cartographie de code (Repo Map + KG structural) — HORS-SCOPE
*Sans cela, l'architecte navigue à l'aveugle dans les gros dépôts.*

> **🔎 État Actuel :** L'agent utilise `list_directory` et lit les fichiers manuellement, ce qui consomme des tokens. Pour l'usage actuel (création de code de zéro sur de petits projets), c'est acceptable.

### ⚖️ Décision explicite : Repo Map tree-sitter ET Knowledge Graph structural — HORS-SCOPE

> L'audit des références (fiches **05-axon**, **06-graphify**, **04-repograph**) consacre 10 entrées du Hall of Fame au « knowledge graph de code » structurel (tree-sitter + networkx, schéma `NodeLabel`/`RelType`, RRF, dead-code detection, impact analysis avec provenance). Conceptuellement riche, mais ces références sont **académiques/POC** (pas production-éprouvées) et le périmètre produit actuel ne les justifie pas.

**Décision : ne PAS intégrer de Repo Map (tree-sitter) ni de KG structural dans l'immédiat.** Raisons :
1. **Périmètre produit actuel** : l'usage dominant est la **création de code de zéro** (Prompt-Vault) sur des projets neufs et petits — pas l'exploration de gros dépôts existants. Ces briques n'apportent de valeur que sur une base de code préexistante à cartographier.
2. **Références non production-éprouvées** : RepoGraph (papier SWE-bench), axon, graphify = projets académiques/perso, contraires au principe directeur (cf. en-tête).
3. **Coût / complexité** : pipeline tree-sitter multi-langage + graphe networkx + requêtes = investissement lourd (estimé 1-2 semaines) pour un ROI faible sur l'usage actuel.

**Compromis retenu (palliatif léger, ROI bien meilleur, référencement graphify)** : enrichir le KG DuckDB existant de métadonnées de confiance/provenance — tags `EXTRACTED`/`INFERRED`/`AMBIGUOUS` + `confidence_score` + provenance edge (`via_file`/`via_location`). Concrètement : ajouter 2 colonnes au store de claims existant (pas de réécriture de persistance) pour que le Judge et le nœud d'escalade puissent distinguer un fait avéré d'une inférence. Voir fiche **06-graphify** → `graphify/extractors/models.py` et `graphify/affected.py`.

- [ ] *(palliatif, court terme)* **Tags de confiance + provenance sur les claims** : ajouter un tag `EXTRACTED`/`INFERRED`/`AMBIGUOUS` + `confidence_score` + provenance au niveau de l'edge (`via_file`/`via_location`) sur les claims/refutations DuckDB existants. Effort moyen (~2-4 jours), pas de réécriture de persistance.
- [ ] *(réévaluation, moyen terme)* **Repo Map + KG structural** : à réévaluer si l'usage évolue vers la maintenance/évolution de gros dépôts existants. Blueprints prêts (fiches **05-axon**, **06-graphify**, **04-repograph**).

- [ ] *(audit fiches 30 + 35 + 44, 2026-08-12)* **Réévaluation du KG structural** : Trois nouvelles références renforcent les blueprints SI l'usage évolue vers la maintenance de gros dépôts existants. Fiche **30-Ix** → `core-ingestion/src/queries.ts` (requêtes tree-sitter natives réutilisables en Python) + `ix-cli/src/client/types.ts` (schéma de graphe relationnel) + `skills/ix/SKILL.md`. Fiche **35-Understand-Anything** → `agents/tour-builder.md` (topologie Fan-In/Fan-Out + BFS) + scripts Python `merge-batch-graphs.py` (`merge_graphs`, fusion de batchs). Fiche **44-sentrux** → `analysis/lang_registry.rs` + `analysis/parser/` (pipeline tree-sitter multi-langage, registre dynamique 50+ langages sans logique compilée en dur). **Statut inchangé : HORS-SCOPE** tant que l'usage reste la création de code de zéro. — *Références : fiches **30-Ix**, **35-Understand-Anything**, **44-sentrux**.*

## 🔵 Priorité 5 : L'Automatisation de l'Environnement
- [x] **Auto-Résolution des Dépendances** : Si le nœud `Tester` détecte un `ModuleNotFoundError`, lancer automatiquement un `uv add <package>` ou `pip install` avant de relancer les tests, évitant de gâcher un cycle LLM pour ça. — *FAIT (F-26) : `extract_missing_module` (regex + validation identifiant anti-injection) + `_install_module` (`pip install` non-persistant, jamais d'exception) branchés dans `PythonTestRunner.run()`. Au 1er échec `ModuleNotFoundError`, installe le module puis relance (cap 1 retry). Opt-out `AUTO_INSTALL_DEPS=false`. 12 tests PASS.*

## 🟣 Priorité 6 : L'Évaluation Avancée & Stratégie TDD
*Suite à l'analyse croisée des prompts systèmes de référence (Open-SWE, LlamaBot), l'évaluation du code est le prochain goulot d'étranglement qualitatif.*
Plus d'information : `system_prompts_analysis.md`

> **🔎 État Actuel :** Les nœuds Juge et Security sont binaires. Le Web Tester effectue des vérifications volatiles via Puppeteer en console sans persistance de harnais de tests.
> **🚀 Pourquoi ce sera mieux :** Empêcher le Juge de bloquer le graphe sur des "nits" (débats de nommage) accélère le workflow. Produire des tests persistants (TDD RED-to-GREEN) garantit l'anti-régression au-delà de la session courante de l'agent.

- [x] **Code Review (Anti-Bruit & Rubric)** : Intégrer la stratégie Open-SWE dans les nœuds Security et Code Judge. Interdire les critiques de style, classer les retours (Critical, High, Medium, Low) et forcer l'ancrage strict sur le `diff` modifié pour réduire drastiquement les faux positifs et l'enlisement du Judge. — *Base doctrinale du Reviewer : fiche **17-system-prompts-and-models-of-ai-tools** → `Anthropic/Claude Code 2.0.txt` (« professional objectivity » : truth > validation, désaccorder l'utilisateur si nécessaire). C'est la posture attendue du Judge : il n'est pas là pour valider, mais pour évaluer objectivement.* — *FAIT (F-44) : Judge docstring enrichi (rubric critical/high/medium/low + IN-DIFF ONLY + ANTI-NITS + professional objectivity + vérification comportementale via task_requirements) ; Security docstring enrichi (OWASP Top 10 + scores CVSS + DEFENSIVE ONLY). Schéma `Finding` partagé (`severity/category/location/description/suggestion`) + champ additif `findings: List[Finding] = []` sur `CodeJudgeOutput` et `SecurityOutput` (non-cassant : défaut []).*
- [ ] **TDD Persistant (Web Tester)** : Modifier le workflow de test (inspiré de LlamaBot). Structurer le nœud Tester selon un pipeline strict : Bug Intake → Test Design → Failing Test (RED) → Verify (GREEN) → Hand-off. Le Testeur (web ou python) doit générer un vrai script automatisé, s'assurer qu'il échoue prouvant l'erreur, l'envoyer au Coder pour correction, puis vérifier sa réussite. — *Références (fiche **17-system-prompts** et **29-system-prompts-leaks**) : `Devin AI/Prompt.txt` (« ne jamais modifier les tests sauf demande explicite » = règle de non-régression du Tester) + `Gemini CLI` (workflow verify-after : Understand→Plan→Implement→**Verify Tests**→**Verify Standards**) + `Codex CLI` (philosophie de test « start specific → broaden », « fix root cause not surface »).*
- [x] **Nettoyage DOM (Inspiré de LlamaBot)** : Pour le Web Tester, ajouter un filtre de troncature HTML (suppression des `<svg>`, `<canvas>`, et `<script>`) avant l'envoi au LLM pour économiser massivement les tokens. — *FAIT (F-37) : `dom_filter.py` — `clean_dom_for_llm` strippe `<script>/<style>/<svg>/<canvas>/<iframe>/<noscript>/<template>/<head>` + commentaires HTML (regex case-insensitive + DOTALL, variants XHTML auto-fermants), compacte whitespace, tronque à `max_chars=8000`. Sans dépendance (pas de BeautifulSoup). Branché dans `web_tester.py` : directive "NETTOYAGE DOM" dans le prompt avec un snippet JS exécuté côté navigateur via `puppeteer_evaluate` (plus efficace qu'un round-trip Python). Gain mesuré : taille ÷3 sur un HTML réaliste (test_significant_token_reduction). 18 tests PASS.*
- [ ] **Structuration des "Findings" et Déduplication (Inspiré d'Open-SWE)** : Limiter le nombre de retours du Judge (cap strict, ex: 6 maximum) pour éviter l'enlisement. Ajouter un système de fingerprinting (empreinte SHA) pour dédupliquer les erreurs d'une itération à l'autre et ne pas rabâcher les mêmes remarques. — ***Judge auto-apprenant + taxonomie d'erreurs (audit fiche 25-hermes-agent, 2026-08-05)** : fiche **25-hermes-agent** → `references/hermes-agent/agent/background_review.py` (`spawn_background_review_thread`, `_digest_history`, `_bg_review_auto_deny` : pattern de fork thread daemon + whitelist stricte d'outils, pour un Judge qui crée/maj des skills et mémoires après chaque tour) + `references/hermes-agent/agent/error_classifier.py` (`classify_api_error`, `FailoverReason`, `_classify_400`/`_classify_402` : taxonomie fine d'erreurs API LLM, Python pur portable direct pour le nœud d'escalation/error recovery).*
- [ ] **Cycle de vie du Plan (Inspiré d'Open-SWE + learn-claude-code)** : Structurer l'étape de l'Architecte avec des statuts clairs (planning, ready, revising, approved). Le passage au Coder ne doit se faire que sur un statut "approved" (gate explicite). — *Référence production : fiche **09-open-swe** → `references/open-swe/agent/dashboard/plan_store.py` (`PLAN_STATUS_*`, 6 statuts, `save_plan_content`, cycle approve/reject complet).* — *Mini-orchestrateur DAG (Python natif) : fiche **16-learn-claude-code** → `s12_task_system/code.py` (`Task` dataclass avec `blockedBy`, `can_start`/`claim_task`/`complete_task` avec reportage du débloquage aval) + `s05_todo_write/code.py` (`nag reminder` qui réinjecte la liste si stale, `_normalize_todos` validation défensive car les LLM envoient souvent des strings au lieu de lists).*
- [x] **System Prompts Spécialisés (Inspiré de claude-code-unified-agents)** : Enrichir les prompts système des nœuds existants à partir des 8 prompts « purs » alignés avec les rôles du graphe — `code-reviewer` (Judge), `security-auditor` (Security), `test-engineer` (Tester : pyramide 70/20/10 + pattern AAA), `python-pro` (Coder : type hints + PEP 8), `frontend-specialist` (Coder web : a11y), `backend-architect` (Architect : 5 axes). — *Référence : fiche **15-claude-code-unified-agents** (53 agents, valeur concentrée dans ~8 prompts purs). Attention : les prompts restent souples (best practices), il faut les durcir en gates bloquants pour les rôles Judge/Security. Le schéma `AgentCapabilitySchema` de `agent-generator.md` fournit un modèle pour déclarer formellement un rôle.* — *FAIT (F-44) : cf. ROLE_BLOCKS dans prompts.py (couvre les 8 prompts purs alignés). Coder type hints + verify-after, WebTester pyramide+AAA, Judge/Security durcis en gates (rubric + in-diff + anti-nits + OWASP/CVSS), Architect read-only + 5 axes. Les marqueurs doctrinaux vérifiés par `test_role_contains_doctrinal_marker` (17 cas paramétrés).*

- [ ] *(audit fiches 39 + 42 + 43 + 44, 2026-08-12)* **Enrichissement du Judge** (4 angles complémentaires au Judge F-44/F-65) :
  - **Grounding des findings par alignement** (fiche **39-langextract**) : ancrer chaque finding LLM dans le code source via `WordAligner` (alignement difflib/LCS) → rejeter les findings hallucinés (localisation inexistante dans le fichier). Élimine le failure mode "finding sur une ligne qui n'existe pas". (F-93) — **✅ FAIT** : module `judge_grounding.py` (0 LLM, miroir F-70) branché post-LLM dans le Judge (fail-open). Port sliding-window langextract + line-range check. **Politique Option 1 non-destructive** : findings non ancrés rétrogradés d'un cran + flagués `[ungrounded]`, `is_approved` JAMAIS changé (calibrage d'abord ; automation du verdict en cycle futur après mesure du taux de faux-positifs par le Meta-Analyste P15). 36 tests + isolation `debug/run_judge_grounding.py`. 0 régression.
  - **Judge pairwise anti-biais** (fiche **42-anydoc**) : évaluation pairwise A/B avec inversion de position pour annuler le biais de position (`bench/judge.py` + `PROMPT`). Complément orthogonal au council llm-council (fiche 22).
  - **Revue 3-axes + debug par hypothèses** (fiche **43-framework**) : `plugins/aidd-dev/skills/05-review/SKILL.md` (axes code/functional/relevancy + rubric de verdict stricte) + `08-debug/SKILL.md` (procédure prescriptive 11 étapes par validation d'hypothèses, empêche les drive-by refactors).
  - **Capteur de santé structurelle** (fiche **44-sentrux**) : 5 métriques racines (modularité/acyclicité/profondeur/égalité/redondance) snapshot avant (`session_start`) / après (`session_end`) l'intervention de l'agent → bloquer ou corriger si le score architectural global se dégrade. Boucle de feedback sur la qualité, au-delà du verdict binaire. (F-94)
  — *Références : fiche **39-langextract** → `langextract/resolver.py` (`WordAligner`) ; fiche **42-anydoc** → `bench/judge.py` ; fiche **43-framework** → `plugins/aidd-dev/skills/05-review/SKILL.md` + `08-debug/SKILL.md` ; fiche **44-sentrux** → `sentrux-core/src/metrics/root_causes.rs`.*

### P6-bis : Enrichissement du Judge — signaux quantitatifs + council + deux axes
> 📈 **Évolution** (audit références 20/22/23, 2026-08-03). Le Judge actuel (F-44) ne s'appuie que sur du **qualitatif LLM**. Trois nouvelles références apportent des enrichissements complémentaires (signaux quantitatifs, arbitrage multi-juges, format deux-axes).

- [ ] **Signaux quantitatifs de risque pour le Judge** : Injecter un `risk_score ∈ [0,1]` composite (test coverage + security keywords + caller count + churn) comme **contexte structuré** au prompt du Judge, pour qu'il priorise ses findings sur des signaux mesurables et non seulement sur son intuition. Calibrer les seuils de bucketing `high/medium/low` initialement à 0.7/0.4. — *Référence : fiche **20-code-review-graph** → `code_review_graph/changes.py` (`compute_risk_score(store, node, churn_counts) -> float`, poids : flow participation 0.25, cross-community callers 0.15, test coverage transitive 0.30→0.05, security keywords +0.20, caller count 0.10, churn 0.15) + `code_review_graph/tools/context.py` (bucketing `>0.7 high, >0.4 medium`) + `code_review_graph/constants.py` (`IMPACT_EDGE_WEIGHTS`, `SECURITY_KEYWORDS` réutilisables telles quelles). Action suggérée : module `judge_signals.py`.*
- [ ] **Council anonymisé (option d'enrichissement pour findings à enjeu)** : Pour les findings critiques à enjeu, activer un « council » : N évaluations LLM indépendantes se jugent mutuellement **à l'aveugle** (identités anonymisées par labels A/B/C), puis un Chairman compile. Coût 2N+1 appels → réserver aux findings à enjeu, pas au run systématique (incompatible GPU local). — *Référence : fiche **22-llm-council** → `backend/council.py` (`stage1_collect_responses`/`stage2_collect_rankings`/`stage3_synthesize_final`, anonymisation `label_to_model = {"Response A": ...}` jamais envoyé aux juges, prompt strict `FINAL RANKING:`, agrégation Borda par position moyenne). ⚠️ Réserves : vibe-coded non testé, coût 2N+1 appels, OpenRouter payant — traiter comme **inspiration de pattern**, pas dépendance. À reloger sur nos modèles locaux.*
- [ ] **Judge à deux axes (Standards + Spec)** : Faire évaluer le Judge sur deux axes parallèles — **Standards** (conventions repo + coding standards de référence type Fowler smells) et **Spec** (conformité au cahier des charges / PRD) — sans fusionner les évaluations (un finding ne peut pas être compensé par l'autre axe). — *Référence : fiche **23-mattpocock-skills** → `skills/engineering/code-review/SKILL.md` (two axes en sub-agents parallèles jamais fusionnés ni re-rankés, Fowler smell baseline 12, `git diff <fixed-point>...HEAD`).*
- [x] **Diff multi-fichiers pour ancrer le Judge** *(audit fiches 09-open-swe + 20-code-review-graph, 2026-08-04)* : Le Judge (F-44) exige « IN-DIFF ONLY » mais ne dispose pas de brique pour **construire** ce diff multi-fichiers (le `runs/` est versionné git local depuis F-53, mais le diff n'est consommé que par le Tester ciblé, pas formalisé pour le Judge). (a) Diff full-content multi-fichiers à la `build_pr_diff_files` (open-swe) ; (b) format de sortie structuré pour le Judge (`Risk Assessment` / `File-by-File` / `Recommendations`) à la `skills/review-pr/SKILL.md` (code-review-graph). — *Références : fiche **09-open-swe** → `references/open-swe/agent/dashboard/pr_diff.py` (`build_pr_diff_files`) ; fiche **20-code-review-graph** → `code_review_graph/skills/review-pr/SKILL.md`.* — *FAIT (F-70) : module `graph_orchestrator/judge_diff.py` (`build_judge_code_block`, 0 LLM) + branchement `execute_code_judge_node`. Découverte clé : `sub_dict['git_diff']` (git unified diff nativement multi-fichiers, F-53 `git_snapshot.get_last_diff`) était DEJA calculé et propagé (`workflows.py:583`) mais consommé uniquement par le web_tester — jamais par le Judge. Iter 1 (diff vide) = full-file rétrocompat ; iter >1 = bloc diff annoté IN-DIFF ONLY + full-file tronqué (contexte vérif exigences). Aucune modif workflows.py/git_snapshot.py/prompts.py. (a) écarts justifiés : `build_pr_diff_files` (fetcher GitHub UI) écarté — on a déjà git local via F-53 ; `compute_risk_score` écarté (KG structural = P4 HORS-SCOPE).*
- [x] **Métriques de benchmark du Judge (P/R/F1, MRR)** *(audit fiche 20-code-review-graph, 2026-08-04)* : Aujourd'hui le Judge est évalué qualitativement (lecture de logs). Pour la P15 (Meta-Analyste) et toute regression-test du Judge, il faut des métriques quantitatives : `compute_precision_recall` (sur un jeu de findings attendus) et `compute_mrr` (mean reciprocal rank sur l'ordonnancement des findings). — *Référence : fiche **20-code-review-graph** → `code_review_graph/eval/scorer.py` (`compute_precision_recall`, `compute_mrr`).* — *FAIT (F-70) : module `graph_orchestrator/judge_metrics.py` (offline, 0 LLM). `canonicalize_finding` → ID stable `location|category|severity` (insensible à la paraphrase, extension consciente vs le scorer de référence identity-based) + `compute_precision_recall` + `compute_mrr` (ports typés de la réf) + `judge_verdict_accuracy` (TP/FP/FN sur `is_approved`). Évaluations OFFLINE (P15 Meta-Analyste + regression tests), pas dans la boucle chaude.*
- [ ] **Mécanisme d'évaluation LLM Juge avec Vision** : Utiliser un prompt structuré qui combine la tâche, les étapes de l'agent, et les captures d'écran pour qu'un Judge puisse statuer sur la réussite d'une exécution UI/Web. — *Référence : fiche **27-browser-use** → `references/browser-use/browser_use/agent/judge.py` (`construct_judge_messages`).*

- [ ] *(audit fiche 40-waku-agent, 2026-08-12)* **Isolation stricte du Judge (LLM-as-judge reproductible)** : S'inspirer de `evals/judge/test_response_quality.py` (isolation stricte d'un LLM-as-judge via DeepEval). Complément méthodologique aux métriques offline de F-70 (`judge_metrics.py`) — objectiver la reproductibilité du verdict Judge au-delà du calcul P/R/F1 post-mortem. — *Référence : fiche **40-waku-agent** → `references/waku-agent/evals/judge/test_response_quality.py`.*

- [ ] *(update références 2026-08-14)* **Recettes de vérification exécutable (F-100)** : au-delà du grounding de localisation (F-93) et du diff (F-70), le Judge manque d'une preuve d'**exécution**. hermes-agent a ajouté un module `verify` : détection statique de la stack (Node/Python-Django-FastAPI/Go/Rust/Java/Makefile/docker-compose) → commandes bootstrap/build/test/start + port/chemin de readiness, puis boucle de readiness HTTP (timeouts 600s/60s, tail de sortie 2000 chars). Pour notre Static Tester/Tester : « la page est servie et répond » devient une preuve exécutable au lieu de `file://` + console seulement. Python pur, portage direct. — *Référence : fiche **25-hermes-agent** → `references/hermes-agent/agent/verify/` (`recipes.py` : `Recipe` + détection stack ; `runner.py` : `PhaseResult`/`ReadinessResult` + boucle de readiness ; `environment.py`). Mention : Claude Code v2.1.232 a ajouté une skill `code-review` à niveaux d'effort low/medium/high/ultra (findings incertains assumés au niveau high) — calibrage possible de notre rubric Judge (fiche **29-system-prompts-leaks** → `Anthropic/Claude Code/claude-code-fable-5.md`).*
- [ ] *(audit fiches 45 + 46, 2026-08-14)* **Format de findings auto-déclarés + gabarit de grounding symptomatique** : (a) fiche **46-deepseek-harness** → `packages/workflow/tool-ralph/README.md` (handoff structuré `status: continue|complete|blocked` + `summary` + **`evidence`** + `nextSteps` + `blocker`, borné `maxHandoffChars`) : un format éprouvé de rapport d'agent **auto-déclaré avec preuves exigées** — candidate pour le format de sortie du Coder vers le Judge (chaque claim du Coder accompagné de son `evidence`). Le README documente honnêtement la limite « Completion is worker self-declaration — no independent evaluator » : **argument de plus pour notre Judge indépendant** (notre architecture Coder→Judge est déjà dans le bon sens, ne pas la régresser). (b) fiche **45-OpenSandbox** → `skills/troubleshoot-sandbox/SKILL.md` (tableau **symptôme → vérification → cause probable** : exit codes 137/126/127, OOMKilled, ImagePullBackOff) : gabarit de rubric déterministe pour ancrer les findings du Judge/Security sur des symptômes observables plutôt que de l'intuition (complément du grounding F-93 qui ancre la localisation, ceci ancre le diagnostic). — *Références : fiche **46-deepseek-harness** → `references/deepseek-harness/packages/workflow/tool-ralph/README.md` ; fiche **45-OpenSandbox** → `references/OpenSandbox/skills/troubleshoot-sandbox/SKILL.md`.*

### P6-ter : Mémoire durable du Knowledge Graph (consolidation + scratch/notebook)
> 🧠 **Nouvelle sous-section** (audit fiche **14-qm**, 2026-08-04 ; enrichi fiche **28-TencentDB-Agent-Memory**, 2026-08-07). Le gisement d'écarts le plus massif de l'audit de cohérence est **qm** : sur les 9 briques 🟢 Haute du Hall of Fame qm, le plan n'en exploitait que 3 (compaction P9, idempotence F-43, sandbox P8-bis). Les briques **mémoire** sont oubliées — or le projet a un KG DuckDB de claims/refutations qui **grossit indéfiniment sans dédup ni consolidation** (le nœud d'escalade F-23 ne fait que synthétiser, pas consolider). L'INDEX (l.100) prévient explicitement que c'est « la plus grande valeur de qm — ignorée par l'ancienne fiche ». **Enrichissement TencentDB (2026-08-07)** : qm couvrait le contrat de persistance scratch/notebook, mais **pas** l'extraction sémantique des atomes, ni la dédup cross-type, ni l'oubli par chaleur. TencentDB comble ces 3 maillons manquants (pipeline L0→L3 complet, dédup `store/skip/update/merge` supérieure, recall hybride + split stable/dynamic) — les 2 références sont complémentaires et **nécessaires ensemble** pour F-68.

- [x] **Consolidation des claims par LLM-juge** *(F-68 Phase 1)* : Nœud DSPy `execute_consolidation_node` qui consolide/déduplique les claims du KG par entité via un LLM-juge émettant des actions `UPDATE/DELETE/ADD` (format qm line-oriented adapté en JSON typé via OutputField DSPy — plus fiable sur 9B que du texte libre). Évite le rabâchage des mêmes réfutations d'une itération à l'autre. L'applier `apply_consolidation_actions` (port qm `applyConsolidationActions`, 0 LLM) applique les actions de façon déterministe (DELETE-d'abord puis UPDATE puis ADD, index 1-based, fail-open sur index invalide). Seuil de déclenchement `memory_consolidation_after=10` (qm `DEFAULT_CONSOLIDATE_AFTER`). Branché en fin de run (workflows.py, après boucle sous-tâches, avant `clear_checkpoint`). Oubli par rétention temporelle `prune_old_claims` (miroir `prune_idempotency`, préserve `escalation`+`insight` = leçons durables, défaut 30j). Nouvelles méthodes KG : `get_claims_by_run`, `get_entities_by_run`, `update_claim_content`, `delete_claim` (CASCADE manuelle provenance+edges). 24 tests PASS. Dégradation gracieuse (LLM down → KG intact). — *Référence : fiche **14-qm** → `references/qm/src/memory/strategies/consolidation.ts` (`MEMORY_CONSOLIDATION_PROMPT`, `applyConsolidationActions`, `ConsolidationAction`). L'INDEX pointe explicitement « consolidation de claims pour le Judge (Priorité 6) ».*
- [ ] **Mémoire durable deux-tiers (scratch volatile + notebook consolidé)** *(F-68)* : Structurer la persistance du KG en deux tiers : un **scratch** volatile (claims de la session courante, déjà couvert par DuckDB) + un **notebook** consolidé durable (faits survivant d'un run à l'autre). Contrat `recall`/`capture`/`replaceIfRevision` + dédup normalisée + concurrence optimiste par révision (`MAX_FACTS=300`). C'est le socle d'une vraie mémoire cross-run (aujourd'hui chaque run repart de zéro malgré le KG). — *Référence : fiche **14-qm** → `references/qm/src/memory/strategies/scratch-promote.ts` (`createScratchPromote`, `PROMOTION_PROMPT`) + `memory-service.ts` (`MemoryService`, `foldCapture`, `replaceIfRevision`, `MAX_FACTS=300`).*
- [ ] **Juge empirique (vitest-evals)** *(audit fiche 24-pi, 2026-08-05)* : Intégrer un système d'évaluation similaire à vitest-evals de `pi`, qui confronte directement le Judge LLM à des tests automatisés pour valider ses findings empiriquement, transformant le Judge d'un simple linter qualitatif en un vérificateur de comportement. — *Référence : fiche **24-pi** → `packages/evals/src/vitest-evals/harness-table.ts`.*
- [ ] **Pipeline mémoire L0→L3 (extraction + dédup + consolidation + oubli)** *(audit fiche **28-TencentDB-Agent-Memory**, 2026-08-07)* : Au-delà de la consolidation qm (ligne `Consolidation des claims` ci-dessus), TencentDB fournit un pipeline complet et **supérieur** sur 2 des 3 maillons. **(a) Extraction L1** : adopter les 3 principes 宁缺毋滥 (qualité>quantité, filtrer le trivial), 独立完整 (chaque atome reste valide **hors de son contexte d'extraction**), 归纳合并 (fusion causale des messages liés) + dualité chat/code (persona/episodic/instruction vs work_fact/work_task/work_method/work_artifact). **(b) Dédup L1** : remplacer/étendre le contrat `UPDATE/DELETE/ADD` de qm par `store/skip/update/merge` qui gère en plus le **merge cross-type** + **many-to-many** (`target_ids`) + **bump de priorité sur merge** (2× prio 70 → 80). **(c) Consolidation L2 par chaleur** : le mécanisme d'oubli qui manque totalement à notre KG (qui grossit indéfiniment) — `maxScenes` cap + warnings red/orange/yellow + MERGE-before-CREATE + **heat** (new=1, update=old+1, merge=sum+1) + soft-delete `[DELETED]`. — *Référence : fiche **28-TencentDB-Agent-Memory** → `MemoryCore/src/core/prompts/l1-extraction.ts` (`EXTRACT_MEMORIES_SYSTEM_PROMPT`/`EXTRACT_WORK_MEMORIES_SYSTEM_PROMPT`, principes 独立完整/归纳合并), `l1-dedup.ts` (`CONFLICT_DETECTION_SYSTEM_PROMPT`, actions store/skip/update/merge, `formatBatchConflictPrompt`), `scene-extraction.ts` (`buildSceneSystemPrompt`, heat + `[DELETED]` + maxScenes).* — *Compléter F-68 : qm couvrait le contrat `recall`/scratch, TencentDB couvre l'extraction+dédup+oubli. Les deux sont nécessaires.*
- [ ] **Recall hybride + split stable/dynamic** *(audit fiche **28-TencentDB-Agent-Memory**, 2026-08-07)* : Au moment d'implémenter le `recall` du notebook F-68, adopter la stratégie de retrieval de TencentDB : fusion BM25 + vecteur + RRF, **caps** (item count / char budget / timeout) pour ne pas saturer la fenêtre, et surtout le **split stable (persona + index scène, cacheable) / dynamic (L1 per-turn)** qui préserve le **prompt caching** (subtilité absente du scratch/notebook qm). Ajouter les **4 trigger conditions "must-search-before-answering"** et la règle anti-hallucination « je n'ai pas trouvé en mémoire » plutôt qu'inventer. — *Référence : fiche **28-TencentDB-Agent-Memory** → `MemoryCore/src/core/hooks/auto-recall.ts` (`appendSystemContext` stable vs `prependContext` dynamic) + `MemoryProxy/src/injection/injectors/tdai-profile-memory-injector.ts` (`MEMORY_TOOLS_GUIDE`, 4 trigger conditions).*


- [ ] *(audit fiche 40-waku-agent, 2026-08-12)* **Decisioning d'injection mémoire + distillation périodique** : Compléter le recall F-68 (Phase 2) par un gate de décision `should_retrieve` (évite l'injection systématique des leçons quand non pertinent) et une distillation périodique `consolidate_if_due` (déclenche la consolidation sur critère temporel plutôt qu'en fin de run seulement). — *Référence : fiche **40-waku-agent** → `references/waku-agent/waku/memory/retrieval_gate.py` (`should_retrieve`) + `waku/memory/consolidation.py` (`consolidate_if_due`).*

## 🟤 Priorité 7 : Le "Shift Left" (Linter-as-a-Reviewer) ✅ TERMINÉ
*Suite à l'analyse croisée des références (Aider, OpenCode), filtrer les erreurs de syntaxe de base avant d'engager l'orchestrateur lourd permet des économies massives.*

> **🔎 État Actuel :** Le graphe exécute le code (via Tester) pour capter les erreurs. C'est lent et cher si l'erreur est un simple "deux-points manquant" ou une variable non définie.
> **🚀 Pourquoi ce sera mieux :** L'utilisation de linters locaux très rapides agit comme un filtre "pas cher". Les nœuds lourdes (Tester, Judge) ne sont sollicitées que lorsque le code est syntaxiquement valide.

- [x] **Linter-as-a-Reviewer** : Intégrer un linter ultra-rapide (ex: AST Tree-sitter, Flake8, oxlint) qui s'exécute immédiatement après la génération de code du Coder. Si la compilation syntaxique échoue, forcer une boucle d'auto-correction locale et rapide sans déclencher le reste du graphe. — *FAIT : `linter.py` (317 lignes, 0 LLM) existe et est branché dans `run_coding_workflow` (`workflows.py:381-402`) — il court-circuite le nœud Tester si une erreur de syntaxe est détectée, renvoyant directement le Coder en correction. (Incohérence doc/code corrigée le 2026-07-31 : cette priorité était marquée ❌ à tort.)*

## ⚪ Priorité 8 : Middlewares d'Auto-Réparation (Anti-Crash)
*Suite à l'analyse d'Open-SWE, l'orchestrateur doit être capable de survivre aux hallucinations JSON et aux coupures systèmes.*

> **🔎 État Actuel :** Les outils crashent ou renvoient une erreur 400 (Bad Request) à l'API LLM si les arguments JSON sont mal typés. Pire, si un Checkpoint charge un historique contenant un appel d'outil sans résultat, l'API LLM plantera systématiquement en bloquant le graphe entier.
> **🚀 Pourquoi ce sera mieux :** Cacher ces erreurs au graphe principal grâce à des "Middlewares" (proxys) permet de sauver des itérations précieuses et de rendre le système 100% résilient (plus aucun crash lié à l'historique ou aux typos LLM).

- [x] **Sanitizer (Auto-typage)** : Implémenter un proxy avant l'exécution des outils qui détecte les arguments mal formés (ex: `"1, 80"` au lieu de `80`) et les caste silencieusement pour éviter l'échec de validation Pydantic. (Validé par les approches d'Input/Output Sanitization de DeerFlow). — *Implémentation F-42 : `sanitizer.py` (100% Python natif, 0 LLM, déterministe, best-effort). `SanitizedTool(BaseTool)` proxy interceptant `__call__` avant `forward` (chemin CodeAgent confirmé : executor local appelle les outils via `__call__` directement, PAS `execute_tool_call`/`validate_tool_arguments`). Coercition selon le schéma `tool.inputs` RÉEL (integer=dernier nombre d'une chaîne, boolean, array/object via `json.loads`→`ast.literal_eval` fallback). Valeur non coercible laissée telle quelle → validation smolagents reste l'arbitre final. Branché `execute_coder_node` + `execute_architect_node` via `sanitize_tools(enabled=settings.sanitizer_enabled)`. Opt-out `SANITIZER_ENABLED`. 23 tests PASS. Inspiré DeerFlow InputSanitization + learn-claude-code `_normalize_todos`.*
- [x] **Orphan Repair (Anti-corruption d'historique)** : Au démarrage (chargement de Checkpoint), scanner l'historique des messages. Si un `ToolCall` n'a pas de réponse associée, injecter un faux message `{"status": "error", "error": "Interrompu"}` pour permettre à l'agent de reprendre au lieu de faire crasher l'API. — *Blueprint + test (Python natif) : fiche **16-learn-claude-code** → `s08_context_compact/code.py` (les 4 couches de compaction préservent explicitement les paires `tool_use`/`tool_result`) + `tests/test_compaction_tool_pairs.py` (`assert_no_orphan_tool_results`, 5 scénarios). C'est le test exact de cette préoccupation.*
- [ ] **Middlewares de Contrôle d'Exécution (Inspiré de DeerFlow & Open-SWE)** :
  - *LoopDetectionMiddleware* : Détecter la stagnation et forcer une réponse finale pour stopper une boucle infinie d'outils.
  - *ToolOutputBudgetMiddleware* : Tronquer automatiquement les résultats d'outils trop longs au niveau du middleware pour protéger le budget de tokens.
  - *Circuit Breaker* : Gérer les timeouts de modèles ou les crashs distants de manière gracieuse. — *Squelette anti-crash (Python natif) : fiche **16-learn-claude-code** → `s11_error_recovery/code.py` (`RecoveryState` + `with_retry` backoff exponentiel+jitter max 10 + escalation max_tokens 8K→64K + fallback model après 529 consécutifs).* — ***Middleware de retry résilient production-éprouvé (audit fiche 45-OpenSandbox, 2026-08-14)** : fiche **45-OpenSandbox** → `sdks/sandbox/python/src/opensandbox/transport/retry.py` + `_classify.py` + `_async_retry.py` (`RetryPolicy` frozen dataclass : backoff x2 plafonné 30s + jitter DÉCORRÉLÉ, codes retryables distingués idempotent/non-idempotent, `per_attempt_timeout` + `overall_deadline` mur-mur, hook `on_retry` ; `classify_transport_exception` = classification pré-send (safe sur toute méthode) vs post-send (idempotents seulement) ; `Retry-After` honoré et plafonné, sleep clampé au budget restant, réponse rejetée `aclose()`d). **Python pur copiable tel quel** (seule dépendance httpx) pour nos appels llama.cpp/MCP — design documenté dans `oseps/0017-resilient-sdk-transport.md`. C'est la brique P8 la plus directement exploitable du dossier références.* — ***Retry durable + timeout d'outil (audit fiche 46-deepseek-harness, 2026-08-14)** : fiche **46-deepseek-harness** → `packages/llm/llm-retry/src/index.ts` (le compteur de retries est **persisté dans le log de session et relu au restart** → un retry survit à un crash — exactement ce que notre checkpoint F-24 ne couvre pas au niveau de l'appel LLM) + `packages/guard/timeout-policy/src/index.ts` (`TOOL_TIMEOUT` : deadline coopérative sur l'exécution d'outil avec code d'erreur structuré routable par retry — borne nos appels MCP chrome-devtools pendants) + `packages/llm/llm/src/assembler.ts` (`BlockAssembler` : parsing streaming tolérant aux protocoles delta-only, premiers block-end gagnants) + `docs/tool-execution-pipeline.md` (**spec des 5 waterfalls** d'exécution d'outil : pre-execute → guards monotones → approval → execute (timeout/retry) → post-execute accept/block/replace/add-contexts — le cahier des charges formel de notre chaîne de middlewares P8, à côté du contrat 4-kinds hermes-agent).*
- [ ] *(update références 2026-08-14)* **Retry LLM v2 — pré-contenu vs mid-stream + init MCP non bloquante (F-104)** : compléments au retry résilient OpenSandbox (ci-dessus) et au `with_retry` F-42. (1) openfox distingue les échecs AVANT tout contenu (retry de la même requête, RIEN n'entre à l'historique — `message.start` différé au premier event streamé) des échecs MID-STREAM (bulle partielle conservée `partial:true` + exactement UN prompt de continuation visible) + re-résolution du client LLM à chaque tentative (switch de provider mid-backoff pris en compte). (2) opencode : jitter 25% (`RETRY_JITTER_FACTOR`) + `RETRY_MAX_RETRIES=5` + cap 30s + respect prioritaire `retry-after-ms`. (3) crush : init MCP JAMAIS bloquante en interactif (palette construite sur l'état actuel, serveurs lents rattrapés au tour suivant via `EventToolsListChanged`, timeout PAR SERVEUR — un serveur pendu ne bloque pas le run) — directement applicable à chrome-devtools ; écho Claude Code v2.1.232 `WaitForMcpServers`. (4) Fix deer-flow DanglingToolCallMiddleware : ne collecter `additional_kwargs.tool_calls` que si `tool_calls` ET `invalid_tool_calls` sont vides tous les deux (sinon double ToolMessage pour un id = 400 provider). — *Références : fiches **12-openfox** → `references/openfox/src/server/chat/agent-loop.ts` ; **11-opencode** → `packages/opencode/src/session/retry.ts` ; **10-crush** → `internal/agent/coordinator.go` ; **08-deer-flow** → `backend/packages/harness/deerflow/agents/middlewares/dangling_tool_call_middleware.py` ; **29-system-prompts-leaks** → `Anthropic/Claude Code/claude-code-fable-5.md`.*

> **🏗️ Architecture (patron commun, Python natif)** : tous ces middlewares s'implémentent sur le **même patron d'hooks** — fiche **16-learn-claude-code** → `s04_hooks/code.py` (`HOOKS = {event: [callbacks]}` + `trigger_hooks` qui court-circuite si un callback retourne non-None ; events `PreToolUse`/`PostToolUse`/`UserPromptSubmit`/`Stop`). C'est le squelette unifié de la chaîne de middlewares (Sanitizer/Orphan/Loop) ET de l'event stream (Priorité 11). ~30 lignes pour le système complet.

> **📦 Contrat middleware 4 kinds + bibliothèque sécurité Python pur (audit fiche 25-hermes-agent, 2026-08-05)** : hermes-agent apporte DEUX briques complémentaires. **(1) Contrat middleware formel** : fiche **25-hermes-agent** → `references/hermes-agent/docs/middleware/README.md` (4 kinds `llm_request`/`tool_request`/`llm_execution`/`tool_execution` avec `next_call` chain of responsibility, **fail-open** explicite, traçabilité via `middleware_trace`, versions de schema `hermes.middleware.v1`). Le design `tool_request` avant guardrails/approvals puis `tool_execution` après est le modèle à transposer pour P8 — plus riche que le patron hooks learn-claude-code (4 points d'extension typés vs events génériques). **(2) Bibliothèque sécurité multi-couches copiable quasi telle quelles** : `references/hermes-agent/tools/threat_patterns.py` (`scan_for_threats(content, scope)` scope-aware all/context/strict, `MAX_SCAN_CHARS`, couvre prompt injection/C2/frameworks offensifs/exfiltration) + `tools/approval.py` (`DANGEROUS_PATTERNS` ~260 regex, `detect_dangerous_command`) + `tools/path_security.py` (`validate_within_dir`, `has_traversal_component`) + `tools/url_safety.py` (guards **SSRF** : `is_safe_url`, `_is_blocked_ip`, `create_ssrf_safe_client` intercepte `connect_tcp` pour bloquer IPs privées/métadonnées cloud). Enrichit davidondrej (21, 27 regex) et notre `bash_guard.py` (F-38), et couvre l'angle SSRF/ traversal qui n'est nulle part ailleurs dans les références.

- [ ] *(audit fiches 33 + 39, 2026-08-12)* **Middlewares complémentaires** : (a) fiche **33-room** → `src/shared/rate-limit.ts` (`RATE_LIMIT_PATTERNS`, détection des rate limits 429 sur logs texte + sleep automatique — utile si l'orchestrateur consomme un jour une API distante payante) ; (b) fiche **39-langextract** → `langextract/core/format_handler.py` (`FormatHandler._parse_with_fallback`, parsing robuste gérant les balises `<think>` et fallback multi-formats — durcir la robustesse du parseur de sortie des nœuds DSPy). — *Références : fiche **33-room** → `src/shared/rate-limit.ts` ; fiche **39-langextract** → `langextract/core/format_handler.py`.*

## 🔧 Priorité 8-bis : Robustesse Runtime (Isolation & Idempotence)
*qm est une plateforme agent production-éprouvée (Slack/Web) ; ses primitives d'isolation et d'idempotence comblent les angles morts de l'orchestrateur actuel.*

> **🔎 État Actuel :** `bash_command` (`tools.py:208`) tourne en `subprocess.run(shell=True)` sans aucune isolation — risque d'injection et surface d'attaque réelle. C'est le **bloqueur de la transition CodeAgent** (P0 : un CodeAgent génère et exécute du Python arbitraire). Par ailleurs, les replays de checkpoint peuvent réappliquer des effets de bord (écritures fichiers, installs).
> **🚀 Pourquoi ce sera mieux :** Isoler l'exécution (sandbox) débloque le CodeAgent et sécurise le système. Garantir l'idempotence des effets de bord rend les replays/retries sûrs.

- [x] *(couche 1, faite)* **Guard bash denylist** : Avant la sandbox Docker (lourde), un guard denylist (`bash_guard.py`) bloque les commandes manifestement destructrices (`rm -rf /`, `format`, `mkfs`, `dd of=/dev/sd*`, `shutdown`, `git push --force`, `curl|sh`...) AVANT `subprocess.run(shell=True)`. C'est le premier pas concret vers la robustesse runtime ; la sandbox complète reste ci-dessous. — *FAIT (F-38) : `bash_guard.py` — `check_bash_command` renvoie `(allowed, reason)` (jamais d'exception), denylist regex case-insensitive Unix+Windows+cross. Branché dans `tools.py` `bash_command`. Opt-out `BASH_GUARD_ENABLED=false`. 66 tests PASS.* — *Enrichissement (audit fiche **21-davidondrej-skills**, 2026-08-03) : la denylist `dangerous-patterns.txt` apporte **27 regex POSIX-ERE** testées couvrant des patterns que notre F-38 n'a pas encore (`gh repo/release/secret/ssh-key/gpg-key delete`, `gh api -X DELETE`, `gh repo edit --visibility public`, `gh auth token`, fork bomb `:(){ :\|:& };:`, `git reflog expire --expire=now`, `git gc --prune=now|all`, `chmod 777 /`, `chown -R /`). À transpiler pour Python `re` (`[:space:]`→`\s`) et merger dans `bash_guard.py`. La **doctrine fail-open** (`deny-dangerous.sh` : rejet explicite citant le pattern, mais fail-open sur erreur) et la suite de **~115 tests** (`test-guard.sh` : block + allow, faux positifs comme `rm -rf node_modules` qui doit passer) sont prêtes à porter en pytest paramétrés. Doctrine companion : `global-agent-guardrails/SKILL.md` (« block catastrophic-only, allow recoverable »). ⚠️ Correction : la procédure Annexe D cite « 52 regex » mais c'est **27 regex** (fichier de 52 lignes dont 15 commentaires + 10 vides).*
- [ ] **Sandbox bash / pytest** : Cloisonner `bash_command` et le `PythonTestRunner` dans un environnement isolé (Docker exec ou process cloisonné avec fs/cwd restreint), au lieu du `shell=True` actuel. — *Référence production : fiche **14-qm** → `references/qm/src/sandbox/docker-exec.ts` + `local-sandbox.ts` (layers RO/RW, routing multi-backend) ; `references/aider/aider/commands.py` (`cmd_run` subprocess isolé). Débloque directement la Priorité 0 (CodeAgent). Le guard denylist (F-38) est une première couche, mais ne remplace pas l'isolation processus/fs complète.* — ***ABC de sandbox (audit fiche 25-hermes-agent, 2026-08-05)** : fiche **25-hermes-agent** → `references/hermes-agent/tools/environments/base.py` (`BaseEnvironment` ABC au contrat propre `execute(command)→{stdout,returncode,cwd}`, `_BoundedOutputCollector` cap la taille stdout head+tail+notice avec spill disque, `touch_activity_if_due` heartbeat 10s, `_pipe_stdin` gère Windows `\n`→`\r\n`). 7 backends concrets (Docker/SSH/Modal/Daytona/Vercel/Singularity/local) dont `local.py` et `docker.py` pertinents pour llama.cpp local. L'ABC + output collector borné + heartbeat sont 🟢 Python pur portable.* — ***Contrat de lifecycle sandbox (audit fiche 45-OpenSandbox, 2026-08-14)** : fiche **45-OpenSandbox** → `specs/sandbox-lifecycle.yml` (spec OpenAPI 1712 L, source de vérité d'une plateforme production ex-Alibaba/CNCF : création asynchrone **202+polling**, machine à états Running/Pausing/Paused/Resuming, **TTL absolu renouvelable** `renew-expiration` anti-fuite, **snapshot asynchrone** 202+`Location` et restore par `snapshotId` avec vérification `READY` + entrypoint neutre cf. `server/opensandbox_server/services/snapshot_restore.py` (`resolve_sandbox_image_from_request`, `DEFAULT_SNAPSHOT_RESTORE_ENTRYPOINT=["tail","-f","/dev/null"]`), endpoints signés à expiration, télémétrie `/metrics/events` fire-and-forget « MUST NOT affect usability »). À imiter comme CONTRAT de notre sandbox future : chaque exécution de code généré = environnement horodaté + snapshot avant mutation + restore avant retry + TTL. Sémantique du rejouable aussi dans `oseps/0013-isolated-execution-api.md` (sessions `run_once` idempotent). Runtime Go/K8s non portable — on prend le contrat, pas la plateforme.*
- [ ] *(update références 2026-08-14)* **Checkpoint git par itération Coder pour le Judge (F-102)** : complète F-53 (git_snapshot niveau run) et F-70 (judge_diff) au niveau ITÉRATION. open-swe snapshot le worktree à chaque tour sans le contaminer : arbre construit via `GIT_INDEX_FILE` scratch (`read-tree` + `add -A` + `write-tree`), `commit-tree` + `update-ref refs/open-swe/turns/<key>` (une REF et pas un tree nu : survit à un `git gc` mi-run), diff relu via `git diff --numstat` + `cat-file --batch`, tout best-effort (`None`/`status=error` si échec). Le Judge relit CE QUE GIT DIT de l'itération au lieu de rejouer les tool-calls. Python pur. — *Référence : fiche **09-open-swe** → `references/open-swe/agent/utils/turn_checkpoint.py`.*
- [ ] *(update références 2026-08-14)* **Durcissement sécurité : groupe 10 password managers + redaction secrets (F-105)** : (a) davidondrej a ajouté un groupe 10 « password managers & OS keychain » à sa denylist : CLIs `bw`/`bws`/`lpass`/`keepassxc-cli`/`rbw`/`nordpass`, `security find-generic-password|dump-keychain`, `gpg --export-secret-keys`, `~/.password-store` — `pass`/`op` bloqués avec leurs VRAIS subcommands seulement (« bare op/pass stay unblocked because they are common words », ~40 cas allow/block) ; à transpiler en `re.M` dans `bash_guard.py` F-38. (b) Directive « Redact » de mattpocock (secrets `<REDACTED>` avant de montrer/journaliser commandes/outputs — les HAR portent des auth headers ; repro construites sur env vars) pour la sanitization de nos traces avant Judge/persistance DuckDB. — *Références : fiches **21-davidondrej-skills** → `hooks/dangerous-patterns.txt` + `hooks/test-guard.sh` ; **23-mattpocock-skills** → `skills/engineering/diagnosing-bugs/SKILL.md` (section Redact).*
- [x] **Idempotence des effets de bord (replays/retries)** : Wrapper les opérations non-idempotentes (écritures fichiers, installs pip) via un guard `once(key, fn)` indexé par `run_id`+hash, pour qu'un replay de checkpoint n'applique pas deux fois le même effet. — *FAIT (F-43) : `idempotency.py` (port Python fidèle de `references/qm/src/idempotency/idempotency-store.ts`, 100% natif, 0 LLM). `IdempotencyStore.once(key, fn)` : inflight set + done map (RAM) + backing DuckDB durable (survit à un nouveau process). Marque done SEULEMENT si fn retourne sans lever (échec = retryable). Rétention 14j (pruning lazy). Contexte module-level (`_scoped_idempotency` → `get_current_store()`). Extension `knowledge_graph.py` : table `idempotency_record` + save/is_committed/prune/clear. `clear_idempotency(run_id)` aux MÊMES sites que `clear_checkpoint` (FRESH_START + fin de run). Branchement : `append_file` wrappé (write_file NON wrappé = idempotent par écrasement par design ; bash_command NON wrappé = couvert par bash_guard + loop_guard) ; `_install_module` wrappé via `_install_module_or_raise` (échec non marqué done → retryable). Opt-out `IDEMPOTENCE_ENABLED`. 25 tests PASS.*
- [ ] **Modèle de Gatekeepers (Capability-based Security)** *(audit fiche 26-cloudflare-os, 2026-08-06)* : Adopter une approche de sécurité basée sur les capacités (Capability-based access control). Plutôt que de donner tous les droits globaux, chaque agent reçoit des `bindings` granulaires (ex: accès réseau, accès fs partiel) via un `Overseer` central. — *Référence : fiche **26-cloudflare-os** → `packages/workshop-backend/src/overseer.ts` et `user.ts`. Les Gatekeepers valident l'activation des modules de sécurité avant de distribuer une capacité.*

- [ ] *(audit fiches 32 + 33 + 36, 2026-08-12)* **Robustesse FS & runtime** (5 briques complémentaires à F-38/F-43/F-67) :
  - **Transactions & Rollbacks par hardlinks** (fiche **32-OpenKB**) : journaliser les mutations du graphe/fichiers avec snapshot O(touched) via hardlinks + rollback déterministe au crash. Complémente l'idempotence F-43 (replays) par l'annulation des effets partiellement appliqués. (F-95)
  - **Verrous FS coopératifs** (fiche **32-OpenKB**) : read/write locks advisory cross-platform + atomicité stricte des remplacements de fichiers (`atomic_write_bytes`). Durcit notre Mutex F-20 (intra-process) vers du cross-process.
  - **Cloisonnement des outils IO** (fiche **32-OpenKB**) : allowlist stricte de chemins (`wiki/`, `output/`, `skills/`) appliquée avant exécution — renforce la ReadGate F-67 et le bash_guard F-38 avec une allowlist chemin (plutôt qu'une denylist commande).
  - **Contournement limite Windows 8191 chars** (fiche **33-room**) : `resolveNodeScript` extrait le script Node sous Windows pour contourner la limite de ligne de commande — utile si on génère des scripts JS longs pour DevTools/Puppeteer.
  - **Gates anti-emballement + OutputAccumulator** (fiche **36-prime-agent**) : `AgentAutonomousConfig` (limites de continuations, gates de validation) + `OutputAccumulator` (troncature stream-safe avec spill disque temporaire — miroir du `feedback_utils.truncate_output` mais streamé).
  — *Références : fiche **32-OpenKB** → `openkb/mutation.py` (`MutationSnapshot`, `snapshot_paths`, `_restore_hardlinked_dir`) + `openkb/locks.py` (`kb_lock`, `_LocalRwLock`, `atomic_write_bytes`) + `openkb/agent/tools.py` (`read_kb_file`, `write_kb_file`) ; fiche **33-room** → `src/shared/claude-code.ts` (`resolveNodeScript`) ; fiche **36-prime-agent** → `core/autonomous.ts` (`AgentAutonomousConfig`) + `core/tools/output-accumulator.ts` (`OutputAccumulator`).*

## 🟢 Priorité 9 : Optimisation de l'État du Graphe (Event-Sourcing & Reducers)
*Le maintien d'un contexte propre sur plusieurs itérations est vital pour les LLMs locaux.*

> **🔎 État Actuel :** Le graphe maintient une liste brute de messages qui grandit à l'infini, posant un risque fort de "Context Overflow".
> **🚀 Pourquoi ce sera mieux :** Inspiré par DeerFlow (Reducers typés), OpenFox (EventStore & Compaction) et surtout **qm** (compaction duale mature), structurer la mémoire par domaines empêche sa croissance linéaire.

- [x] **Reducers d'État (DeerFlow)** : Étendre l'état du graphe (GraphState) avec des reducers (ex: `artifacts`, `todos`, `summary_text`, `delegations`) pour compacter l'état sans perte d'information critique. — *FAIT / ARCHITECTUREL : Comme l'orchestrateur n'utilise pas LangGraph mais une boucle Python déterministe avec `KnowledgeGraph` (DuckDB), l'état est géré manuellement de façon transactionnelle dans `coding_state` (évitant le besoin de reducers concurrents). L'idempotence des retries (F-43) garantit la pureté de l'état sans les reducers complexes.*
- [x] **Compaction Automatique (qm + learn-claude-code)** : Intégrer un "Compactor" qui se déclenche lorsque la limite de contexte approche. Gérer la compaction via des "Context Epochs" : définir une baseline immuable et n'y rajouter que les deltas chronologiques. Remplacer l'historique brut par un résumé tout en conservant les instructions globales. — *FAIT : `compaction.py` avec un `CompactingCodeAgent` qui surcharge `write_memory_to_messages` pour appliquer les 3 couches L1 (snip), L2 (micro) et L3 (budget) avant chaque inférence. Le contexte est taillé de manière déterministe pour rester dans les limites, sans LLM supplémentaire.*
- [x] **Compaction File-State et Branch Summarization** *(audit fiche 24-pi, 2026-08-05)* : Améliorer la compaction existante en utilisant l'état des fichiers (file operations) plutôt qu'une troncature purement chronologique/longueur. De plus, lors d'un retour arrière ou undo (ex: le nœud Coder efface des modifs), utiliser un algorithme de "branch summarization" pour préserver l'apprentissage ("j'ai essayé cette approche et ça a échoué") tout en réduisant la longueur du contexte LLM. — *Référence : fiche **24-pi** → `packages/coding-agent/src/core/compaction/compaction.ts` et `branch-summarization.ts`.* — ***Compaction offline + live avec garde-fous (audit fiche 25-hermes-agent, 2026-08-05)** : fiche **25-hermes-agent** → `references/hermes-agent/trajectory_compressor.py` (`TrajectoryCompressor`, `CompressionConfig` : compaction **offline** d'une trajectoire JSONL post-exécution — protège head (system/first turn) + tail (last N turns), "snappe" les boundaries pour ne pas couper une paire `gpt→tool`, summarise le milieu via LLM, maintient un budget cible `target_max_tokens`. Python pur portable, réutilisable pour le mode "offline trajectory dataset" ET la compaction live en remplaçant le summarizer par Qwen local) + `references/hermes-agent/agent/context_compressor.py` (`ContextCompressor.should_compress`, `record_completed_compaction`, `get_active_compression_failure_cooldown` : compaction **live** mid-conversation avec garde-fous persistés cooldown/streak/ineffective = l'anti-loop de compaction qui manque au projet, cf. P3).*
- [x] **Compression de code par skeleton (libcst)** *(audit fiche 04-repograph, 2026-08-04)* : Réduire le contexte LLM en remplaçant les corps de fonctions par `...` via `get_skeleton` (garde ClassDef/FunctionDef/docstrings/constantes, supprime le corps). **Orthogonal au KG structural (P4 HORS-SCOPE)** : ce n'est pas une brique de knowledge graph, c'est un utilitaire de réduction de contexte applicable même en création de code (ex: réinjecter l'existant `runs/` ou un fichier partiellement écrit au Coder pour qu'il l'étende sans consommer tout le budget tokens). Le plan avait jeté `get_skeleton` avec le reste du trio académique RepoGraph/axon/graphify en P4 — à tort, c'est classé séparément 🟢 Haute dans le Hall of Fame. — *FAIT : lib `libcst` installée, `skeleton.py` implémenté d'après RepoGraph, et outil `read_python_skeleton` exposé au Coder (dans `tools.py`).*
- [ ] **Règles de Frontière de Phase (Phase Boundaries)** : Définir des règles d'arbitrage claires sur *quand* compacter le contexte vs continuer vs fork vs clear. La compaction ne doit intervenir qu'aux frontières de phase (ex: après une étape de design, avant l'implémentation) pour éviter que l'agent ne perde le fil de son raisonnement. (F-79) — *Référence : fiche **mattpocock-skills** → `skills/engineering/ask-matt/PHASE-BOUNDARIES.md`.*
- [ ] **Compaction du DOM (HTML → Markdown)** : Réduire l'empreinte contextuelle des pages web (pour le Web Tester ou un agent web) en extrayant le DOM sous forme de Markdown épuré. — *Référence : fiche **27-browser-use** → `references/browser-use/browser_use/dom/markdown_extractor.py` (`extract_clean_markdown`).*
- [ ] **Context-offload LLM (résumé scoré + cognitive tombstones)** *(audit fiche **28-TencentDB-Agent-Memory**, 2026-08-07)* : Notre `compaction.py` est **déterministe et 0 LLM** (snip/micro/budget) — c'est sa force (coût nul) mais sa limite (pas de sémantique). TencentDB propose un **complément sémantique** en 3 prompts pour les runs longs où la troncature déterministe perd de l'information : **(1) L1 summarization** : chaque paire tool-call + tool-result → résumé JSON **scoré 0-10** (3-step : 任务对齐/价值过滤/影响评估) qui décide si le résumé peut remplacer l'original ; **(2) L1.5 gatekeeper** : détecte la fin/continuation de cycle de tâche (`taskCompleted`/`isLongTask`/`continuationMmdFile`) ; **(3) L2 graphe Mermaid** : machine à états cognitive ≤4000 chars avec **"cognitive tombstones"** (`status: blocked` pour les culs-de-sac, au lieu de les tronquer silencieusement) + `node_mapping` exhaustif (1 tool_call = 1 nœud). Hypothèse d'intégration : **opt-in** sur runs longs (fan-out multi-sous-tâches) seulement, là où la compaction déterministe sature ; garder la compaction 0-LLM par défaut. — *Référence : fiche **28-TencentDB-Agent-Memory** → `MemoryCore/src/offload/local-llm/prompts/l1-prompt.ts` (`L1_SYSTEM_PROMPT`, scoring 0-10), `l15-prompt.ts` (gatekeeper), `l2-prompt.ts` (`L2_SYSTEM_PROMPT`, cognitive tombstones). `offload_server/prompts/` contient un jumeau quasi-identique.*

- [ ] *(audit fiches 35 + 36 + 39 + 42, 2026-08-12)* **Compaction complémentaire** (au P9 déjà livré : `compaction.py` 3 couches + skeleton libcst F-71) : (a) fiche **35-Understand-Anything** → `merge-batch-graphs.py` (`merge_graphs`, fusion Python de résultats de batchs pour construire un graphe global — pattern de réduction parallèle) ; (b) fiche **36-prime-agent** → `core/compaction/compaction.ts` (`createCompactionSummaryMessage`, réduction basée sur les opérations fichier accomplies — miroir du branch-summarization pi F-24, already-cité) ; (c) fiche **39-langextract** → `prompting.py` (`ContextAwarePromptBuilder`, conservation de la fin du chunk précédent dans le chunk suivant — pattern crucial pour l'analyse de gros fichiers découpés) ; (d) fiche **42-anydoc** → `_anydoc.pyi` (`Document`, typage AST clair pour ingérer/manipuler des documents bureautiques — élargit le périmètre d'ingestion au-delà du code). — *Références : fiches **35-Understand-Anything**, **36-prime-agent**, **39-langextract**, **42-anydoc**.*

- [ ] *(audit fiche 46-deepseek-harness, 2026-08-14)* **Compaction sémantique par checkpoint structuré + pruning gratuit + comptabilité** : notre `compaction.py` est déterministe 0-LLM (sa force, cf. la ligne Context-offload TencentDB pour le complément sémantique opt-in). deepseek-harness apporte la **meilleure matière du dossier pour ce complément** : (a) **Prompt de checkpoint 8 sections** : `packages/compaction/compaction-basic/src/summarizer.ts` (`COMPACTION_INSTRUCTION` : Primary Request and Intent / Key Technical Concepts / Files and Code / Errors and Fixes / Pending Jobs / Current Work / Next Step / Critical Context, avec règles « preserve exact paths, error strings » + fusion d'un checkpoint antérieur ; `summarizeWithLlm` **rejoue le préfixe de conversation identique** pour réutiliser le KV cache du provider (subtilité de coût majeure avec llama.cpp) ; `finishError` = fail-closed si le résumé est tronqué par max-tokens). Réutilisable quasi tel quel comme prompt du summarizer opt-in. (b) **Pruning déterministe head/middle/tail** : `packages/compaction/compaction-tool-result-pruner/src/index.ts` (`ToolResultPruner` : coupe le MILIEU des gros résultats d'outils en gardant tête+queue avec `PRUNE_MARKER`, model-free, replay-safe — complément structurel gratuit à nos couches L1/L2/L3 pour les gros outputs DevTools). (c) **Comptabilité « shadow price »** : `packages/compaction/compaction/src/types.ts` (events `compaction/*` avec `shadowedSeqs`/`shadowedTokenCount` ; la substitution se fait par `SurfaceOp replace` dans le log, **jamais par destruction** — observabilité de ce que la compaction a caché, que qm/loopx/hermes n'ont pas). (d) **Pool de pré-chauffage** : fiche **45-OpenSandbox** → `docs/guides/client-pool.md` (pool d'IDs pré-chauffés — pattern transposable à un pool de processus llama.cpp pour supprimer le coût de spawn, complément P9 hors compaction). — *Références : fiche **46-deepseek-harness** → `references/deepseek-harness/packages/compaction/compaction-basic/src/summarizer.ts` + `compaction-tool-result-pruner/src/index.ts` + `compaction/src/types.ts` ; fiche **45-OpenSandbox** → `references/OpenSandbox/docs/guides/client-pool.md`.*
- [ ] *(update références 2026-08-14)* **Compaction v2 — alignée petits modèles + anti-boucle de compaction (F-101)** : notre `compaction.py` 3 couches reste déterministe (sa force) ; 5 évolutions amont enrichissent le volet sémantique ET les garde-fous. (1) opencode a réécrit sa compaction POUR PETITS MODÈLES : prompt d'identité simple (« You are a context summarization agent » — l'« anchored summary » perdait les petits modèles), règles « anything you do not carry into the new summary is lost » / « conversation wins on conflict », et frontière d'entrée ENTIÈRE (ne plus couper une entrée en deux au split head/recent). (2) learn-claude-code s08 réordonné pour matcher Claude Code réel : `tool_result_budget` batch 200k (gros outputs persistés sur disque + bloc `<persisted-output>` avec preview) → `snip_compact` avec ARCHIVE DISQUE `.transcripts/*.jsonl` + marqueur « [N messages archived] » → `micro_compact` réutilisant les chemins persistés → compact LLM ; `reactive_compact` avec retry unique. (3) pi harness.md §3.9 : `overflowRecoveryUsed` — une seule reprise d'overflow par input utilisateur, sinon `failure_drain` (l'invariant anti-boucle de compaction qui nous manque). (4) claude-science (Anthropic) : le fold nomme ses valeurs porteuses ÉCRITES COMME DES SEARCH QUERIES + « never reconstruct a value from memory » + scratchpad `frame` survivant à la compaction. (5) hermes `conversation_compression` : budget de compression remboursé après PROGRÈS RÉEL puis re-armé sur usage vérifié. — *Références : fiches **11-opencode** → `references/opencode/packages/core/src/session/compaction.ts` + `packages/opencode/src/agent/prompt/compaction.txt` ; **16-learn-claude-code** → `s08_context_compact/code.py` ; **24-pi** → `packages/agent/docs/harness.md` (§3.9) ; **29-system-prompts-leaks** → `Anthropic/claude-science.md` ; **25-hermes-agent** → `agent/conversation_compression.py`.*

## 🔵 Priorité 10 : Contexte à la Demande (Prompt-Vault / Skills)
*Le LLM n'a pas besoin de connaître toutes les documentations d'outils dès la première seconde.*

> **🔎 État Actuel :** Le prompt système initial peut devenir très lourd si on y intègre toutes les descriptions détaillées d'outils et de skills.
> **🚀 Pourquoi ce sera mieux :** Réduit drastiquement l'usage des tokens et améliore le respect des consignes grâce à un chargement "Lazy".

- [x] **Skill Activation Middleware (Inspiré de DeerFlow + learn-claude-code + awesome-claude-skills)** : Ne placer que les titres et résumés des outils dans le prompt initial. Lorsqu'un outil ou skill est appelé par l'agent, le middleware s'active, lit le contenu de `SKILL.md` (ou du Prompt-Vault) et l'injecte localement dans le contexte d'exécution de cet appel. — *FAIT : Cette doctrine a été implémentée formellement via la Phase 1 (lazy-loading du Coder) juste en dessous.* — *Blueprint quasi direct (Python natif) : fiche **16-learn-claude-code** → `s07_skill_loading/code.py` (`_scan_skills` parse le YAML frontmatter de `skills/*/SKILL.md` au démarrage, injecte nom+description ~100 tokens/skill dans SYSTEM, `load_skill(name)` charge le contenu complet via `tool_result` à la demande). Presque copier-coller dans le `skills_loader.py` existant du projet cible.* — ***Caution externe du modèle 3-niveaux** (« Progressive Disclosure ») : fiche **18-awesome-claude-skills** → `skill-creator/SKILL.md`. Doctrine officielle Claude : (1) Metadata ~100 mots toujours en contexte, (2) corps SKILL.md <5k mots au déclenchement, (3) resources bundled illimitées car les scripts s'exécutent sans être lus. C'est exactement le modèle lazy loading visé ici.* — ***Théorie de l'authoring (fusion doctrine, audit fiches 18+21+23, 2026-08-03)** : 3 dépôts de skills (awesome-claude-skills + davidondrej + mattpocock) fusionnent en une **doctrine enrichie** plutôt qu'en 3 fiches séparées. **mattpocock = socle conceptuel dominant** (fiche **23-mattpocock-skills** → `skills/productivity/writing-great-skills/SKILL.md` + `GLOSSARY.md` : racine = **Predictability** ; deux charges [**context load** pour model-invoked, **cognitive load** pour user-invoked] ; hiérarchie 3 rungs [in-skill step > in-skill reference > external reference derrière context pointer] ; **router skill** quand les user-invoked se multiplient ; 5 failure modes [premature completion, duplication, sediment, sprawl, no-op, negation « prompt the positive »] ; **leading word** = concept compact du prétraining [_tight_, _red_, _tracer bullet_]). **davidondrej = complément pratique** (fiche **21-davidondrej-skills** → `skill-authoring/effective-agent-skills/SKILL.md` : Pattern A capability primitives vs Pattern B process primitives). Concepts agnostiques du runtime, applicables à nos skills smolagents. À combiner avec awesome-claude-skills (modèle 3-niveaux) + learn-claude-code s07 (blueprint Python).*

- [x] *(Phase 1, F-57, 2026-08-04)* **Coder lazy loading (2 niveaux)** : Le Coder (Qwen3.5-9B) lisait le corps complet de 3-5 skills (~15k chars) en EAGER à CHACUN de ses 12-25 steps → ~60-120k tokens/run répétés sur du contenu statique (context overflow 32891 > 32768 observé). IMPLÉMENTÉ : niveau 1 (cheap, system) = `build_skills_catalog` retourne metadata (name+description ~100 tokens/skill) de TOUS les skills applicables ; niveau 2 (on-demand) = `@tool load_skill` (nouveau module `skill_loader_tool.py`) charge le corps complet à la demande. Découplage EAGER vs LAZY par criticité métier : EAGER (corps complet toujours en system) = file-creation, coding, context7-research (failure modes fatals si oubliés). LAZY (metadata + tool) = frontend-design, devtools-preview, python-health-audit (utiles mais conditionnels). Gain mesuré : system prompt **-36.8%/step** (17386→10988 chars), ~6.4k chars × 12 steps ≈ 24k tokens/run économisés. Opt-out `SKILL_LAZY_LOADING_ENABLED=false` (fallback eager complet). 30 tests PASS. — *Référence blueprint : fiche **16-learn-claude-code** → `s07_skill_loading/code.py`.* — **PHASE 2 (CYCLE SUIVANT, CONDITIONNEL)** : généralisation aux nœuds DSPy (Router/Architect/Judge/Security) via input field `methodology_context` UNIQUEMENT SI la Phase 1 est performante en run de validation (baseline = run 2026-08-04_1816_bubble_sort, index.html 255 lignes one-shot).
- [ ] **Pivot v3 Coder sélection Architect (P10 Phase 1)** : PIVOT JUSTIFIÉ suite à l'échec de la Phase 1 ci-dessus en validation (le Coder 9B n'appelait pas `load_skill`, perdant les skills conditionnels). Nouveau design : l'Architect SÉLECTIONNE les skills pertinents par sous-tâche (`subtask.skills` dans ArchitectOutput), et le Coder reçoit leur corps complet directement, plafonnés par un budget tokens (`skill_budget_tokens`, ~24% du contexte). Injection directe fiable à 100%. (F-80)
- [x] **Prompt Offloading — outils Python dédiés encapsulant snippets JS/algorithmes** (F-72) : Plutôt que d'injecter la complexité (snippets JS, algorithmes) en texte brut dans les prompts système des nœuds, l'encapsuler dans des OUTILS Python dédiés (description 1 ligne expose l'INTENTION, le JS vit dans l'outil). Réduit la taille des prompts + la charge cognitive des petits LLMs (moins de crashes parsing). IMPLÉMENTÉ : (1) `js_utils.py` (extraction DRY de `run_node_check` partagé Static Tester + Coder) ; (2) `@tool check_js_syntax(path)` (Coder, verify-after syntaxe JS via `node --check`) ; (3) module `devtools_dom_tools.py` (3 sous-classes Tool `clean_dom`/`add_visual_tags`/`fuzz_click_all_buttons` wrappant `evaluate_script` DevTools — **répare le « modèle » cassé** : les anciens `puppeteer_clean_dom`/`add_visual_tags` wrappaient `puppeteer_evaluate` = navigateur Puppeteer qui ne charge pas les `file://` locaux = morts ; le prompt disait de NE PAS les utiliser) ; (4) centralisation doc DevTools (`DEVTOOLS_BASE_DOC` partagée Coder/Tester, signatures communes + anti-IIFE CRITIQUE). Snippets JS corps préservés EXACTS, adaptation `script→function` + retrait enveloppe IIFE (DevTools exige fonction non invoquée). 19 nouveaux tests, 940 passed / 8 pré-existants, 0 régression. — *Référence (cassée, réparée) : Web Tester `puppeteer_clean_dom`/`add_visual_tags`.*

- [x] **Révélation Progressive (Progressive Disclosure) des assets de skills complexes** (F-92) : Refactoriser les gros fichiers `SKILL.md` (`animation-vocabulary`, `skill-creator`, etc.) en déplaçant les longues sections (glossaires, exemples) vers un dossier `resources/`. `SKILL.md` devient un index léger indiquant à l'agent *quand* et *comment* charger les fichiers secondaires (via l'outil `view_file` ou autre). Inspiré du modèle Anthropic pour éviter le context bloat et économiser les tokens, permettant un chargement paresseux granulaire à l'intérieur même d'une compétence.

- [x] **Adoption de l'outillage canonique des skills** : Introduire `init_skill.py` (scaffolding `SKILL.md` + `scripts/` + `references/` + `assets/`) et `quick_validate.py` (CI gate : `name` hyphen-case strict, pas de chevrons `<>` dans `description`, description = déclencheur). — *FAIT : Importés sous `scripts/new_skill.py` et `scripts/quick_validate.py` pour un usage direct.*
- [x] **Structure `scripts/` + `references/` + `assets/`** : Faire évoluer nos skills (actuellement **mono-fichiers** : tout inline dans `SKILL.md`, chargées eager via `build_skills_block`) vers le format canonique découplé. Les instructions restent dans `SKILL.md`, le code déterministe part dans `scripts/` (exécutable sans LLM), la doc lourde dans `references/`. — *FAIT : Preuve de concept réalisée sur la skill `python-health-audit` où le code Python inline a été extrait dans `skills/python-health-audit/scripts/run_audit.py`.*
- [x] *(audit fiches 18+23, 2026-08-04)* **Manuel MCP & design d'outils** : Adopter les principes de design d'outils MCP de `mcp-builder` (`Build for Workflows`, `Optimize for Limited Context`, `Actionable Error Messages`) pour calibrer nos outils smolagents + MCP (Puppeteer, Chrome DevTools, Context7). Complément : pattern d'**évaluation** (« 10 QA XML + vérification ») réutilisable comme cas de vérification structurés pour le nœud Judge (P6). — *FAIT : Documenté formellement dans `docs/adr/0001-mcp-tool-design.md`.*
- [x] *(audit fiche 23-mattpocock, 2026-08-04)* **Doctrine hard-dependency vs soft-dependency** : Formaliser quand exiger une dépendance dure (fail-loud) vs accepter une dégradation gracieuse (degrade-gracefully). Notre Context7/devtools font déjà du degrade-gracefully par défaut ; ce pattern cadre les exceptions. — *FAIT : Documenté formellement dans `docs/adr/0002-dependencies-graceful-degradation.md`.*
- [x] *(audit fiche 25-hermes-agent, 2026-08-05)* **Provenance, Guard et AST audit des skills** : Au-delà du format SKILL.md (fiche 18) et de la doctrine d'authoring (fusion 18+21+23), hermes-agent apporte 3 briques outillage pour la **fiabilité** du chargement de skills. — *(1) **Provenance** : fiche **25-hermes-agent** → `references/hermes-agent/tools/skill_provenance.py` (`set_current_write_origin`/`reset_current_write_origin` via contextvars, `get_current_write_origin`, `is_background_review`). Trace QUI a créé/modifié chaque skill/memory (user/agent/hub/background_review) — pattern Python pur transposable direct pour auditer nos skills générés.* (2) **Sync/diff** : `references/hermes-agent/tools/skills_sync.py` (`sync_skills`, `_content_hash`, `_dir_hash`, `diff_bundled_skill` : détecte les modifications utilisateur, permet reset/diff/restore des skills officiels vs modifiés).* (3) **Guard sécurité** : `references/hermes-agent/tools/skills_guard.py` (`ScanResult` verdict 3 niveaux safe/caution/dangerous + trust_level community/trusted/agent_created + `scan_skill`) **+ AST audit** `references/hermes-agent/tools/skills_ast_audit.py` (`ast_scan_path` détecte imports dynamiques `importlib.import_module`/`__import__`/`getattr` computed). Complément structurel au guard regex : la combinaison regex + AST couvre les skills malveillantes qu'un simple `init_skill.py`/`quick_validate.py` (fiche 18) ne attrape pas. À intégrer comme gate CI durcie au-delà de notre `quick_validate.py` actuel.*
- [ ] **Approbation Asynchrone (Human-in-the-loop via simulation)** *(audit fiche 26-cloudflare-os, 2026-08-06)* : Pour la confiance et l'UX, les outils dangereux (non `read-only`) ne bloquent pas l'agent de façon synchrone. Le système simule une réussite (ou met en file d'attente) pour l'agent, pendant qu'un humain (ou un Gatekeeper) valide l'action asynchronement en arrière-plan. — *Référence : fiche **26-cloudflare-os** → `packages/mcp-shared/src/tools.ts` (`readOnlyHint`). Pattern puissant pour ne pas briser la boucle de l'agent lors d'une attente d'approbation humaine.*
- [ ] **Extraction de Skills avec Isolation de Rôle (Anti-Hallucination)** *(audit fiche 28-TencentDB-Agent-Memory, 2026-08-07)* : Lors de la génération et l'extraction de skills à partir d'historiques, isoler strictement les instructions système des conversations passées à l'aide de balises non-standards (ex: `<<past-user>>`) pour empêcher l'agent de confondre le contexte historique avec ses propres directives. — *Référence : fiche **28-TencentDB-Agent-Memory** → `MemoryCore/src/core/skill/skill-extractor.ts`.*
- [ ] **Skill Review Gate (classification 5-critères + acceptation 4-dim/100pts)** *(audit fiche **28-TencentDB-Agent-Memory**, 2026-08-07)* : Va **au-delà** de l'isolation `<<past-user>>` ci-dessus. Quand on générera/validera des skills (F-65 ingestion pépites, F-80 sélection Architect), objectiver l'acceptation via une gate formelle en 2 étages : **(1) classification 5-critères** — l'artefact est-il une Skill, une Memory, une page Wiki, un nœud CodeGraph, ou du contexte temporaire ? (évite de stocker comme skill ce qui devrait être un fait mémoire) ; **(2) gate d'acceptation 4-dimensions / 100 points** avec seuils **≥72 total ET ≥12 par dimension** (empêche une dimension faible compensée par les autres). Le prompt impose aussi un template SKILL.md canonique (When to use / When not / Required inputs / Workflow / Decision rules / Output format / Validation / Pitfalls). — *Référence : fiche **28-TencentDB-Agent-Memory** → `MemoryCore/src/core/skill/prompts/skill-review-prompt.ts` (`SKILL_REVIEW_PROMPT`, gate 5-critères, gate 4-dim/100pts, role-isolation `<<past-*>>`, contrat `Nothing to save.` si rien à faire).*

- [ ] *(audit fiches 30 + 32 + 33 + 34 + 37 + 38 + 42 + 43, 2026-08-12)* **Doctrine & tooling des skills** (8 références convergentes, complémentaires au lazy-loading F-57/F-80 et à la Progressive Disclosure F-92) :
  - **Harnais d'évaluation des skills** (fiche **38-skills**) : `skill-creator/scripts/run_loop.py` (`split_eval_set`, split train/test pour skills agentiques) + `mcp-builder/scripts/evaluation.py` (harnais de robustesse des serveurs MCP). Objectiver l'itération des skills au lieu de les juger au petit bonheur. (F-96)
  - **Navigation sémantique** (fiche **30-Ix**) : `skills/ix/SKILL.md` (template de skill de navigation dans un graphe de contexte persistant).
  - **Doctrine de skill + Skill Factory** (fiche **32-OpenKB**) : `skills/openkb/SKILL.md` (trust boundary explicite, « MUST NOT modify the KB ») + `openkb/skill/creator.py` (`build_skill_create_agent`, `run_skill_create` — nœud de génération dynamique de skills).
  - **Skills portables** (fiche **34-brooklyn-skills**) : `SKILL.md` autonome par dossier + patterns (`cpr` cycle clean+PR, `ui-only`).
  - **Architecture modulaire déclarative** (fiche **37-obsidian-skills**) : séparation stricte `SKILL.md` (prompt de comportement) / `references/` (doc injectable) — valide rétroactivement notre Progressive Disclosure F-92.
  - **Skill déclaratif canonique** (fiche **42-anydoc**) : `skills/convert-documents-to-markdown/SKILL.md` (modèle propre de définition déclarative).
  - **Skills à graphes d'état Mermaid** (fiche **43-framework**) : modélisation `Actions`/`Assets`/`References` + machine à états Mermaid dans le `SKILL.md` (blueprint pour nos skills de workflow).
  - **Singleton navigateur** (fiche **33-room**) : `src/shared/web-tools.ts` (`_browser`, instance Playwright active en singleton — évite 2s de boot à chaque appel navigateur).
  — *Références : fiches **30-Ix**, **32-OpenKB**, **33-room**, **34-brooklyn-skills**, **37-obsidian-skills**, **38-skills**, **42-anydoc**, **43-framework**.*

- [ ] *(audit fiches 45 + 46, 2026-08-14)* **Registre de skills en Python natif + résolution par couches** : compléments au lazy-loading F-57/F-80 et à la Progressive Disclosure F-92. (a) **Registre Python pur** : fiche **45-OpenSandbox** → `cli/src/opensandbox_cli/skill_registry.py` (`SkillSpec(slug, title, summary, trigger_hint)` chargé eager + `split_frontmatter()` + `extract_section(body, heading)` = sections lazy extractibles à la demande + `BUILTIN_SKILLS` ; leur `skills/troubleshoot-sandbox/SKILL.md` montre le format attendu avec frontmatter `user-invocable`/`argument-hint`) — pattern directement transposable à notre `skill_loader_tool.py`. (b) **Registry scopé avec rank et invalidation par révision** : fiche **46-deepseek-harness** → `packages/skill/skill/src/index.ts` (`SkillProvider {list, get}` : catalog/body séparés — `list()` ne renvoie que `SkillSummary` name/description/`whenToUse` ; `SkillCandidate {rank}` ; `SkillLayer` + `ScopedLayers` = couches globale + par scope avec shadowing « le plus proche gagne » ; cache de collect invalidé par `revision` avec re-tentative `MAX_COLLECT_ATTEMPTS=2` ; `SkillInvocationPolicy {modelInvocable, userInvocable}` ; wrapper canonique `renderSkillContent` `<skill_content>`). Portage algorithmique TS→Python — le rank et l'invalidation par révision sont les 2 mécanismes que notre implémentation n'a pas. — *Références : fiche **45-OpenSandbox** → `references/OpenSandbox/cli/src/opensandbox_cli/skill_registry.py` + `skills/troubleshoot-sandbox/SKILL.md` ; fiche **46-deepseek-harness** → `references/deepseek-harness/packages/skill/skill/src/index.ts`.*
- [ ] *(update références 2026-08-14)* **Skills : routeur compact + références par workflow + skills écrites par l'agent** : (a) davidondrej a restructuré sa skill `deepapi` d'un monolithe de 1432 lignes en **routeur de 81 lignes** + `references/` organisées par workflow (avec `fingerprint` frontmatter et auto-update par version) — le patron exact de la Progressive Disclosure F-92 pour nos SKILL.md volumineuses ; (b) open-swe a ajouté `DynamicToolMiddleware` (outils d'intégration chargés uniquement après un appel explicite `load_integration_tools`) + `save_user_skill`/`delete_user_skill` (l'agent ÉCRIT lui-même ses skills persistantes namespacées) — à considérer pour F-82 (Skill Finder). Mention : le nouveau prompt claude-science documente la doctrine « a loaded skill is reference, not a recipe » (décider SI on applique selon la forme de la tâche). — *Références : fiches **21-davidondrej-skills** → `references/davidondrej-skills/skills/research-and-web/deepapi/SKILL.md` ; **09-open-swe** → `references/open-swe/agent/middleware/dynamic_tools.py` + `agent/tools/user_skills.py` ; **29-system-prompts-leaks** → `Anthropic/claude-science.md`.*

## 🟠 Priorité 11 : Observabilité et Protocole d'Événements
*Un système opaque est impossible à déboguer.*

> **🔎 État Actuel :** Le logging se fait de manière classique vers `stdout` ou des fichiers, ce qui est difficile à exploiter par d'autres systèmes.
> **🚀 Pourquoi ce sera mieux :** Un flux d'événements strict permet l'observabilité et l'intégration de TUIs.

- [ ] **Run Event Stream (Inspiré de Deer Flow + learn-claude-code)** : Définir un contrat JSON strict (versionné) pour les événements du graphe (trace, message, tool_call, error, subagent_status). Cela permettra de brancher facilement une UI de monitoring, un TUI (terminal UI), ou de faire de l'analyse post-mortem avancée sans parser des logs texte. — *Spec concrète à ne pas réinventer : fiche **08-deer-flow** → `references/deer-flow/contracts/run_event_stream_contract.json` (17 Ko, contrat versionné avec règles frozen/additive/breaking). C'est exactement le contrat d'événements cherché. (OpenFox écarté : référence 🔴 non production-éprouvée.)* — *Couche d'observabilité native (Python) : fiche **16-learn-claude-code** → `s04_hooks/code.py`. Le système d'hooks (`trigger_hooks` sur `PreToolUse`/`PostToolUse`/`Stop`) EST l'event stream : un callback d'observation branché sur chaque event permet d'émettre les événements du contrat deer-flow sans parser des logs texte. Les deux références sont complémentaires (deer-flow = le contrat, learn-claude-code = le hookpoint d'émission).* — ***Event sourcing idempotent + ledger classifié (audit fiche 19-loopx, 2026-08-03)** : loopx apporte deux briques qui manquent aux deux références ci-dessus. (a) **Idempotence de l'event stream** : fiche **19-loopx** → `loopx/event_sourced_state.py` (`AppendOnlyStateEventStore` : idempotence par `event_id`+`event_fingerprint`, détection de conflit `StateEventConflictError`, rebuild d'état par projection `build_state_projection`, checksum SHA256 du flux `event_stream_checksum`). Complète notre F-43 (idempotence des effets de bord) côté event stream, et rend la **reprise après crash** robuste. Persistance JSONL+filelock dans loopx → **à reloger sur DuckDB** (le projet cible l'utilise déjà). (b) **Ledger classifié** : fiche **19-loopx** → `loopx/control_plane/runtime/event_ledger.py` (`EVENT_LEDGER_CLASSES` = 5 classes `accounting`/`decision`/`evidence`/`state`/`work`, agrégation sur fenêtres 24h/7d via `build_event_ledger_summary`). Donne une **classification** des événements pour l'analyse post-mortem que ni deer-flow (contrat) ni learn-claude-code (hookpoint) ne fournissent. Stdlib pure, portable direct. Les 3 références sont complémentaires : deer-flow = contrat, learn-claude-code = hookpoint, loopx = idempotence + classification.*
- [ ] **Multi-BDD DuckDB et Log Rotation (Historisation)** : ~~Remplacer l'accumulation infinie dans le journal plat~~ **FAIT (F-106, 2026-08-14)** — fichier journal supprimé, historique complet (199 événements datés, 2026-07-29 → 2026-08-14) récupéré depuis git et ré-importé dans `data/event_stream.duckdb` (`run_id='legacy_md'`, corrige la perte de dates de la migration F-75), CLI assistant `scripts/log_event.py` ouvert. RESTE : l'instrumentation runtime automatique (workflows → event stream / `runs_history.duckdb` pour stats de run et tokens) + routine de rétention.
- [ ] *(note utilisateur, 2026-08-14)* **Migration éventuelle des autres fichiers de suivi plats vers DuckDB (F-107)** : `progress.md` (~1 500 lignes, croissance append-only par cycle) est le candidat n°1 si le gonflement devient gênant — archiver les sections d'itérations passées en base, ne garder que le sprint courant en texte. `feature_list.json` et `contract.md` restent préférables en texte formaté (diff git lisible en review Kilo). Déclencheur : taille/bruit excessif constaté. Pattern prêt : `scripts/recover_log_history.py` + table `run_event` (F-106). — ***Schéma SQLite FTS5 event-sourcing (audit fiche 25-hermes-agent, 2026-08-05)** : fiche **25-hermes-agent** → `references/hermes-agent/hermes_state_common.py` (`SCHEMA_SQL` : tables `sessions` (avec `parent_session_id` → lineage de compaction), `messages` (role/content/tool_calls/tool_name/token_count/active/compacted), `compression_locks` (verrou cross-process holder/expires_at), `async_delegations` (event-sourcing des subagents), `session_model_usage` ; `FTS_SQL`/`FTS_TRIGRAM_SQL` + `_FTS_TRIGGERS` FTS5 sur `messages.content` via triggers AFTER INSERT/UPDATE/DELETE + FTS5 trigram pour CJK/fuzzy + sanitizer anti-injection `MAX_FTS5_QUERY_CHARS` ; `SCHEMA_VERSION = 25`). Le pattern event-sourcing + lineage parent/enfant + locks cross-process est directement transposable conceptuellement sur DuckDB (⚠️ DuckDB n'a pas FTS5 natif — soit garder un SQLite séparé pour le search via `hermes_state_search.py` `search_messages`/`_sanitize_fts5_query`/`_run_trigram_search`, soit utiliser l'extension FTS DuckDB). Schéma SQL réutilisable tel quel ; `apply_wal_with_fallback`/`repair_state_db_schema`/`quarantine_zeroed_state_db` (`hermes_state.py`) apportent en bonus la robustesse DB (WAL fallback, repair, quarantine). Complément opérationnel aux 3 références existantes : deer-flow = contrat, learn-claude-code = hookpoint, loopx = idempotence + classification, hermes-agent = schéma SQL éprouvé + lineage compaction + search FTS5.*
- [ ] **Historique et formatage des messages** : Implémenter un gestionnaire de messages (MessageManagerState) pour normaliser et historiser le stream LLM. — *Référence : fiche **27-browser-use** → `references/browser-use/browser_use/agent/message_manager/service.py`.*
- [ ] **Validation de l'Event Sourcing (Reducer JSONL)** : Ajouter un validateur structurel pour l'Event Stream (Single-Writer Record Protocol) pour détecter les corruptions logiques dans le journal d'événements (ex: `multiple_open_operations`, `record_after_finish`). Permet de rejeter un état corrompu à la restauration plutôt que de le réparer à l'aveugle. (F-78) — *Référence production : fiche **pi** → `packages/agent/src/harness/reducer.ts`.*
- [ ] **Store Multi-Versionné pour Skills/Mémoires (is_head)** *(audit fiche 28-TencentDB-Agent-Memory, 2026-08-07)* : Étendre la base DuckDB pour gérer le multi-versioning des skills et mémoires consolidées sans écrasement, en utilisant un flag `is_head=1` pour pointer vers la version active. Permet l'auditabilité et le rollback des skills générées. — *Référence : fiche **28-TencentDB-Agent-Memory** → `MemoryCore/src/core/skill/skill-store.ts`.*


- [ ] *(audit fiches 33 + 40, 2026-08-12)* **Observabilité** : (a) fiche **33-room** → `src/server/event-bus.ts` (`EventBus`, bus mémoire Map/Set minimaliste pour le fan-out d'événements — blueprint d'implémentation de l'event stream plus simple que les middlewares) ; (b) fiche **40-waku-agent** → `waku/ops/tracing.py` (`Tracer`, pattern d'observateur pour event stream en JSONL/OpenTelemetry). Compléments au contrat deer-flow + hookpoint learn-claude-code + idempotence/ledger loopx déjà cités (P11 cases existantes). — *Références : fiches **33-room**, **40-waku-agent**.*

- [ ] *(audit fiches 45 + 46, 2026-08-14)* **Event log typé extensible + corrélation par ContextVar** : (a) **Le modèle de log de session le plus abouti du dossier** : fiche **46-deepseek-harness** → `packages/core/session/src/types.ts` (`SessionEventMap` extensible par declaration merging (les plugins ajoutent leurs types sans toucher au core — exactement ce que notre event stream P11 devra faire) ; `SessionEvent {type, seq, time, data, ignorable?}` où le flag `ignorable` force un lecteur qui ne connaît pas un type à **refuser** plutôt que corrompre silencieusement (fail-closed) ; `sourceEventSeqs` = **provenance** : chaque message assemblé cite les chunks qu'il consomme ; `SurfaceOp = 'append' | {op:'replace', start, end}` = le log n'est JAMAIS détruit, seulement shadowé ; convention « **Model-visible ⟺ logged** » avec invariant vérifié à l'exécution ; tests par rejeu de transcript sur snapshots JSONL). Complète la chaîne existante : deer-flow = contrat, learn-claude-code = hookpoint, loopx = idempotence+classification, hermes = schéma SQL, **deepseek-harness = typage extensible + provenance + fail-closed**. Deux backends interchangeables du même log (`session-persistence-jsonl`/`-sqlite`) valident l'architecture backend-agnostique (DuckDB = 3e backend). (b) **Corrélation par ContextVar** : fiche **45-OpenSandbox** → `server/opensandbox_server/middleware/request_id.py` (`request_id_ctx: ContextVar` + `RequestIdMiddleware` (reset du token en `finally`) + `RequestIdFilter` qui injecte `request_id` dans CHAQUE LogRecord sans plumbing) — ~30 lignes Python pour corréler tous nos logs par run/nœud, prérequis cheap de l'event stream ; + spec `/metrics/events` (télémétrie SDK fire-and-forget « MUST NOT affect usability » : la télémétrie ne doit jamais casser l'usabilité) et `integrations/otel/metrics.py` (`setup_otel_metrics`). — *Références : fiche **46-deepseek-harness** → `references/deepseek-harness/packages/core/session/src/types.ts` + `packages/session/session-persistence-jsonl/` ; fiche **45-OpenSandbox** → `references/OpenSandbox/server/opensandbox_server/middleware/request_id.py` + `integrations/otel/metrics.py`.*
- [ ] *(update références 2026-08-14)* **Prompt envelopes hash-dedup + event bus bufferisé** : (a) qm a remplacé le stockage du body de requête LLM par step (coût O(turns²) en BDD) par des « prompt envelopes » : system/tools/config stockés UNE fois, dédupliqués par hash sha256 (table `llm_prompt_envelopes`), fenêtre de messages reconstruite depuis la tape à la lecture — debug inchangé, stockage constant ; pattern DuckDB direct pour notre historisation P11. (b) pi a extrait un `HarnessEventBus` minimal (`on()` par type + `watch()` qui BUFFERISE les events jusqu'à `start()` — snapshot+replay, aucun event émis avant le démarrage du consommateur n'est perdu). (c) Claude Code replie les ticks `noop` de ScheduleWakeup en streak (« long quiet holds stay legible ») — pattern pour nos logs de poll DevTools. — *Références : fiches **14-qm** → `references/qm/src/sessions/session-store.ts` (`promptEnvelopeBody`) ; **24-pi** → `references/pi/packages/agent/src/harness/events.ts` (`HarnessEventBus`) ; **29-system-prompts-leaks** → `Anthropic/Claude Code/claude-code-fable-5.md`.*

## ✨ Priorité 12 : Meta-prompt (PromptRefiner avant l'Architect)
*L'Architect planifie mieux sur une spec bien structurée que sur un prompt utilisateur vague.*

> **🔎 État Actuel :** Le prompt utilisateur passe tel quel du fichier de tâches au Router puis à l'Architect. Aucune étape de clarification/structuration.
> **🚀 Pourquoi ce sera mieux :** Un nœud LLM qui reformule/enrichit le prompt avant l'Architect améliore la qualité du plan sur tous les runs (pattern "Enhance Prompt" éprouvé : Kilo Code, Cline, Roo Code).

- [x] **Phase 1 — Nœud PromptRefiner (F-39)** : nœud DSPy `execute_prompt_refiner_node` (gemma REASONING, `ChainOfThought`) inséré entre `task_content` et le Router dans `run_coding_workflow`. Reformule le prompt brut en spec structurée (sections Objectif/Fonctionnalités/Contraintes/Critères Given-When-Then) en connaissant le catalogue des capacités (skills + statut Context7 + testers via `_build_capabilities_summary`). Détection termes vagues (`ambiguities_detected`). Anti-invention. Branché APRÈS le `run_id` (hash sur prompt brut → reprise stable). Persistance checkpoint (`refined_prompt` skip LLM à la reprise). Opt-out `PROMPT_REFINER_ENABLED`. Dégradation gracieuse (repli prompt brut si LLM down). Context7 = citer seulement (Architect fait déjà le pré-fetch, pas de duplication). — *Inspiré de : Kilo Code/Cline/Roo Code "Enhance Prompt" + open-swe (template sortie) + claude-code requirements-analyst (liste noire termes vagues). 8 tests PASS.*
- [ ] **Phase 2 — Optimisation MIPROv2 (CHANETIER FUTUR, non démarré)** : optimiser le prompt du PromptRefiner via DSPy MIPROv2 sur métrique **end-to-end** (juger le plan produit par l'Architect, jamais le prompt réécrit isolément — doc `dspy_prompt_inconnu.md`). **Écarté ce cycle** car : (1) signal biaisé en mono-modèle GPU 6 Go (juge = architecte = même gemma qui se juge lui-même) ; (2) risque d'overfit sur 8 prompts hétérogènes du Prompt-Vault ; (3) coût GPU élevé (~75 appels) pour gain incertain. **À réévaluer** si besoin constaté en prod (specs systématiquement mauvaises sur un type de prompt récurrent) + avec un vrai dataset accumulé. La Phase 1 est la base nécessaire à toute optimisation future.

## 📁 Priorité 13 : Output daté par run (isolation des artefacts)
*Chaque run doit écrire dans son propre répertoire daté, pas à la racine du projet.*

> **🔎 État Actuel :** Le Coder écrit dans `os.getcwd()` (= racine du projet). Les outils `write_file`/`edit_file`/`search_replace`/`append_file` (`tools.py`) résolvent les chemins via `os.path.abspath(path)` relatif au cwd. Le `web_tester.py:76` construit `workspace_url = file:/// + os.path.abspath(os.getcwd())`. Le `python_tester.py:136,180` lance pytest en `cwd=os.getcwd()`. Conséquence concrète constatée lors du ménage du workspace : `bubble_sort/index.html` et `landing_page/` finissent à la racine, écrasent les runs précédents, polluent le repo et rendent la traçabilité impossible.
> **🚀 Pourquoi ce sera mieux :** Isoler chaque run dans `runs/YYYY-MM-DD_HHMM_slug/` permet de comparer les runs, de garder un historique daté pour l'analyse post-mortem, et de garder la racine du projet propre.

- [x] **Isolation datée des sorties** : faire écrire le Coder/Tester dans un répertoire dédié par run (ex: `runs/{date}_{slug}/`). **Option A (recommandée) — `chdir` global** : changer le cwd vers le output_dir daté avant l'exécution des nœuds Coder/Tester dans `run_coding_workflow`. Le + propre, les chemins relatifs des `target_files` continuent de marcher naturellement. **Point d'attention critique** : `kg_path` doit être résolu en **absolu avant le chdir** (sinon la DB DuckDB change de place à chaque run → perte de la mémoire entre runs / reprise cassée). **Option B — préfixer les target_files** : muter `ArchitectTask.target_files` (`landing_page/index.html` → `runs/.../landing_page/index.html`). Plus invasif, risque de casser le dispatch techno du Tester (détection par extension). À évaluer. — *FAIT (F-40) : Option A implémentée. `_resolve_run_output_dir` (reprise via checkpoint `output_dir` sinon nouveau daté `runs/YYYY-MM-DD_HHMM_slug/`) + `_scoped_chdir` (context manager, restoration cwd garantie via `finally`). Le KG est instancié AVANT le chdir → DuckDB reste à `kg_path` stable (testé `test_e2e_kg_path_stable_after_chdir`). 13 tests PASS.*
- [x] *(complément)* **Slug du nom de run** : dériver un slug lisible depuis le contenu de la tâche (ex: `bubble_sort_visualizer`) plutôt que seulement le `run_id` (hash). Éviter les caractères interdits sur Windows (`:`, `?`, `*`). — *FAIT : `_slugify` (lowercase, `[^a-z0-9]→_`, collapse `__+`, truncate 24, fallback 'run'). Source = `seed_tasks[0]['id']`.*
- [x] *(complément)* **Nettoyage optionnel** : config `OUTPUT_RETENTION` (nb de runs à garder, ex: 10) pour éviter que le dossier `runs/` ne grossisse indéfiniment. À ajouter dans `.gitignore` (`runs/` déjà implicitement non versionné mais à expliciter). — *FAIT : `runs/` ajouté au `.gitignore`. Config `OUTPUT_RETENTION` (défaut 10) ajoutée dans `.env.example` et `config.py`. Fonction `_prune_old_runs` appelée à la création du run directory pour nettoyer les plus anciens.*
- [x] *(référence)* **Point d'insertion** : `run_coding_workflow` (`workflows.py`), après calcul du `run_id` et AVANT la première écriture Coder. Le `run_id` (hash du contenu de tâche, PAS du cwd) reste stable — il ne faut PAS le dériver du cwd (sinon un déplacement du projet casserait la reprise). — *FAIT : chdir après le chargement du checkpoint (pour la décision reprise/nouveau), corps du workflow wrappé dans `with _scoped_chdir(run_output_dir):`.*

## 🎯 Priorité 14 : Durcissement des prompts de nœuds (procédure concrète)
*Source : audit comparatif F-55 (`debug/isolation/COMPARISON_AUDIT.md`).*

> **🔎 État Actuel :** Les prompts des nœuds DSPy (`dspy_nodes.py` + rôles/invariants F-44 dans `prompts.py`) sont **bons sur les principes** (rubric critical/high/medium/low, in-diff only, anti-nits, mention OWASP Top 10) mais **manquent de procédure opératoire concrète**. L'audit comparatif entre les méthodes manuelles (`MANUAL_<NODE>_METHODOLOGY.md`, l'agent qui joue le nœud à la main) et les nœuds de production a révélé ~15 gaps : les prompts prod laissent le LLM **libre de sa séquence** et parlent en **abstrait** ("sois exhaustif") là où ma méthode impose un **ordre fail-fast** et des **patterns/grep concrets**. Hypothèse : ces gaps sont la cause probable des failure modes observés en prod (Router qui déborde vers javascript par défaut, Security qui rate pickle/secrets, Judge qui valide sans vérifier la couverture).
> **🚀 Pourquoi ce sera mieux :** Injecter la procédure concrète (ordre des étapes, patterns explicites, biais nommés) directement dans les prompts prod aligne le comportement du LLM sur la méthode manuelle de référence — qui a prouvé sa valeur (démo Judge : localisation d'un opérateur fautif ligne 11 qu'un rapport Tester imprécis n'avait pas signalé). Effort faible (ajouts ciblés de quelques lignes par prompt), ROI élevé (résout des failure modes récurrents).

### P14-A : Router — mots-clés canoniques + règle extensions + justification
- [x] **F-56a** : Injecter dans `RouterSignature` (`dspy_nodes.py:40-50`) un mini-tableau de tokens canoniques par langage (python/.py/pandas → python ; react/.tsx/typescript → typescript ; .html/landing page → html ; vanilla js/canvas → javascript ; rust/cargo → rust ; go/golang → go). ~6 lignes. — *FAIT : tableau MOTS-CLÉS CANONIQUES injecté.*
- [x] **F-56a** : Règle de priorité explicite : "si le prompt mentionne des extensions de fichiers (.py/.ts/.html...), elles priment sur les mots-clés." — *FAIT : RÈGLE DE PRIORITÉ injectée.*
- [x] **F-56a** : Anti-biais (3 lignes) : ne pas déborder vers javascript par défaut ; HTML/CSS pur sans JS métier = html ; React/Next.js = typescript. — *FAIT : section ANTI-BIAIS injectée.*
- [ ] **F-56a** *(optionnel, REPOUSSÉ)* : Ajouter `justification: str = ""` à `RouterOutput` (`models.py:78-80`) pour rendre les verdicts auditables. — *REPOUSSÉ : changement de schéma trop invasif ce cycle. L'auditabilité se fait via les logs pour l'instant.*

### P14-B : Architect — sections incremental + biais multifile/incremental
- [x] **F-56b** *(pré-corrigé cycle F-55)* : Doc `MANUAL_ARCHITECT_METHODOLOGY.md` alignée sur F-15 prod ("1 livrable testable = 1 sous-tâche", pas "1 fichier = 1 sous-tâche").
- [x] **F-56b** : "La 1ère section DOIT être le squelette structural (ex: `<!DOCTYPE>…</body></html>` pour HTML) — socle sur lequel les autres s'appendent." — *FAIT : injecté dans RÈGLES POUR 'incremental' (sections).*
- [x] **F-56b** : "Vise 3-7 sections par fichier incremental (~50-100 lignes chacune, gérable pour un petit Coder)." — *FAIT.*
- [x] **F-56b** : Avertissement biais explicite : "ATTENTION : 'incremental' = UN gros fichier par morceaux (HTML monolithique). Ne mets JAMAIS 'incremental' sur un projet multifichier Python/TS — utilise 'multifile'." — *FAIT : section BIAIS 'incremental' vs 'multifile'.*

### P14-C : Judge — procédure ordonnée + couverture par exigence + croisement défiant
- [x] **F-56c** : Imposer procédure ordonnée dans `CodeJudgeSignature` : "Procède dans cet ordre : (1) liste chaque exigence de `task_requirements` ; (2) vérifie présence+implémentation dans `code` ; (3) croise avec `test_results` ; (4) applique `security_vulnerabilities` ; (5) décide." — *FAIT : section PROCÉDURE OBLIGATOIRE (5 étapes numérotées).*
- [x] **F-56c** : Exiger check couverture par exigence : "Pour CHAQUE exigence de `task_requirements`, atteste Présente/Implémentée/Testée avant de conclure." — *FAIT : intégré étape 2 de la procédure.*
- [x] **F-56c** : Règle croisement défiant : "Si `test_results` dit PASS mais qu'une exigence n'est pas implémentée dans `code` → finding critical, `is_approved=False`." — *FAIT : intégré étape 3 de la procédure.*
- [x] **F-56c** : Localisation obligatoire : "Chaque finding DOIT citer ligne/fragment exact (ancre in-diff)." — *FAIT : section LOCALISATION OBLIGATOIRE.*

### P14-D : Security — grille OWASP concrète + vérif input + A09
- [x] **F-56d** : Ajouter liste de patterns dangereux concrets par catégorie OWASP dans `SecuritySignature` : `innerHTML`/`document.write` (A03 XSS), `os.system`/`subprocess shell=True`/`eval` (A03 cmd), concat SQL/f-strings, `pickle.loads`/`yaml.load` (A08), `md5`/`sha1`/`password=`/`api_key=` (A02), `verify=False`/`CORS *`/`debug=True` (A05). — *FAIT : section PATTERNS DANGEREUX À CHERCHER ACTIVEMENT.*
- [x] **F-56d** : Clause discrimination input : "Confirme la source de la donnée avant de flagger (externe = vuln, littérale = faux positif)." — *FAIT : section DISCRIMINATION INPUT.*
- [x] **F-56d** : Ajouter A09 Logging explicitement (absent du prompt prod). — *FAIT : A09 Logging ajouté à la liste des patterns.*
- [x] **F-56d** : Avertissement faux positifs (constantes, JS client où eval n'est pas une RCE serveur). — *FAIT : section ATTENTION FAUX POSITIFS.*
- [ ] **F-56d** *(optionnel, REPOUSSÉ vers F-57)* : `truncate_output` sur `code_content` (`dspy_nodes.py:516`) par parité avec Judge/Escalation. — *REPOUSSÉ : sera pertinent avec F-57 (lazy loading) qui gérera la taille du contexte.*

### P14-E : Linter — warning non bloquant sur fichier absent (DÉCISION USER)
- [x] **F-56e** : `linter.py:226-227` — **DÉCISION USER** (après exploration) : fichier absent reste `is_valid=True` (défense contre échec silencieux Coder + évite boucle mode correction), MAIS **avertissement non bloquant** remonté dans `details` pour l'observabilité. `lint_file` ajoute une erreur d'avertissement, `execute_linter_node` l'affiche dans une section "AVERTISSEMENTS (non bloquants)". — *FAIT : défense conservée + observabilité ajoutée. 2 tests (test_missing_file_is_valid mis à jour + test_missing_file_warning_in_details nouveau).*
- [ ] **F-56e** *(complément)* : Documenter le fail-open actuel (fichier illisible/encodage cassé → `is_valid=True` silencieux, `linter.py:238-239`). — *LAISSÉ en l'état (fail-open défensif justifié, similaire au fichier absent).*

### P14-F : Validation des durcissements
- [x] **F-56f** : Relancer l'audit comparatif pour vérifier que les gaps sont comblés. — *FAIT : correspondance 1:1 recommandations audit→implémentation vérifiée (grep marqueurs : 3/3 pour chaque nœud A-D).*
- [x] **F-56f** : Suite pytest complète → **587 passed / 0 failed** (586 baseline + 1 nouveau test Linter warning). Marqueurs `test_prompts.py` préservés (INVARIANTS UNIVERSELS/strategy/incremental/multifile/OWASP/CVSS/critical/IN-DIFF ONLY/ANTI-NITS). `run_linter.py` 7/7 ✅.
- [ ] **F-56f** *(à faire au prochain run réel)* : Validation run Bubble Sort + prompt multi-techno (Python) pour vérifier le Router corrigé ne déborde plus vers javascript. — *À valider sur un run réel (les tests mockent le LLM, ne valident pas le comportement réel).*

> **Priorité relative** : P14 est une **optimisation de qualité** (résout des failure modes récurrents), pas un chantier structurel comme P0-P3. Effort faible (ajouts ciblés de lignes), peut se faire en 1-2 cycles. Le gap Linter (P14-E) est le plus urgent car c'est un **vrai bug** potentiel (validation d'une non-livraison).

> **🔗 SYNERGIE P10 ↔ P14 (décidée 2026-08-02)** : P14 (durcir les prompts) et P10 (contexte à la demande / skills lazy) ne sont **pas en concurrence, ils sont complémentaires** — et F-55 est le pont entre les deux.
>
> **Le constat** : l'audit F-55 (`debug/isolation/COMPARISON_AUDIT.md`) révèle que les nœuds DSPy (Router/Architect/Judge/Security) manquent de **procédure concrète** (ordre fail-fast, patterns/grep explicites, biais nommés). Or ce contenu est **riche** (tableau de couverture du Judge, grille OWASP du Security — chacun ~50-100 lignes). L'injecter directement dans le prompt DSPy (P14 seul) le **gonflerait pour TOUS les appels**, même quand le cas ne se présente pas.
>
> **La double stratégie** :
> - **P14 (prompt inline)** = les règles **courtes et toujours pertinentes** vont dans le docstring du nœud (ex: anti-biais Router "ne pas déborder vers javascript", règle Judge "is_approved ssi 0 critical/high"). Toujours actives, ~3-8 lignes.
> - **P10 (skill à la demande)** = les procédures **longues et conditionnelles** deviennent des skills chargés via le middleware P10 quand le nœud en a besoin (ex: grille OWASP complète du Security = ~50 lignes, seulement utile si le code audité contient des patterns dangereux ; tableau de couverture du Judge = ~30 lignes, seulement utile sur les grosses sous-tâches).
>
> **Le pivot F-55** : les docs `MANUAL_<NODE>_METHODOLOGY.md` sont **déjà écrits** comme des skills potentiels (étapes + patterns grep + biais + contre-mesures). P10 serait l'infrastructure pour les injecter aux nœuds DSPy exactement comme les skills sont déjà injectés au Coder/Tester (`build_skills_block`), mais en lazy loading. Concrètement :
> - État actuel : `BASE_SKILLS_BY_NODE` (skills_loader.py) ne branche des skills QUE sur Coder + Tester. Les nœuds DSPy (Router/Architect/Judge/Security) reçoivent `[]`.
> - Cible P10 : les nœuds DSPy reçoivent un skill dérivé de `MANUAL_<NODE>_METHODOLOGY.md`, chargé à la demande (pas en permanence — c'est tout l'intérêt du lazy loading vs l'inline de P14).
>
> **Décision de séquençage** :
> 1. **P14 d'abord** (F-56) — durcir les prompts avec les règles courtes (toujours pertinentes) + corriger le bug Linter P14-E. Effort faible, ROI immédiat.
> 2. **P10 ensuite** — implémenter le middleware lazy loading, puis migrer les procédures longues des docstrings vers des skills `node-<role>-methodology` (dérivés des docs F-55). Permet de **dégonfler** les prompts que P14 aura partiellement alourdis, et d'injecter le contenu riche (grille OWASP, tableau couverture) seulement quand nécessaire.
> 3. La **source unique de vérité** reste les `MANUAL_<NODE>_METHODOLOGY.md` : les prompts P14 (résumé court) et les skills P10 (version complète) en sont tous deux des projections, ce qui évite la dérive.

## 🚀 Priorité 15 : Agent d'Amélioration Continue (Meta-Analyste)
*Parce que les graphes IA génèrent énormément de logs impossibles à lire à la main.*

> **🔎 État Actuel :** Quand le système échoue, l'humain doit lire des milliers de lignes de logs pour comprendre pourquoi l'agent a planté ou bouclé.
> **🚀 Pourquoi ce sera mieux :** Une boucle de feedback structurée (Feature F-61) permet d'automatiser le post-mortem et de corriger les failles du prompt/skill à la source.

- [x] **F-60 / F-61** : Création du script `scripts/run_analyzer.py` qui parse la base de données SQLite/DuckDB et les logs `task-*.log` pour générer un rapport Markdown. — *FAIT.*
- [x] **F-61** : Établissement du workflow "Meta-Analyste" (documenté dans `AGENTS.md`) : exécution autonome par l'IA Antigravity, diagnostic, validation humaine, et patching chirurgical des prompts/skills. — *FAIT.*
- [ ] **Traçabilité des itérations (Git local / Versioning)** : Afin d'éviter la perte d'informations lors des cycles de réécriture (ex: le Coder qui casse un code fonctionnel suite à un faux-positif du Tester), implémenter un versioning automatique dans le dossier `runs/`. À chaque itération de la boucle de code, l'orchestrateur effectue un commit Git local dans le dossier `runs/` (indépendant du dépôt principal) ou sauvegarde l'état dans des sous-dossiers `iter_1/`, `iter_2/`. Cela permettra de faire des `diff` post-mortem exacts pour voir comment l'IA a dégradé le code au fil des essais.

## 📚 Priorité 16 : Audit des prompts externes & ingestion des pépites (référence `references/`)
*Nos prompts F-44 (invariants) + F-0 (rôles) + F-56 (durcissement) sont-ils complets vs l'état de l'art des outils IA ?*

> **🔎 État Actuel :** Le dossier `references/system-prompts-and-models-of-ai-tools/` contient 34 dossiers de prompts extraits d'outils IA (Cline, Codex CLI, Gemini CLI, Claude Code, Cursor, Devin, Kiro, Traycer, Amp, VSCode, etc.). Aucun inventaire machine ni mapping vers nos nœuds n'existait — l'ingestion était ponctuelle (F-44 a absorbé les 10 invariants, mais les mécanismes différenciants restaient dormants).
> **🚀 Pourquoi ce sera mieux :** Un audit complet du **contenu** (pas des licences) produit une cartographie actionnable : ce qu'on a déjà, ce qui nous manque, et par où commencer.

- [x] **F-64 / Audit complet (FAIT)** : Lecture intégrale du contenu de 28+ prompts → livrable `docs/system_prompts_audit_full.md` (8 sections : périmètre, taxonomie 4 archétypes, 10 invariants universels [confirme F-44], 6 catégories de mécanismes différenciants [édition/sécurité/routing/planification/méta-cognition/narration], 20 pépites uniques, mapping ✅/⚠️/❌ vers nos nœuds, gaps actionnables). — *CONFIRME : nos 10 invariants (F-44) et 9 rôles (F-0) sont alignés avec le corpus. IDENTIFIE : 20 mécanismes différenciants non ingérés → alimente F-65.*
- [x] **F-85 / Extraction fiche 29 (FAIT)** : Minage approfondi de `references/system_prompts_leaks/` (~300 prompts, 3 passes parallèles sur Codex/Claude Code/Cowork/Cursor/Devin/Amp/Copilot) → livrable `docs/system_prompts_leaks_extraction.md` (12 sections, citations verbatim `file:line`). **3 découvertes structurantes** : (1) bloc anti-injection Claude Cowork le plus complet du corpus (6 tags imbriqués, défense récursive) ; (2) taxonomie de confirmation Codex 4 tiers (base pour F-65 #1) ; (3) **CORRECTION d'attribution** — plusieurs pépites citées dans F-65 (`<think>` 13 triggers, `report_environment_issue`, `<cite>`, "same turn/update_emitted") ne sont PAS dans ce corpus, elles viennent de la fiche 17/F-64. **Ingestion immédiate** (quick wins + gap n°1) : invariant n°11 anti-injection + durcissement prompts faibles (JSONFixSignature, SkillResearchSignature, DrafterSignature, Synth) + enrichissement invariant n°9 (honest-reporting). Le reste alimente F-65.
- [x] **F-65 / Ingestion des pépites différenciantes** : Porter les mécanismes actionnables identifiés dans le code. — *FAIT (scope A : tier prompts 5/5 + 3 quick wins).* **5 mécanismes prioritaires** : (1) **Gates bloquantes** → invariant n°5 enrichi d'une grille de **réversibilité/blast-radius** (Codex 4-tier + Claude Code 3-tier, fiche 29) + role `security` classifie chaque finding par réversibilité. (2) **Write-lock parallel policy** → role `router` documente la politique (parallèle ssi cibles d'écriture disjointes ET aucun contrat partagé muté ; séquentiel sinon) — Amp `amp-code.md:466-480` + Codex `codex-full.md:1295-1310` (fiche 29). Fan-out reste séquentiel par défaut (VRAM local 6 Go). (3) **Self-correction vérifiable** → nouvel **invariant n°12** « don't end with a promise » (Claude Code `claude-code-desktop-fable-5.md:105`, fiche 29 + Cursor `tools_used=>update_emitted` **fiche 17** `Agent Prompt 2025-09-03.txt:156`) + role `judge` exige vérifications effectives. (4) **Citation localisation canonique** → format `file:start-end` standardisé sur `Finding.location` (Devin `devin-cli.md:84-90` + Cursor `cursor.md:57-90`, fiche 29) dans roles `judge`+`security`. (5) **Quality gates triage** → role `web_tester` enrichi (deltas PASS/FAIL + requirements coverage) — VSCode gpt-5 `gpt-5.txt:43` (**fiche 17**, absent de la fiche 29). **3 quick wins secondaires** : EARS format (`architect`, Kiro fiche 17) + engineering mindset (`coder`/`coder_frontend`, VSCode fiche 17) + `{{secret_name}}` canary (`security`, Warp `warp-2.0-agent.md:86-90` fiche 29). **Hors scope ce cycle** (scope B/C futurs) : gate logicielle `requires_approval` réelle sur bash_command, modèle `TestResult` structuré PASS/FAIL delta, parallélisation réelle du fan-out. Attributions vérifiées par grep indépendant (cf. corrections d'attribution F-85 confirmées). 28 cas paramétrés `test_prompts.py` PASS, suite complète 711 passed / 0 régression F-65 (12 échecs pré-existants sur main).

  **Relation avec P10/F-57** : F-65 (inline dans les prompts, toujours actif) précède logiquement F-57 (lazy loading des skills) — même séquençage que P14→P10 : on durcit d'abord en inline, puis on migre le contenu long vers les skills si besoin.


## 🔴 Priorité 17 : Jeux de tests par nœud (Isolation LLM pour debug rapide)
*Devant P0-P16. Sans ça, chaque bug d'un nœud coûte un run E2E complet de 30-40 min. C'est le levier de vélocité de debug n°1.*

> **🔎 État Actuel :** Le débogage d'un nœud (prompt, skill, logique) nécessite de relancer tout le graphe (`agent_graph.py`), qui prend 30-40 min en GPU-local. Les scripts d'isolation existants (`run_coder_tca.py`, `run_coder_codeagent.py`) sont obsolètes (ne reproduisent pas le Drafter/multi-fichier/visions) ou utilisent des mocks qui ne valident pas le comportement LLM réel. `debug/run_coder.py` (créé pendant le fix F-88) est le premier script moderne (multi-fichier + draft injecté + vrai `execute_coder_node`), mais il ne couvre que le Coder.
> **🚀 Pourquoi ce sera mieux :** Un script d'isolation + un jeu de fixtures (entrées figées) par nœud permet d'itérer en **secondes/minutes** au lieu de dizaines de minutes. On coupe si erreur, on corrige, on relance — la boucle de debug que le fix F-88 a prouvée indispensable (le bug du hash CodeAgent n'aurait jamais été trouvé en run E2E complet sans itération rapide).

> **⚠️ Prérequis découvert (F-88 post-mortem 2026-08-08) :** le bug du hash CodeAgent vide a mis 1h10 de run E2E + post-mortem manuel à diagnostiquer, alors qu'il était reproductible en <1s sur le vrai parseur (`extract_tool_calls_from_step` → `compute_material_fingerprint`). Un jeu de tests par nœud aurait attrapé ce bug **avant** la PR. C'est la leçon qui place cette priorité en MAX.

- [x] **Coder (`debug/run_coder.py` — DÉJÀ CRÉÉ)** : appelle le vrai `execute_coder_node` (workflows.py:573). Inputs : spec (Bubble Sort par défaut, ou `@fichier`), `target_files` (multi-fichier), draft injecté optionnel (`--draft runs/.../draft_*.md`). Isolation stricte (`debug/coder_isolation_out/` nettoyé). Fixtures : `prompts/bubble_sort_spec.md`, `prompts/dashboard_admin_*.md`. — *FAIT (fix F-88) : script `debug/run_coder.py` opérationnel.*
- [x] **Drafter** : appelle le vrai `execute_drafter_node` (workflows.py:558). Input : `sub_dict` (content + target_files + strategy). Output : `draft_markdown` sauvegardé dans un fichier pour inspection visuelle. Permet de valider la qualité du brouillon sans relancer le Coder. — *FAIT (F-89) : `debug/run_drafter.py` + draft sauvegardé pour réinjection dans run_coder.py --draft.*
- [x] **Architect** : appelle le vrai `execute_architect_node` (DSPy). Input : spec raffinée (PromptRefiner output). Output : `ArchitectOutput` (sous-tâches + stratégies simple/incremental/multifile + skills sélectionnés F-57 v3). Valide le découpage sans lancer le Coder. — *FAIT (F-89) : `debug/run_architect.py`.*
- [x] **Router** : appelle `execute_router_node`. Input : prompt brut. Output : techno détectée. Jeu de tests : prompts variés (Python, React, HTML, Rust, ambigu) pour valider que le Router ne déborde plus vers javascript (bug F-56a). — *FAIT (F-89) : `debug/run_router.py` (5 prompts figés).*
- [x] **PromptRefiner** : appelle `execute_prompt_refiner_node`. Input : prompt brut vague. Output : spec structurée + ambiguities_detected. Jeu de tests : prompts avec termes vagues ("rapide", "user-friendly"). — *FAIT (F-89) : `debug/run_prompt_refiner.py` (3 prompts figés).*
- [x] **Judge** : appelle `execute_code_judge_node`. Inputs : code généré + test_results + security + task_requirements + git_diff (F-70). Output : verdict + findings. Jeu de tests : code correct (→ approved), code avec bug subtil (→ critical), code avec nit de style (→ low, pas critical). — *FAIT (F-89) : `debug/run_judge.py` (4 scénarios dont fail-closed security=None).*
- [x] **Security** : appelle `execute_security_reviewer_node`. Inputs : code. Output : is_secure + findings (CVSS). Jeu de tests : code propre, code avec XSS (innerHTML), code avec eval, code avec pickle.loads. — *FAIT (F-89) : `debug/run_security.py` (4 codes figés).*
- [x] **Tester (web)** : `debug/run_web_tester_standalone.py` existe déjà (F-45). À étendre avec fixtures : HTML correct (→ success), HTML bugué (→ failure documenté). — *EXISTANT (F-45).*
- [x] **Convention** : un dossier `debug/fixtures/` avec des jeux d'entrées figées par nœud (specs, codes buggés/corrects, drafts). Convention documentée dans `debug/isolation/README.md` (existant F-55). Chaque script affiche le contrat entrée/sortie + métriques (tokens, durée, steps). — *FAIT (F-89) : fixtures inline dans chaque script + README isolation mis à jour.*
- [x] **(F-89) Fixture golden files** : figer les sorties attendues des nœuds déterministes (Linter, Static Tester) sous `debug/fixtures/golden/` pour détecter les régressions de comportement. Les nœuds LLM restent non-golden (non-déterministes), mais leurs entrées sont figées. — *FAIT (F-89) : `debug/fixtures/golden/README.md` documente la convention (golden = déterministe seulement).*


---

## 🎯 Amélioration Continue : Log de Modifications Récentes

### F-62 : Correction perte de contexte Architecte (Shift Context)
> *Suite à une analyse post-mortem, l'Architecte s'est avéré trop zélé dans sa synthèse, perdant les contraintes métier du prompt initial.*

- **Vérification** : En examinant le log d'un run, on constate que l'Architecte a résumé un cahier des charges très détaillé en un paragraphe générique de 3 lignes, omettant les tailles, couleurs et délais précis.
- **Validation** : Le Coder recevait un prompt beaucoup trop "léger", ce qui le forçait à deviner les paramètres et empêchait la restitution parfaite du besoin utilisateur.
- **Modification** :
  1. Durcissement du prompt de `ArchitectSignature` dans `dspy_nodes.py` (RÈGLE DE PRÉSERVATION DES DONNÉES) pour interdire le résumé des valeurs techniques explicites.
  2. Injection dynamique du contexte global initial (`original_content`) directement dans le prompt système du Coder (`execute_coder_node` dans `nodes.py`) pour garantir la transmission de l'intégralité de la demande.

### F-63 : Correction Fallback llama-server (Mismatch mmproj)
> *Suite à l'échec d'un test d'exécution du Coder isolé sur Qwen3.5-9B, une analyse a révélé un crash backend silencieux.*

- **Vérification** : Diagnostic de l'erreur `404 model 'qwen3.5:2b' not found`. L'enquête a révélé que le crash initial provenait de `llama-server`.
- **Validation** : Le serveur tentait de charger un adaptateur de vision `mmproj` (configuré pour Gemma) inadapté au modèle texte `Qwen3.5-9B`. Ce crash provoquait un timeout de la sonde de santé (`/health`), forçant le gestionnaire `model_lifecycle` à faire un repli (fallback) vers un serveur Ollama externe en utilisant l'identifiant par défaut `qwen3.5:2b`.
- **Modification** : Retrait de l'export de `FAST_MMPROJ` pour l'environnement de test du modèle texte, permettant à `llama-server` de démarrer correctement sans planter.

### F-64 : Audit complet des system prompts d'outils IA
> *P16 — Cartographie de l'état de l'art des prompts d'agents pour calibrer nos nœuds.*

- **Vérification** : Le dossier `references/system-prompts-and-models-of-ai-tools/` (34 outils) n'avait jamais été audité en profondeur pour en extraire les mécanismes exploitables ; seule une synthèse manuelle (`system_prompts_analysis.md`) existait.
- **Validation** : Lecture intégrale du contenu de 28+ prompts → livrable `docs/system_prompts_audit_full.md`. Confirme nos 10 invariants (F-44) et 9 rôles (F-0) alignés avec le corpus. Identifie 20 mécanismes différenciants non ingérés (gates bloquantes, write-lock policy, self-correction, citation obligatoire, quality gates triage…).
- **Modification** : Aucune modification de code (audit pur). Alimente F-65 (ingestion des pépites, pending).

### F-65 : Ingestion des pépites prompts différenciantes *(FAIT — scope A)*
> *P16 — Porter dans le code les 5 mécanismes actionnables prioritaires identifiés par F-64.*

- **Vérification** : F-64 (fiche 17) + F-85 (fiche 29) ont identifié 20 mécanismes différenciants non ingérés ; 5 prioritaires + 3 quick wins secondaires retenus pour ce cycle.
- **Validation** : scope A (tier prompts uniquement, cohérent avec la lignée F-44→F-56→F-85). Aucune modif de schéma (`models.py` inchangé), aucune gate logicielle, aucun changement au contrat runners.
- **Modification** : (a) `prompts.py` — invariant n°5 enrichi (grille réversibilité/blast-radius) + nouvel invariant n°12 (self-correction vérifiable « don't end with a promise ») + 7 role blocks enrichis (router write-lock policy, architect EARS, coder/coder_frontend engineering mindset, web_tester deltas+requirements coverage, judge self-correction+citation `file:start-end`, security réversibilité+`{{secret_name}}` canary) + commentaires stale corrigés (10→12). (b) `docs/NODES_AND_SKILLS.md` — passage de 11 à 12 invariants + nouvelle section F-65 avec tableau des attributions exactes et corrections d'erreurs. (c) `tests/test_prompts.py` — 28 cas paramétrés (11 nouveaux marqueurs) + test compte invariants mis à jour + test grille réversibilité. 52 passed / 0 failed sur `test_prompts.py`. **Attributions vérifiées** : `tools_used=>update_emitted` = Cursor fiche 17 (PAS fiche 29) ; `<cite>` = DeepWiki fiche 17 (n'existe pas en fiche 29) ; quality gates triage = VSCode gpt-5 fiche 17 (absent fiche 29). Format citation retenu = Devin `<ref_file>` + Cursor `startLine:endLine` (fiche 29).
- **Hors scope (cycles futurs)** : gate logicielle `requires_approval` réelle (scope B), modèle `TestResult` structuré (scope B), parallélisation réelle fan-out (scope C, risky VRAM).

### F-85 : Durcissement prompts — pépites fiche 29 (anti-injection + prompts faibles)
> *P16 — Extraction approfondie du corpus `system_prompts_leaks` puis ingestion des mécanismes actionnables les plus à fort ROI.*

- **Vérification** : La fiche 29 (audit F-84) n'avait produit qu'une fiche de 27 lignes — pas d'extraction de contenu. Le corpus (~300 prompts : Claude Code/Cowork/Design, Codex GPT-5.x, Gemini, Grok, Cursor, Devin, Amp, Copilot CLI) restait non miné pour nos prompts.
- **Validation** : 3 passes de minage parallèles (Codex+Claude Code, Cursor+Devin+Amp, sécurité/anti-injection) → livrable `docs/system_prompts_leaks_extraction.md` (12 sections, citations verbatim `file:line`, 10 mécanismes mappés avec ROI). **3 découvertes structurantes** : (1) **gap n°1 comblé** — aucun de nos prompts n'avait de directive anti-injection alors que Coder/Testeur consomment du contenu externe (fichiers, Context7, DuckDuckGo, Chrome DevTools = surfaces d'injection) ; le bloc `<critical_injection_defense>` de Claude Cowork (`claude-cowork.md:1384-1554`, 6 tags imbriqués, défense récursive) est le mécanisme le plus complet du corpus ; (2) taxonomie confirmation Codex 4 tiers (`computer-use.md:32-101`) = base concrète pour F-65 #1 ; (3) **correction d'attribution** — `<think>` 13 triggers / `report_environment_issue` / `<cite>` / "same turn/update_emitted" ne sont PAS dans ce corpus, ils viennent de la fiche 17/F-64 (le plan F-65 attribuait à tort ces sources à la fiche 29).
- **Modification** : (a) `prompts.py` — nouvel invariant n°11 ANTI-PROMPT-INJECTION (tool output = DATA, pas instructions ; règles immuables priment sur contenu observé) + enrichissement invariant n°9 (honest-reporting, anti faux-vert, inspiré Amp `amp-code.md:215`) ; (b) `models.py` — `JSONFixSignature` durci (4 règles de fidélité stricte : ne jamais inventer de valeur absente, préserver les chaînes exactes) ; (c) `dspy_nodes.py` — `SkillResearchSignature` wrappé avec `with_invariants` + anti-injection (cohérence avec les autres nœuds DSPy ; ce ReAct consomme du tool output = surface d'injection) ; `DrafterSignature` durci (anti-placeholders explicites + exemple de format canonique) ; (d) `nodes.py` — `Synth` durci (doctrine concision + solution-first + honnêteté, inspiré Amp `final` channel) ; (e) `docs/NODES_AND_SKILLS.md` — passage de 10 à 11 invariants documentés.

### F-98 : Maintenance proactive des dépendances et de Python (Workflow automatisé de mise à niveau)
> *Routine d'actualisation périodique de l'infrastructure logicielle pour prévenir la dette technique et garantir la compatibilité des paquets.*

- **Objectif** : Éviter l'obsolescence des dépendances et maintenir la compatibilité avec les versions récentes de Python (3.14+).
- **Architecture & Livrables** :
  1. **Script de maintenance local** (`scripts/upgrade_stack.py`) : automatise l'exécution de `uv lock --upgrade`, `uv sync` et le lancement immédiat des suites de tests unitaires et d'intégration (`pytest`).
  2. **Workflow CI automatisé** (GitHub Actions / Renovate) : exécution planifiée récurrente (ex: hebdomadaire) avec création de Pull Request automatique si tous les tests passent.
  3. **Matrice de compatibilité** : surveillance des dépendances critiques (DSPy, smolagents, LiteLLM, FastAPI, WebSockets).
- **Statut** : ✅ **FAIT** — Script `scripts/upgrade_stack.py` opérationnel (gestion `uv lock`, parsing diff, encodage Windows, rapport markdown et options pytest) + Section 11 dans `AGENTS.md`.

