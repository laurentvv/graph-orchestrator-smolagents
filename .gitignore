# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# --- Environnement virtuel ---
.venv/
venv/

# --- Configuration locale (ne pas committer les secrets) ---
.env

# --- IDE ---
.vscode/
.idea/
.zcode/

# --- OS ---
.DS_Store
Thumbs.db

# --- Donnees runtime (bases DuckDB : Knowledge Graph, Event Stream, Runs History) ---
# data/ regroupe toutes les bases DuckDB du projet (F-75 : migration KG vers data/).
# Données runtime régénérables — jamais commitées.
data/
*.db
*.db.wal
*.duckdb
graph_orchestrator.db.bak.*

# --- Code source externe de référence (volumineux, sous-repos git) ---
references/

# --- Artefacts générés par le graphe (régénérables) ---
landing_page/
bubble_sort/
chartjs_dashboard/
index.html
codeagent_compare/

# --- Frontend (web/) ---
web/node_modules/
web/dist/

# Output daté par run (Priorité 13 du plan usine logicielle)
runs/
debug/
logs/

# Binaires lourds bundlés (build CUDA llama.cpp, ~0,7-1 Go) — gitignoré.
# llama_server.py détecte le GPU NVIDIA (nvidia-smi) et découvre automatiquement :
#   vendor/llamacpp-cuda13/ (préféré, CUDA 13.x) → vendor/llamacpp-cuda/ (legacy 12.x)
#   → PATH système (fallback Vulkan/CPU). MAJ : scripts/update_llamacpp.ps1
vendor/
python_health_report.md

models/

# --- Assistances IA externes ---
.agents/
.claude/
