# Contrat de Validation

## Critères d'Acceptation Automatisés
- [ ] Critère 1 : Le fichier `feature_list.json` existe et respecte le schéma JSON attendu.
- [ ] Critère 2 : Le fichier `contract.md` est présent à la racine.
- [ ] Critère 3 : Le fichier `progress.md` liste les jalons du sprint courant.
- [x] Critère 4 : L'historisation événementielle vit dans `data/event_stream.duckdb` (table `run_event`) — le journal plat est supprimé (F-106 : historique complet récupéré depuis git et ré-importé daté).
- [ ] Critère 5 : Les tests pytest s'exécutent sans erreur.

## Critères de Validation du Coding Workflow (objectif sprint)
- [ ] Critère 6 : Le Router classifie correctement la techno (HTML/JS pour une landing page).
- [ ] Critère 7 : L'Architect produit un plan avec 1 sous-tâche par fichier cible (2-4 max).
- [ ] Critère 8 : Le Coder crée des fichiers NON vides via write_file (garde anti-vide active).
- [ ] Critère 9 : Les 3 fichiers cibles sont créés : landing_page/{index.html, styles.css, script.js}.
- [ ] Critère 10 : Le HTML généré est valide (présence de `<!DOCTYPE html>`, `<html>`, `</html>`).
- [ ] Critère 11 : Le Coder termine par final_answer (pas de boucle de re-écriture infinie).
- [ ] Critère 12 : Le Judge émet un verdict (is_approved booléen) à la fin.
- [ ] Critère 13 : Le Knowledge Graph trace les observations/refutations du run.

## Critères de l'édition sécurisée SEARCH/REPLACE (cycle en cours)
- [ ] Critère 14 : Le Coder peut éditer un fichier existant via search_replace sans corruption (matching tolérant).
- [ ] Critère 15 : search_replace rejette les placeholders (TODO, '...') dans le bloc replace.
- [ ] Critère 16 : search_replace renvoie un feedback didactique (lignes proches) quand le bloc search n'est pas trouvé.
- [ ] Critère 17 : Le Mutex par fichier sérialise les écritures concurrentes (tests test_search_replace.py PASS).
- [ ] Critère 18 : La suite pytest complète passe (0 régression, 11 nouveaux tests inclus).

## Critères de la Persistance d'État (Checkpoints — Priorité 3)
- [ ] Critère 19 : Le `run_id` est stable (dérivé du hash du contenu de tâche) — relancer la même tâche reprend là où c'était arrêté ; deux contenus différents donnent deux run_id différents.
- [ ] Critère 20 : La reprise court-circuite l'Architect (plan rechargé depuis le checkpoint via `ArchitectOutput(**dict)`) et saute les sous-tâches déjà approuvées (résultat `replayed=True`).
- [ ] Critère 21 : La granularité "début d'itération" persiste un état cohérent avant chaque Coder (le checkpoint reflète `(current_subtask_idx, current_iteration)` + `architect_result` + `completed_subtasks`) ; à la reprise on rejoue l'itération complète (idempotent, jamais un état intermédiaire).
- [ ] Critère 22 : `FRESH_START=1` efface le checkpoint existant ; un run allant au bout efface son propre checkpoint (run "terminé"). `save_checkpoint`/`load_checkpoint`/`clear_checkpoint` validés sur DuckDB (upsert, round-trip, absence → None).
- [ ] Critère 23 : La suite pytest ciblée passe (0 régression, 12 nouveaux tests test_checkpoint.py inclus).

## Critères du Tester polyvalent multi-techno + Boucle d'auto-correction (Priorité 2)
- [ ] Critère 24 : `detect_tech` combine Router + extensions de `target_files` ; les extensions l'emportent en cas de conflit (déterministe > LLM) ; fallback `web` si rien de détectable.
- [ ] Critère 25 : `execute_tester_node` dispatche vers le bon runner selon la techno (`get_runner`) ; `task["tech"]` explicite prime sur la détection.
- [ ] Critère 26 : `PythonTestRunner` (subprocess) : exit 0 → `status="success"` ; exit≠0 → `"failure"` + stderr/stdout capturé et tronqué. Le subprocess utilise `sys.executable` (interpréteur du projet, qui a pytest).
- [ ] Critère 27 : Le `WebTestRunner` refactorisé conserve le comportement du bloc Puppeteer d'origine (MCP Chrome DevTools, calcul d'URL, skill web-tester) ; chargé via le loader centralisé.
- [ ] Critère 28 : `truncate_output` préserve head + tail + insère un marqueur transparent (`… [X lignes tronquées] …`) ; texte court retourné intact ; `None`/vide → `""`.
- [ ] Critère 29 : `truncate_history` plafonne l'historique cumulé des réfutations injecté au Coder (≤ `feedback_max_chars`), bugs récents prioritaires ; garde au moins le 1er item (tronqué) pour ne pas laisser le Coder sans info.
- [ ] Critère 30 : La troncature opère **à la lecture** (injection au Coder/Judge) — le contenu écrit en DuckDB reste intégral (pas de collision de `dedup_key`).
- [ ] Critère 31 : Le skill `web-tester` exige désormais un rapport structuré (section « ERREURS CONSOLE JS » = le « stderr » du web) ; le skill `python-tester` documente le verdict pytest (exit code) et la lecture d'un échec.
- [ ] Critère 32 : La suite pytest complète passe (0 régression, nouveaux tests inclus : feedback_utils, tech_detection, python_runner, tester_dispatch, feedback_integration).

## Critères de l'Édition Incrémentale (append_file — F-28)
- [ ] Critère 46 : `append_file(path, content)` AJOUTE `content` à la fin d'un fichier sans réécrire l'existant (contenu préservé) ; crée le fichier s'il n'existe pas (mode `'a'`).
- [ ] Critère 47 : Le mutex par fichier (`_file_lock`) sérialise les écritures concurrentes — 2 appends parallèles ne se corrompent pas (test `test_append_concurrent_does_not_corrupt` : 40 lignes attendues, 0 perdue).
- [ ] Critère 48 : Garde anti-contenu-vide + anti-placeholder — `content=""` ou `"TODO"` est rejeté avec message pédagogique, le fichier N'EST PAS modifié.
- [ ] Critère 49 : Garde anti-doublon légère — si `content` est déjà la fin exacte du fichier, l'opération est signalée (`NOTICE ... duplicate`) SANS réécrire (défense contre le bug "section appendée N fois").
- [ ] Critère 50 : Feedback riche (SWE-agent ACI) — le retour contient le nb de chars ajoutés ET la nouvelle taille totale (chars + lignes), pour que le modèle suive sa progression.
- [ ] Critère 51 : Les sous-dossiers parents sont créés automatiquement (`os.makedirs(exist_ok=True)`), cohérent avec `write_file`.
- [ ] Critère 52 : La suite pytest complète passe (0 régression, nouveaux tests `test_append_file.py` inclus : création, préservation, gardes, anti-doublon, sous-dossiers, concurrence).

## Critères du Cycle P1-P3 (finalisation)
### Linter Shift Left (F-30)
- [ ] Critère 53 : `lint_file(path)` détecte la langue par extension (Python/HTML/CSS/JS/TS/TSX) ; extension inconnue/fichier absent → valide (pas de faux positif, dégradation gracieuse).
- [ ] Critère 54 : Back-end double complémentaire — tree-sitter (SyntaxError, strings non fermées, structures cassées) + py_compile (IndentationError Python — le point noir que tree-sitter tolérant ne voit PAS) + vérifs structurelles HTML (contenu après `</html>`, équilibrage balises).
- [ ] Critère 55 : `execute_linter_node` (déterministe, 0 LLM, `model='tree-sitter-linter'`) retourne `CoderOutput(status, details)` avec status='failure' si AU MOINS un fichier invalide + détails exploitables par le Coder (nom du fichier + erreur).
- [ ] Critère 56 : Branchement workflow — inséré ENTRE le Coder et le Tester ; si invalide → court-circuite le Tester coûteux (écrit réfutation `source='linter'` en DuckDB, relance le Coder à l'itération suivante via `continue`).

### Architect adaptatif (F-29)
- [ ] Critère 57 : `ArchitectTask` expose `strategy` (`Literal['simple','incremental','multifile']`, défaut `'simple'` pour rétro-compat) + `sections` (`List[str]`, vide sauf si `incremental`).
- [ ] Critère 58 : `ArchitectSignature` docstring explique les 3 stratégies + quand utiliser laquelle (HTML/CSS/JS → multifile par défaut, Python/TS → multifile, monolithe imposé → incremental).
- [ ] Critère 59 : `strategy` + `sections` propagés dans `sub_dict` (workflows.py) via `getattr(subtask, 'strategy', 'simple')` → consommés par le prompt Coder (F-32).
- [ ] Critère 60 : Round-trip `ArchitectOutput.model_dump()` → `ArchitectOutput(**dict)` préserve strategy/sections (compatible checkpoint) ; sous-tâche historique sans stratégie → défaut `'simple'`.

### Migration Coder → CodeAgent (P1/F-29a)
- [ ] Critère 61 : `execute_coder_node` instancie un `CodeAgent` (pas un `ToolCallingAgent`) ; `final_answer` attendu en syntaxe Python (`final_answer({...})`), pas en JSON.
- [ ] Critère 62 : `run_with_retry` RÉUTILISÉ (compatible CodeAgent, hérite de MultiStepAgent) ; `extract_and_validate` gère le dict renvoyé par `final_answer` (models.py gère déjà les dicts).

### Prompt Coder réécrit (F-32)
- [ ] Critère 63 : Structure canonique (Rôle → Règles critiques → Format sortie → One-shot → Workflow adapté stratégie → Rappels) ; double-marquage primacy/recency.
- [ ] Critère 64 : Anti "reasoning sans action" — "AGIS, ne raconte pas" + "Une réponse sans appel d'outil = tâche terminée".
- [ ] Critère 65 : Anti triple-quote — "≤60 lignes/appel, chaque bloc syntaxiquement complet, jamais de string/brace ouverte entre 2 appels".
- [ ] Critère 66 : Workflow adapté à la stratégie — `incremental` impose squelette avec marqueurs d'insertion ouverts + append par section (corrige le bug dashboard : on n'append PAS après `</html>` fermé).

### Guard logiciel anti-déraillement (F-33)
- [ ] Critère 67 : `_detect_idle_step` détecte un tour SANS tool call exécuté (inspecte dernier `ActionStep` : `tool_calls`/`code_action`/`observations` tous vides) → message anti-idle ré-injecté (openfox style).
- [ ] Critère 68 : Exception parsing CodeAgent (triple-quote non fermée) → message "découpe au lieu de recommancer le même gros payload" (deer-flow style).
- [ ] Critère 69 : Message de retry adapté au type d'agent — Python (`final_answer(...)`) pour CodeAgent, JSON (`model_json_schema()`) pour ToolCallingAgent.

### Validation globale
- [ ] Critère 70 : La suite pytest complète passe (0 régression, 271 tests : 240 avant + 17 linter + 6 architect strategy + 8 guard).
- [ ] Critère 71 : Import du workflow complet OK — pas de circularité avec `linter.py` + CodeAgent + signatures Architect modifiées.

## Critères de l'Auto-Résolution des Dépendances (Priorité 5 — F-26)
- [ ] Critère 39 : `extract_missing_module(stderr)` extrait le nom de module top-level d'un `ModuleNotFoundError` (`'requests'`, `'requests.auth'` → `'requests'`) ; retourne `None` si pas de `ModuleNotFoundError` ou si le nom extrait n'est pas un identifiant Python valide (regex `^[A-Za-z_][A-Za-z0-9_]*$` — défense en profondeur anti-injection).
- [ ] Critère 40 : Le déclenchement est conditionnel — `extract_missing_module` n'est appelé QUE si `exit_code != 0` ET `settings.auto_install_deps` est vrai ; un AssertionError/SyntaxError/etc. ne déclenche JAMAIS l'install (comportement historique préservé).
- [ ] Critère 41 : `auto_install_deps=False` (opt-out via `AUTO_INSTALL_DEPS=false`) désactive totalement la feature — aucune install tentée, aucune relance, failure normal (comportement historique).
- [ ] Critère 42 : L'installation est **non-persistante** (`pip install` via `sys.executable`, liste d'args sans `shell=True`) — n'écrit ni dans `pyproject.toml` ni dans `uv.lock` (aucun fichier du projet modifié, aucun effet de bord visible en git).
- [ ] Critère 43 : Cap **1 retry** — si l'install réussit, les tests sont relancés une seule fois ; si l'install échoue (PyPI down), aucune relance (pas de boucle infinie).
- [ ] Critère 44 : Dégradation gracieuse — `_install_module` n'élève JAMAIS d'exception (timeout/réseau/pip absent → retourne `False`) ; un échec d'install ne fait jamais planter le run (le test ressort en failure comme avant). L'action est tracée dans `details` pour l'observabilité (ex : `[auto-install] 'requests' installé puis tests relancés`).
- [ ] Critère 45 : La suite pytest complète passe (0 régression, nouveaux tests `test_python_runner.py` inclus : `extract_missing_module` unitaire, comportement auto-install retry/opt-out/non-ModuleNotFoundError/cap, contrat `_install_module` succès/échec/exception).

## Critères de l'Anti-Loop Cryptographique (Priorité 3 — P3 plan usine logicielle)
- [ ] Critère 72 : `compute_tool_call_fingerprint(tool_name, arguments)` produit un hash SHA256 stable — `(tool, args)` identiques → même empreinte, indépendamment de l'ordre des clés du dict (`sort_keys=True`) ; le whitespace de tête/queue des valeurs string est normalisé (les petits LLM ajoutent/suppriment des espaces au hasard).
- [ ] Critère 73 : Deux outils différents appelés avec les mêmes arguments ne sont PAS considérés comme une boucle (le nom de l'outil préfixe le hash) ; deux appels du même outil avec des arguments différents non plus.
- [ ] Critère 74 : `LoopGuard.repeated_action()` renvoie un message pédagogique (`"CIRCUIT BREAKER (Anti-Loop)..."`) uniquement quand une même empreinte atteint le seuil (`threshold`) ; `None` sinon. Seuil `< 2` rejeté (`ValueError`).
- [ ] Critère 75 : Opt-out `LOOP_GUARD_ENABLED=false` désactive totalement la détection (jamais de déclenchement, `record()` no-op) ; seuil configurable via `LOOP_GUARD_THRESHOLD` (défaut 3).
- [ ] Critère 76 : `reset()` vide le compteur entre deux retries (aligné sur la purge de `agent.memory.steps` dans `run_with_retry` — un bug d'une tentative précédente ne fait pas déclencher la suivante).
- [ ] Critère 77 : `extract_tool_calls_from_step` lit les tool calls des deux familles d'agents — ToolCallingAgent (`step.tool_calls`, liste structurée `.name/.arguments`) ET CodeAgent (`step.code_action`, source Python scannée pour les noms d'`@tool` connus) ; step vide → liste vide.
- [ ] Critère 78 : Le guard n'est instancié QUE dans `execute_coder_node` (seul nœud appelant des outils d'écriture — un agent sans outils ne peut pas boucler sur des tool calls) et passé à `run_with_retry` via le paramètre optionnel `loop_guard=None` (non-cassant : les autres nœuds appellent `run_with_retry` sans guard, comportement inchangé).
- [ ] Critère 79 : La suite pytest complète passe (0 régression, nouveaux tests `test_loop_guard.py` inclus : empreinte stable/order/whitespace, détection seuil/opt-out/reset, extraction ToolCallingAgent+CodeAgent, intégration `run_with_retry`).

## Critères du Nettoyage DOM (Priorité 6 — P6 plan usine logicielle, LlamaBot)
- [ ] Critère 80 : `clean_dom_for_llm(html)` supprime ENTIÈREMENT (contenu inclus) les balises bruyantes : `<script>`, `<style>`, `<svg>`, `<canvas>`, `<iframe>`, `<noscript>`, `<template>`, `<head>` — case-insensitive (HTML tolère `<SCRIPT>`), variants auto-fermants (XHTML) inclus.
- [ ] Critère 81 : Les commentaires HTML `<!-- ... -->` sont supprimés (y compris gros contenu conditionnel type `<!--[if IE]>`).
- [ ] Critère 82 : Le contenu sémantique est PRÉSERVÉ — texte, `<div>`/`<table>`/`<p>`, attributs `id`, `class`, `aria-*`, `role` (utiles aux assertions fonctionnelles du Web Tester).
- [ ] Critère 83 : Compactage whitespace — espaces en fin de ligne supprimés, series de ≥3 newlines réduites à 2 ; troncature finale à `max_chars` (défaut 8000, cohérent avec `FEEDBACK_MAX_CHARS`) avec marqueur `[tronqué par dom_filter]`.
- [ ] Critère 84 : Cas limites — `None`/chaîne vide → `""` ; HTML déjà propre → contenu sémantique inchangé.
- [ ] Critère 85 : Gain effectif — sur un HTML réaliste (80% de bruit script+style+svg), la taille est divisée par au moins 3 (justification ROI du plan : "économise massivement les tokens sur le Web Tester").
- [ ] Critère 86 : Branchement `web_tester.py` — le prompt du Web Tester contient une directive "NETTOYAGE DOM" qui fournit un snippet JS (exécuté côté navigateur via `puppeteer_evaluate`, plus efficace qu'un round-trip Python) appliquant le même nettoyage avant analyse/citation du DOM dans le rapport.
- [ ] Critère 87 : La suite pytest complète passe (0 régression, nouveaux tests `test_dom_filter.py` inclus : suppression de chaque famille de balises, casse/XHTML, commentaires, préservation sémantique, compactage, troncature, cas limites, gain effectif).

## Critères du Guard bash denylist (Priorité 8-bis — robustesse runtime)
- [ ] Critère 88 : `check_bash_command(cmd)` renvoie `(allowed, reason)` — ne lève JAMAIS d'exception ; `allowed=True, reason=""` si la commande passe, `allowed=False, reason="<message pédagogique>"` si bloquée.
- [ ] Critère 89 : Blocage des commandes destructrices Unix — `rm -rf /` (+ `/usr`, `/etc`, `/home`...), `rm -rf ~`/`$HOME`, `rm -rf *` non borné, `mkfs`, `dd of=/dev/sd*`, redirection `> /dev/sd*`, fork bomb `:(){ :|:& };:`, `chmod -R 777 /`.
- [ ] Critère 90 : Blocage des commandes destructrices Windows — `format X:`, `rmdir/rd /s /q` sur `C:\`/`%SystemRoot%`/`%Windir%`/`%ProgramFiles%`, `del /f /s /q` idem, `diskpart`, `reg delete HKLM\...`.
- [ ] Critère 91 : Blocage cross-plateforme — `shutdown`/`halt`/`poweroff`/`reboot`, `git push --force`/`-f`, `curl|sh`/`wget|bash` (exécution code distant non inspecté).
- [ ] Critère 92 : Préservation des usages légitimes (anti faux positifs) — `rm -rf ./build`, `rm fichier`, `git push origin main`, `git commit`, `pytest`, `mkdir`, `cat /etc/hostname`, ET un chemin contenant un mot-clé anodin (`cat /format/rapport.txt` n'est PAS bloqué par le pattern `format X:`).
- [ ] Critère 93 : Insensibilité à la casse (`FORMAT C:`, `RM -RF /`) + normalisation whitespace (espaces multiples ne cassent pas la détection) ; commande vide/None → autorisée (no-op laissé à subprocess).
- [ ] Critère 94 : Branchement dans `bash_command` (`tools.py`) — le guard s'exécute AVANT `subprocess.run(shell=True)` ; une commande bloquée ne lance JAMAIS le subprocess (vérifié par test d'intégration). Opt-out `BASH_GUARD_ENABLED=false` contourne le guard (utile pour les envs de confiance).
- [ ] Critère 95 : La suite pytest complète passe (0 régression, nouveaux tests `test_bash_guard.py` inclus : 66 tests paramétrés couvrant blocages Unix/Windows/cross, légitimes préservés, casse/whitespace, message pédagogique, intégration subprocess + opt-out).

## Critères du nœud PromptRefiner (F-39, meta-prompt avant l'Architect)
- [ ] Critère 96 : `PromptRefinerSignature` (DSPy, `ChainOfThought`) prend 2 inputs (`raw_prompt` + `available_capabilities`) et renvoie un `PromptRefinerOutput` Pydantic (`refined_prompt: str` + `ambiguities_detected: List[str]`).
- [ ] Critère 97 : L'instruction (docstring) impose 4 étapes — (1) détection termes vagues (fast/easy/user-friendly/flexible...) → `ambiguities_detected` ; (2) orientation selon capacités dispo (web-tester/python-tester/context7) ; (3) structuration en sections fixes `## Objectif / ## Fonctionnalités attendues / ## Contraintes techniques / ## Critères de validation` (Given/When/Then quand pertinent) ; (4) complétion légère SANS inventer de scope. Règle "Tu STRUCTURES, tu n'INVENTES PAS" + concision ~30 lignes + préservation des exigences explicites.
- [ ] Critère 98 : `_build_capabilities_summary(settings)` produit un résumé compact contenant — le catalogue COMPLET des skills (via `agent_server.skills.list_skills()`, repli lecture dossier `skills/` si import échoue, chaîne vide si tout échoue), le statut Context7 (`bool(CONTEXT7_API_KEY)`, sans connexion réseau), et les testers statiques (Puppeteer/pytest). Dégradation gracieuse à 3 niveaux.
- [ ] Critère 99 : `execute_prompt_refiner_node` clone le pattern `execute_router_node` (`_configure_dspy` + `dspy.ChainOfThought` + `asyncio.to_thread`), utilise le modèle REASONING (gemma), `node="prompt_refiner_dspy"`, dégradation `(None, None)` sur exception (l'appelant replie sur prompt brut).
- [ ] Critère 100 : Branchement dans `run_coding_workflow` — le nœud s'exécute APRÈS le calcul du `run_id` (l.221, hash du prompt BRUT → stable) et AVANT le Router. Si succès → `task_content = refined.refined_prompt` muté dans `seed_tasks[0]['content']` (propagé à Router/Architect/Tester/Judge) ; si échec/None → repli prompt brut.
- [ ] Critère 101 : Persistance checkpoint — `refined_prompt` est sauvegardé dans le payload (`save_coding_state`) et hydraté à la reprise : si `checkpoint["refined_prompt"]` existe, le nœud LLM est SKIPPÉ (économie, même logique que `architect_result`).
- [ ] Critère 102 : Opt-out `PROMPT_REFINER_ENABLED=false` → le nœud n'est jamais appelé, l'Architect reçoit le prompt brut (comportement historique). Défaut `True` dans la dataclass (ne casse pas les `Settings(...)` positionnels en test).
- [ ] Critère 102-bis : Setting `PROMPT_REFINER_MODEL_ID` — si vide (défaut), le nœud utilise `reasoning_model_id` (gemma-12B, ~5min/prompt) ; si setté, il utilise ce modèle dédié (recommandation : gemma-4-E4B, ~8× plus rapide pour qualité équivalente — test comparatif réel (consigné dans l'historique des événements DuckDB)). `_configure_dspy` reçoit le modèle dédié et la métrique `model` reflète le modèle réellement utilisé. Fallback transparent sur `reasoning_model_id` si vide (rétro-compatibilité).
- [ ] Critère 103 : Context7 est CITÉ seulement dans le catalogue (statut dispo), JAMAIS consommé par le PromptRefiner — l'Architect fait déjà le pré-fetch en `dspy_nodes.py:225` (pas de duplication d'appel).
- [ ] Critère 104 : La suite pytest complète passe (0 régression, nouveaux tests `test_prompt_refiner.py` inclus : exécuteur mock LLM + available_capabilities propagé, dégradation gracieuse, helper capabilities avec/sans clé/repli, E2E toggle off, E2E toggle on + propagation Architect, E2E checkpoint skip). Les 3 helpers E2E existants (`test_escalation`/`test_checkpoint`/`test_feedback_integration`) mockent `execute_prompt_refiner_node` pour éviter un appel LLM réel en test.

## Critères de l'Output daté par run (Priorité 13 — isolation des artefacts)
- [ ] Critère 105 : `_slugify(text)` produit un slug sûr cross-plateforme (lowercase, `[^a-z0-9]→_`, collapse `__+`, strip bord, truncate `max_len=24`) ; fallback `'run'` si texte vide/nul ; aucun caractère interdit Windows (`:`/`?`/`*`/`<>`/`|`).
- [ ] Critère 106 : `_resolve_run_output_dir` renvoie un chemin **absolu** — si `checkpoint["output_dir"]` existe → REPRED ce dossier (reprise après crash, fichiers préservés) ; sinon → nouveau dossier daté `{output_dir}/{YYYY-MM-DD}_{HHMM}_{slug}/` résolu en absolu.
- [ ] Critère 107 : `_scoped_chdir(target_dir)` est un context manager qui restore **TOUJOURS** le cwd original à la sortie (y compris en cas d'exception mid-bloc — `try/finally`). Critical pour les tests E2E qui enchaînent plusieurs runs.
- [ ] Critère 108 : Branchement dans `run_coding_workflow` — ORDRE CRITIQUE respecté : (1) KG instancié AVANT le chdir → DuckDB reste à `kg_path` stable ; (2) checkpoint chargé AVANT la décision reprise/nouveau ; (3) `run_id` (hash du contenu brut, PAS du cwd) calculé avant et inchangé.
- [ ] Critère 109 : Le `output_dir` est persisté dans le checkpoint (`save_coding_state` payload clé `"output_dir"`) pour que la reprise après crash reprenne dans le MÊME dossier.
- [ ] Critère 110 : Les fichiers générés par le Coder atterrissent dans `runs/.../`, PAS à la racine du projet (validé par test E2E qui écrit un fichier relatif et vérifie son emplacement).
- [ ] Critère 111 : `kg_path` ne suit PAS le chdir — la DB DuckDB reste à sa place d'origine (testé `test_e2e_kg_path_stable_after_chdir` : la DB existe à `kg_path`, pas dans `runs/`).
- [ ] Critère 112 : Cas `kg_path=":memory:"` (tests) — ne crash pas (dossier neuf créé, pas de persistance checkpoint).
- [ ] Critère 113 : La suite pytest complète passe (0 régression, nouveaux tests `test_output_dir.py` inclus : slugify 5 cas, resolve 3 cas, scoped_chdir 2 cas, E2E 3 cas — écriture run dir, reprise même dossier, kg_path stable).
- [ ] Critère 114 : `repair_orphan_tool_results` détecte un `tool_use` orphelin (sans `tool_result` associé) et injecte une fausse réponse `FAKE_INTERRUPTED` (`{"status": "error", "error": "Interrompu"}`) — testé `test_single_orphan_repaired` (1 réparation, temoin id+content).
- [ ] Critère 115 : `repair_orphan_tool_results` ignore les appels d'outil déjà répondus (idempotence complète) — testé `test_already_answered_untouched` (0 réparation, liste strictement égale) + `test_reapply_is_noop` (0 réparation au second passage).
- [ ] Critère 116 : `repair_orphan_tool_results` gère les orphelins multiples sur plusieurs messages (réparation = somme) et détecte les blocs sérialisés `type="function"` + `function.name` (OpenAI/smolagents ToolCall.dict) via `id` — testé `test_multiple_orphans_repaired` (3 réparations) et `test_block_with_id_and_name_is_tool_use`.
- [ ] Critère 117 : `repair_orphan_tool_results` résiste aux cas dégénérés — appel sans id, string `content`, content non-liste — sans crasher (testé `test_call_without_id_skipped`, `test_string_content_skipped`, `test_non_list_content_skipped`).
- [ ] Critère 118 : `repair_orphan_steps` répares un ActionStep smolagents avec `tool_calls` mais sans `observations`/`error` en injectant `observations=FAKE_INTERRUPTED` — testé `test_steps_level_uses_observations` (1 étape réparée, `.observations == FAKE_INTERRUPTED`).
- [ ] Critère 119 : `repair_orphan_steps` ignore les étapes non-orphelines : avec observations, avec erreur, réponse finale, sans tool_calls (0 réparation) — testé `test_steps_status_nodes` + `test_final_answer_respected`.
- [ ] Critère 120 : L'intégration dans `nodes.run_with_retry` est défensive (bloc `try/except Exception`) et répare la mémoire avant chaque exécution d'agent — vérifié par lecture du code (P8 block, lignes ~174-189).
- [ ] Critère 121 : La suite pytest complète passe toujours (0 régression ; `tests/test_orphan_repair.py` 11 tests + `test_guard.py` + `test_loop_guard.py`, total 405 passed / 0 failed).

## Critères du Sanitizer (Auto-typage des arguments d'outil — F-42)
- [ ] Critère 122 : `coerce_value("1, 80", {"type": "integer"})` renvoie `80` (dernier entier de la chaîne) — le cas cible F-42, testé `test_coerce_integer_extracts_last_number`.
- [ ] Critère 123 : `coerce_value` préserve les valeurs déjà typées (int→int, float→int tronqué, bool→bool) et laisse les valeurs non coercibles inchangées (`"abc"`→`"abc"`) — testé `test_coerce_integer_already_int_and_float` + `test_coerce_integer_uncoercible_left_intact` + `test_coerce_integer_bool_preserved`.
- [ ] Critère 124 : `coerce_value` gère `number` (string→float), `boolean` (`"true"`/`"1"`/`"yes"`/`"on"`→True ; `"false"`/`"0"`/`"no"`/`"off"`→False ; non reconnu→inchangé), `string` (non-str→str()) — testé `test_coerce_number` + `test_coerce_boolean` + `test_coerce_string`.
- [ ] Critère 125 : `coerce_value` parse les structures `array`/`object` depuis une string JSON (`json.loads`→`ast.literal_eval` fallback) et laisse une string non parsable inchangée ; `None` respecté (nullable) — testé `test_coerce_array_from_json_string` + `test_coerce_object_from_json_string` + `test_coerce_unparseable_structure_left_intact` + `test_coerce_none_respected`.
- [ ] Critère 126 : `sanitize_tool_arguments` coerce uniquement les clés connues du schéma `inputs` ; les clés inconnues restent inchangées ; les arguments non-dict et les `inputs` non-dict sont renvoyés inchangés — testé `test_sanitize_known_keys_only` + `test_sanitize_unknown_keys_untouched` + `test_sanitize_non_dict_arguments_untouched` + `test_sanitize_non_dict_inputs_noop`.
- [ ] Critère 127 : `SanitizedTool` copie `name`/`description`/`inputs`/`output_type` de l'outil sous-jacent et délègue à son `__call__` en coerçant les kwargs (l'outil réel reçoit des arguments typés, pas les strings) — testé `test_sanitized_tool_copies_metadata` + `test_sanitized_tool_coerces_and_delegates`.
- [ ] Critère 128 : `SanitizedTool` reste un `BaseTool` (satisfait les `isinstance` du framework) ; `wrap_tool` enveloppe un `BaseTool` ; `sanitize_tools(enabled=True)` enveloppe tous, `sanitize_tools(enabled=False)` est no-op (même objet liste) — testé `test_sanitized_tool_valid_callable_isinstance` + `test_wrap_tool_wraps_base_tool` + `test_sanitize_tools_enabled_wraps_all` + `test_sanitize_tools_disabled_is_noop`.
- [ ] Critère 129 : Le branchement dans `nodes.execute_coder_node` et `execute_architect_node` enveloppe les tool sets via `sanitize_tools(..., enabled=settings.sanitizer_enabled)` ; config `sanitizer_enabled` (env `SANITIZER_ENABLED`, défaut True) ajoutée à `config.py` — vérifié par lecture du code (lignes ~424-427 Architect, ~488-491 Coder ; config.py champ + load_settings).

## Critères de l'Idempotence des effets de bord (Priorité 8-bis — F-43)
- [ ] Critère 130 : `IdempotencyStore.once(key, fn)` retourne `True` la 1re fois (fn exécutée), `False` la 2e (skippée) — testé `test_once_runs_fn_first_time_skips_second` ; `committed` reflète l'état — testé `test_committed_reflects_state`.
- [ ] Critère 131 : Inflight set bloque un 2e appel concurrent sur la même clé (fn tourne 1 fois) ; 20 threads concurrents → fn exécutée exactement 1 fois — testé `test_inflight_blocks_concurrent_same_key` + `test_concurrent_once_runs_fn_exactly_once`.
- [ ] Critère 132 : Si `fn` lève, `once` propage l'exception et ne marque PAS done (retryable au prochain replay) — testé `test_fn_raises_not_marked_done_retryable`.
- [ ] Critère 133 : Backing durable DuckDB — un nouveau `IdempotencyStore` (même `kg`+`run_id`, RAM fraîche simulant un crash/nouveau process) voit `committed` True via le backing et `once` skip — testé `test_durable_backing_survives_new_store`.
- [ ] Critère 134 : Isolation par `run_id` — un `run_id` différent ne voit pas les records d'un autre run — testé `test_durable_different_run_id_not_committed`.
- [ ] Critère 135 : Rétention — une key expirée (au-delà de `retention_s`) est re-runnable ; `prune_idempotency` supprime les records expirés — testé `test_retention_expired_key_rerunnable` + `test_prune_removes_old_records`.
- [ ] Critère 136 : `make_op_key(run_id, kind, *parts)` est stable (mêmes inputs → même clé), différenciée par `kind` et par `parts`, et bornée (hash SHA256, pas de clé arbitrairement longue) — testé `test_make_op_key_stable` + `test_make_op_key_differentiated_by_kind` + `test_make_op_key_differentiated_by_parts` + `test_make_op_key_bounded_for_large_content`.
- [ ] Critère 137 : `_scoped_idempotency(store)` set le store courant, le clear TOUJOURS à la sortie (y compris sur exception) ; `store=None` → no-op — testé `test_scoped_idempotency_sets_and_clears` + `test_scoped_idempotency_clears_on_exception` + `test_scoped_idempotency_none_store`.
- [ ] Critère 138 : `KnowledgeGraph` — `save_idempotency` (INSERT OR IGNORE, idempotent) + `is_idempotency_committed` + `prune_idempotency` + `clear_idempotency(run_id)` (efface CE run seulement) — testé `TestKnowledgeGraphIdempotency` (4 tests). `clear_idempotency(run_id)` est appelé aux MÊMES sites que `clear_checkpoint` (FRESH_START l.309 + fin de run l.706 de workflows.py) — vérifié par lecture du code.
- [ ] Critère 139 : Intégration `append_file` — un 2e append identique (même path+content) CE RUN est skippé par le store (au-delà de l'anti-doublon textuel qui échoue si un append ultérieur a déplacé la fin du fichier) ; sans store (opt-out) → comportement historique — testé `test_append_file_idempotent_on_replay` + `test_append_file_no_store_historical_behavior`. `write_file` n'est PAS wrappé (idempotent par écrasement par design).
- [ ] Critère 140 : Intégration `_install_module` (pip) — un 2e appel pour le même module CE RUN est skippé (backing DuckDB) ; un échec n'est PAS marqué done (retryable via `_InstallFailed`) — testé `test_install_module_skipped_on_second_call` + `test_install_module_failure_not_marked_done_retryable`. Suite pytest complète → 442 passed / 0 failed (417 baseline + 25 nouveaux), 0 régression.

## Critères de la Refonte Prompts (Priorités 0 + 0-bis + 6 — F-44)
- [ ] Critère 141 : `UNIVERSAL_INVARIANTS` contient les 10 patterns universels numérotés (read-before-write, no whole-file rewrite, format d'édition formel, never assume library available, verify-after, approval gating, anti-boucle, concision, todo tracking, parallel tool calls) — testé `test_all_10_patterns_present`.
- [ ] Critère 142 : Les 2 marqueurs doctrinaux bonus sont présents dans les invariants — professional objectivity (`FACTUEL ET OBJECTIF`) ET defensive security (`SÉCURITÉ DÉFENSIVE`) — testé `test_key_doctrinal_markers_present`.
- [ ] Critère 143 : `ROLE_BLOCKS` définit les 9 rôles du graphe (router, architect, prompt_refiner, coder, coder_frontend, web_tester, judge, security, escalation) — testé `test_all_9_roles_defined`.
- [ ] Critère 144 : Chaque rôle porte ses marqueurs doctrinaux spécifiques (Judge : in-diff only + anti-nits + professional objectivity ; Security : OWASP + CVSS + defensive-only ; Architect : read-only + 5 axes ; Coder : type hints + verify-after ; WebTester : pyramide + AAA) — testé paramétré `test_role_contains_doctrinal_marker` (17 cas).
- [ ] Critère 145 : `build_role_header(role)` assemble rôle + invariants pour les prompts smolagents ; rôle inconnu → invariants seuls (robustesse, pas de crash) ; `with_invariants(role, doc)` assemble rôle + invariants + métier dans le bon ordre pour DSPy — testé `TestBuildRoleHeader` + `TestWithInvariants`.
- [ ] Critère 146 : Les 6 Signatures DSPy (Router/Architect/PromptRefiner/Security/Judge/Escalation) ont les invariants injectés dans leur `__doc__` via `__doc__ = with_invariants(...)` — mécanisme validé empiriquement (DSPy lit `__doc__` via metaclass, non écrasé) — testé paramétré `test_signature_has_invariants`.
- [ ] Critère 147 : `Finding` (models.py) est le schéma Pydantic de sévérité partagé par Judge et Security (`severity` critical/high/medium/low + `category` + `location` + `description` + `suggestion`) ; `CodeJudgeOutput` et `SecurityOutput` ont un champ `findings: List[Finding] = []` ADDITIF (défaut `[]` = rétro-compatible checkpoints + tests existants) — round-trip Pydantic validé — testé `TestFindingAndAdditiveModels`.
- [ ] Critère 148 : Suppression des ~180 lignes de nœuds smolagents DÉPRÉCIÉS (versions mortes de `execute_router_node`/`execute_architect_node`/`execute_security_reviewer_node`/`execute_code_judge_node` dans `nodes.py`, jamais appelées par `run_coding_workflow`) + imports morts nettoyés (`RouterOutput`/`ArchitectOutput`/`SecurityOutput`/`CodeJudgeOutput` retirés de l'import `nodes.py`) — vérifié par import (4 fonctions absentes, fonctions actives Coder/Tester/Worker/Reduce/Judge/Synth/Adversary préservées).
- [ ] Critère 149 : La suite pytest complète passe (0 régression, 482 passed = 442 baseline + 40 nouveaux `test_prompts.py`).

## Critères du Fix Tester querySelector (F-45 — 3 axes)
- [ ] Critère 150 : Le skill `web-tester` contient une directive explicite ciblant la racine réelle de la friction `document.querySelector is not a function` — l'assignation `=` au lieu de l'appel `()` (ex `document.querySelector='...'` écrase la fonction native) — avec exemple ❌/✅, garde anti-pollution du contexte (jamais réassigner une méthode native, stocker dans une `const` locale), et replis robustes (`getElementById`/`getElementsByTagName` quand on doute des sélecteurs).
- [ ] Critère 151 : Le skill `web-tester` documente un pattern DOMContentLoaded avant la 1re assertion (attendre un délai puis vérifier qu'un élément attendu existe) — évite les faux échecs sur une page qui initialise son DOM dans un handler `DOMContentLoaded`.
- [ ] Critère 152 : `config.py` expose `tester_max_steps` (env `TESTER_MAX_STEPS`, défaut 12) — borne la durée du Web Tester (le défaut historique 24 laissait boucler ~30 min) ; valeur par défaut dans la dataclass (même convention que `loop_guard_threshold`, ne casse pas les helpers de test) ; `web_tester.py` l'utilise (`max_steps=settings.tester_max_steps`) — testé `test_tester_max_steps_default_and_override`.
- [ ] Critère 153 : `run_with_retry` accepte un paramètre `node_kind` (défaut `"coder"`) qui adapte le message idle au contexte ; `_detect_idle_step(agent, node_kind="tester")` produit un message citant `puppeteer_*`/`final_answer` (PAS `write_file`/`append_file`) — testé `test_detect_idle_step_tester_message_mentions_puppeteer` ; le message Coder est inchangé (rétro-compat) — testé `test_detect_idle_step_coder_message_keeps_write_file`.
- [ ] Critère 154 : Le `WebTestRunner` instancie un `LoopGuard` et le passe à `run_with_retry` (via `loop_guard=guard, node_kind="tester"`) — le Tester bénéficie désormais de la détection anti-idle ET anti-boucle (avant : guard absent, le Tester était le seul nœud d'outils sans protection) — vérifié par lecture du code (`web_tester.py`).
- [ ] Critère 155 : La suite pytest complète passe (0 régression, 487 passed = 482 baseline + 5 nouveaux : test_guard.py +3 + test_config.py +1 + test_guard.py +1).

## Critères du Fix bug visuel Coder + FRESH_START (F-46)
- [ ] Critère 156 : Le skill `frontend-design` commence par une ÉTAPE 0 obligatoire distinguant APP/TOOL (défaut : UI simple empilée centrée dans une card, titre 1.5-2rem) vs LANDING/PAGE (hero autorisé, 2.5-3rem max) — la consigne 'hero 3.5rem' littérale est remplacée par une fourchette conditionnelle.
- [ ] Critère 157 : Garde anti-titre-géant explicite dans le skill — `h1 > 3rem` est interdit sauf hero unique d'une landing page ; directive layout APP : NE PAS faire de row à 1024px, rester une colonne (corrige le bug observé : titre 3.5rem/4rem + layout row cassé).
- [ ] Critère 158 : `load_dotenv()` appelé sans `override=True` (override=False par défaut) — l'env shell prime sur .env ; `FRESH_START=1` au shell est respecté (testé) ; .env reste lu en l'absence d'override shell.
- [ ] Critère 159 : La suite pytest complète passe (0 régression, 487 passed après F-46). Skill non re-validé par run complet ce cycle (la vision Coder livrée plus tard par F-50 + F-90 permettra l'auto-validation visuelle).

## Critères du Fix Judge hang — thinking sélectif (F-47)
- [ ] Critère 160 : `_configure_dspy(settings, model_id, think=False)` utilise le provider litellm `ollama/` (parle `/api/chat` natif Ollama au lieu de `/v1`), retire le suffixe `/v1` de l'api_base, et passe le paramètre `think` au LM — validé : `think=False` répond en 5.8s sans thinking (vs ~23 min avec thinking forcé sur /v1).
- [ ] Critère 161 : Thinking DÉSACTIVÉ (think=False) pour Router/PromptRefiner/Security/Judge/Escalation (verdicts/classification, le thinking était du gaspi bloquant) ; thinking CONSERVÉ (think=True) UNIQUEMENT pour l'Architect (le raisonnement aide au découpage/stratégie) — vérifié par lecture des 6 appels `_configure_dspy` dans dspy_nodes.py.
- [ ] Critère 162 : Diagnostic documenté (debug/GAPS_TESTER_JUDGE.md Gap 2) — le hang ne venait PAS du prompt (mesuré ~4k tokens, déjà tronqué) NI de la température (0.3 correcte pour le code), mais du thinking Gemma 4 forcé sur /v1 (Ollama 0.32.5 = dernière version, pas de MAJ à faire).
- [ ] Critère 163 : La suite pytest complète passe (0 régression, 487 passed ; 2 tests test_prompt_refiner.py mis à jour : assertions `_configure_dspy(..., think=False)`).

## Critères de Chrome DevTools MCP + validation visuelle (F-50, ex-F-45 branche locale)
- [ ] Critère 164 : `build_chrome_devtools_params()` retourne `StdioServerParameters` (command="npx", args contient "chrome-devtools-mcp@latest", "--isolated", "1280x800", "jpeg") si `CHROME_DEVTOOLS_ENABLED=1` ; `None` si `CHROME_DEVTOOLS_ENABLED=0` — testé `TestBuildParams`.
- [ ] Critère 165 : `CHROME_PATH` set → ajoute `--executable-path <path>` aux args ; `CHROME_DEVTOOLS_HEADLESS=1` → ajoute `--headless` ; absent par défaut (visible pour debug) — testé `test_chrome_path_ajoute_executable_path` + `test_headless_*`.
- [ ] Critère 166 : `chrome_devtools_tools()` yield `[]` si params None (désactivé) OU si `ToolCollection.from_mcp` lève (Chrome absent/réseau down) — pas de crash, dégradation gracieuse — testé `TestChromeDevtoolsToolsDegration`.
- [ ] Critère 167 : `chrome_devtools_tools()` yield la liste des outils MCP si connexion OK — testé `TestChromeDevtoolsToolsMocked::test_connexion_ok_yield_outils`.
- [ ] Critère 168 : `wrap_screenshot_tools()` wrap UNIQUEMENT les outils `take_screenshot`/`puppeteer_screenshot` (pas les autres), capture l'image PIL retournée dans le holder, préserve le comportement original (retourne quand même l'image) — testé `TestWrapScreenshotTools` + `TestScreenshotCapture`.
- [ ] Critère 169 : `make_screenshot_callback()` peuple `memory_step.observations_images` avec le dernier screenshot, reset le holder après push, noop si holder vide — testé `TestScreenshotCallback`.
- [ ] Critère 170 : `list_mcp_servers_status()` inclut l'entrée `chrome-devtools` (name + transport="stdio" + configured reflète l'état) — testé `TestMcpStatusDiagnostic`.
- [ ] Critère 171 : `_is_web_task()` détecte le web via `router_lang` OU extensions des `target_files` (.html/.htm/.css/.js) ; false pour Python — testé `TestCoderWebDetection`.
- [ ] Critère 172 : `_build_devtools_blocks()` retourne ("","") si pas d'outils DevTools (backward-compat) ; block preview + doc outils si web+outils dispos ; doc seule si non-web+outils — testé `test_build_devtools_blocks_*`.
- [ ] Critère 173 : Skill `devtools-preview` routé pour les tâches web (regex frontend-design), absent pour Python ; son body se charge depuis `skills/devtools-preview/SKILL.md` — testé `TestSkillRouting`.

## Critères de la Checklist de fonctionnalités + Fixes robustesse GPU-local (F-51, ex-F-46)
- [ ] Critère 174 : `extract_functionalities(spec)` parse la section `## Fonctionnalités attendues` en liste Python (regex), s'arrête à la prochaine section `##`, dédoublonne — testé `TestExtractFunctionalities`.
- [ ] Critère 175 : Robustesse — spec vide/prompt brut/section sans puces → `[]` (fallback historique, pas de crash) — testé `TestExtractRobustesse`. Insensible casse/accents.
- [ ] Critère 176 : `build_checklist_block(funcs)` produit un bloc qui compte les N exigences, numérote 1..N, impose un tableau verdict (PASS/FAIL/N-A), et rappelle « 1 FAIL = failure » — testé `TestBuildChecklistBlock`.
- [ ] Critère 177 : Le WebTester importe et injecte `checklist_block` dans son prompt après le cahier des charges — testé `TestTesterInjection::test_web_tester_importe_le_module`.
- [ ] Critère 178 : Skill `coding` contient la règle anti-TypeScript (tableau syntaxes interdites : `: type`, `as`, `: void`, `interface`) avec exemples — vérifié par lecture skill.
- [ ] Critère 179 : Skill `web-tester` contient la règle des 2 essais (conclure FAILURE vite sur bug réel, ne pas explorer 15 sélecteurs) — vérifié par lecture skill.

## Critères du Re-test ciblé + Git diff (F-52/F-53, ex-F-47/F-48)
- [ ] Critère 180 : `should_use_targeted_retest(iter, refs)` active le mode ciblé si iter>1 ET refs non vides ; sinon mode complet — testé `TestShouldUseTargeted`.
- [ ] Critère 181 : `extract_bug_points(refs)` extrait les réfutations (plus récentes d'abord, tronquées à max_chars) ou None si vide — testé `TestExtractBugPoints`.
- [ ] Critère 182 : `build_targeted_retest_block(bugs, iter, git_diff)` produit un prompt ciblé (max_steps 6, assertions par bug, smoke-test, section diff si fourni) — testé `TestBuildTargetedRetestBlock` + `TestTargetedRetestWithDiff`.
- [ ] Critère 183 : Le web_tester a un max_steps ADAPTATIF (TARGETED_MAX_STEPS=6 ciblé, settings.tester_max_steps complet) et remplace checklist_block par targeted_block en mode ciblé — testé `TestWebTesterIntegration`.
- [ ] Critère 184 : `init_run_git()` crée un .git local (idempotent) ; `commit_iteration()` commit après Coder — testé `TestInitRunGit` + `TestCommitAndGetDiff`.
- [ ] Critère 185 : `get_last_diff()` retourne git diff HEAD~1..HEAD (vides si <2 commits ou git absent) ; tronque au-delà de max_chars avec marqueur — testé `TestDiffTruncation` + `TestRobustesse`.

## Critères du Static Tester déterministe (F-54, ex-F-49)
- [ ] Critère 186 : `extract_inline_js(html)` extrait le JS des `<script>` SANS `src` (ignore les externes) — testé `test_extract_inline_js_*`.
- [ ] Critère 187 : `_check_js_syntax` lance `node --check` et détecte TS-in-vanilla (`: type`, `as Cast`) → SyntaxError — testé `test_ts_annotation_in_script` + `test_ts_as_cast_in_script`.
- [ ] Critère 188 : `_check_event_wiring` flagge un contrôle interactif (button/input/select) avec id mais AUCUN handler (le piège n°1 : slider non branché, indétectable par screenshot) — testé `test_slider_not_wired`.
- [ ] Critère 189 : Tolérances wiring légitimes : onclick inline, `<button type=submit>` en form, `<a href>`, `<input type=hidden>` — testés `test_*_not_flagged`.
- [ ] Critère 190 : Tier 2 DevTools détecte les éléments créés en JS mais INVISIBLES (height=0 = bug CSS height:% sur conteneur sans hauteur), APRES déclenchement de l'action primaire (clic bouton start) — testé `test_invisible_bars_height_percent`.
- [ ] Critère 191 : Dégradation `node` absent → Tier 1a skip silencieux (pas d'échec faux, le LLM Tester prend le relais) — testé `test_node_absent_degrades`.
- [ ] Critère 192 : Dégradation Chrome absent/opt-out → tier_reached="tier1" (Tier 2 skip, Tier 1 reste actif) — testé `test_tier2_skipped_when_devtools_off`.
- [ ] Critère 193 : Non-HTML pass-through (target `.py` → success immédiat, le Static Tester est web-only) — testé `test_non_html_passthrough`.
- [ ] Critère 194 : Opt-out `STATIC_TESTER_ENABLED=0` désactive le nœud (pass-through) ; `STATIC_TESTER_DEVTOOLS=0` désactive le Tier 2 seul — testés `test_opt_out_flag_disables` + `test_devtools_disabled_still_runs_tier1`.
- [ ] Critère 195 : Le workflow intègre le Static Tester entre le Linter et le Tester LLM, avec court-circuit (réfutation DuckDB + continue) sur failure — vérifié par lecture workflows.py.

## Critères des Scripts d'isolation « l'agent joue le nœud » (F-55)
- [ ] Critère 196 : Une méthodologie `MANUAL_<NODE>_METHODOLOGY.md` par nœud dans `debug/isolation/`, calquée sur `debug/MANUAL_TESTER_METHODOLOGY.md` (le pattern Tester original) — décrit ce que l'agent (ZCode) fait quand il joue le nœud à la main, SANS appeler le LLM du graphe — vérifié par lecture des 5 docs.
- [ ] Critère 197 : `MANUAL_ROUTER_METHODOLOGY.md` décrit les étapes fail-fast (mots-clés forts → extensions target_files → résolution multi-techno → décision + justification), un tableau de décision par langage, et 3 biais documentés (défaut paresseux vers javascript, confondre web et javascript, ignorer typescript) — vérifié par lecture.
- [ ] Critère 198 : `MANUAL_ARCHITECT_METHODOLOGY.md` décrit les 3 stratégies F-29 (simple/incremental/multifile) avec tableau de décision techno-driven, les 5 étapes (livrables → stratégie → sections → description → assemblage), et 4 biais (sur-découpage, défaut vers simple, confondre multifile/incremental, ignorer la propagation sections) — vérifié par lecture.
- [ ] Critère 199 : `MANUAL_JUDGE_METHODOLOGY.md` décrit la rubric de sévérité F-44 (critical/high/medium/low avec impact sur is_approved), les 6 étapes (Read in-diff → couverture vs cahier → lecture test_res → lecture security_res → décision → feedback actionnable), et 5 biais (biais approbation, sur-analyse nits, confiance aveugle au Tester, in-diff drift, ignorer security) — vérifié par lecture.
- [ ] Critère 200 : `MANUAL_SECURITY_METHODOLOGY.md` décrit la grille OWASP Top 10 (patterns dangereux par catégorie + severity typique), les 6 étapes (Read → grep OWASP → contexte input externe/contrôlé → CVSS → findings defensive-only → is_secure), et 5 biais (faux positifs constantes, sur-détecter client, décrire attaque au lieu mitigation, ignorer misconfig, oublier secrets en clair) — vérifié par lecture.
- [ ] Critère 201 : `MANUAL_LINTER_METHODOLOGY.md` décrit les langages couverts (détection par extension), les 5 étapes (détection → tree-sitter sauf HTML → py_compile Python → vérifs structurelles HTML → verdict agrégé), l'exception HTML (tree-sitter-html trop tolérant → sauté), et 3 biais/limites — vérifié par lecture.
- [ ] Critère 202 : `run_linter.py` (script d'isolation du Linter, seul nœud déterministe → assertion binaire possible) injecte des fichiers buggés connus (Python IndentationError/py_compile, TS-in-vanilla/tree-sitter, contenu après </html>/structure) → `status=="failure"` ; fichiers corrects → `status=="success"` ; multi-fichiers (1 buggé parmi 2 → failure global) — VALIDÉ en exécution (7/7 scénarios ✅ en ms).
- [ ] Critère 203 : `debug/isolation/README.md` documente la convention (une méthodologie par nœud, l'agent joue le nœud à la main), un tableau des docs + contrat entrée/sortie exhaustif par nœud (clés dict task lues + retour Pydantic), et pointe vers Tester (MANUAL_TESTER_METHODOLOGY.md), Coder (audit_coder/), Static Tester (validate_static_tester_live.py) — vérifié par lecture.
- [ ] Critère 204 : Aucun code de production modifié (seulement de nouveaux fichiers dans `debug/isolation/` + mises à jour des fichiers d'état) → 0 régression garantie, vérifiée par suite pytest (586 passed / 0 failed).

## Critères du Durcissement des prompts de nœuds (F-56, P14)
- [ ] Critère 206 : `RouterSignature` (dspy_nodes.py) contient un tableau de MOTS-CLÉS CANONIQUES par langage (python/.py/pandas→python ; react/.tsx/typescript→typescript ; .html/landing→html ; vanilla js→javascript ; rust/cargo→rust ; go→go), une RÈGLE DE PRIORITÉ (extensions priment sur mots-clés), et une section ANTI-BIAIS (3 biais : javascript par défaut, html≠js, ignorer TS) — vérifié par grep des 3 marqueurs.
- [ ] Critère 207 : `ArchitectSignature` contient les RÈGLES POUR 'incremental' (squelette structural en 1ère section + fourchette 3-7 sections ~50-100 lignes) et un avertissement BIAIS 'incremental' vs 'multifile' (jamais incremental sur multifichier Python/TS) — vérifié par grep.
- [ ] Critère 208 : `SecuritySignature` contient PATTERNS DANGEREUX À CHERCHER ACTIVEMENT (tableau OWASP concret : innerHTML/document.write/eval/os.system/subprocess shell=True/pickle.loads/md5/verify=False/CORS*/debug=True), DISCRIMINATION INPUT (externe vs contrôlé), A09 Logging, ATTENTION FAUX POSITIFS — vérifié par grep.
- [ ] Critère 209 : `CodeJudgeSignature` contient PROCÉDURE OBLIGATOIRE (5 étapes numérotées : liste exigences→vérifie présence/implémentée→croise test_results→applique security→décide), la règle de croisement défiant (test PASS mais exigence absente = critical), et LOCALISATION OBLIGATOIRE (ligne/fragment exact) — vérifié par grep.
- [ ] Critère 210 : P14-E Linter — `lint_file` sur fichier absent retourne `is_valid=True` (défense conservée) MAIS avec un avertissement dans `errors` (non vide) ; `execute_linter_node` remonte les warnings dans une section "AVERTISSEMENTS (non bloquants)" de `details` SANS changer le statut (reste success) — testé `test_missing_file_is_valid` (mis à jour) + `test_missing_file_warning_in_details` (nouveau).
- [ ] Critère 211 : Les marqueurs des docstrings sont PRÉSERVÉS (INVARIANTS UNIVERSELS pour les 4 ; strategy/incremental/multifile pour Architect ; OWASP/CVSS/critical pour Security ; critical/IN-DIFF ONLY/ANTI-NITS pour Judge) → `test_prompts.py` (classe TestDSPySignaturesHaveInvariants) passe — vérifié par pytest 40 passed sur test_prompts.py + grep marqueurs 3/3 par nœud.
- [ ] Critère 212 : La suite pytest complète passe (0 régression) — 587 passed / 0 failed (586 baseline + 1 nouveau test Linter warning). `run_linter.py` 7/7 scénarios ✅.
- [ ] Critère 213 : Correspondance 1:1 entre les recommandations de `COMPARISON_AUDIT.md` (P14-A..D) et l'implémentation — vérifié par grep des marqueurs (3/3 pour chaque nœud A-D).
- [ ] Critère 214 : Aucune modification de `prompts.py` (rôles/invariants F-44 inchangés), `models.py`, `nodes.py`, `skills_loader.py` — le durcissement est purement additif aux docstrings métier (2e arg de `with_invariants`) + linter.py warning.

## Critères du Read-Before-Write Gate (Priorité 1 — F-67, Deer Flow #3857)
- [ ] Critère 215 : `compute_content_hash(content)` produit un hash SHA256 stable (même contenu → même hash, deux contenus différents → deux hash différents) — testé `test_compute_content_hash_stable` + `test_compute_content_hash_differs`.
- [ ] Critère 216 : `_normalize_path(path)` résout les chemins équivalents en une seule clé absolue — `a/b/../c.txt` == `a/c.txt`, mixed separators (`/` et `\\`) sur win32, résultat absolu — testé `test_normalize_path_*` (3 cas).
- [ ] Critère 217 : `ReadGate.check_write(path)` applique la règle ALLOW/BLOCK — fichier ABSENT → ALLOW (création) ; fichier existant JAMAIS lu → BLOCK ; fichier lu (hash mark == hash disque) → ALLOW ; read stale (fichier modifié depuis le read) → BLOCK — testé `TestReadGateLogic` (4 cas).
- [ ] Critère 218 : Fail-open garanti — `check_write` ne lève JAMAIS et ne bloque jamais sur une inspection impossible : path None/vide/non-string → ALLOW ; fichier illisible (binaire `\xff\xfe` → UnicodeDecodeError) → ALLOW ; `record_read`/`record_write` ne lèvent pas sur bad input — testé `TestReadGateFailOpen` (6 cas).
- [ ] Critère 219 : Mode STRICT (fidèle Deer Flow) — `record_write(path)` supprime la mark de ce path → un write RÉUSSI invalide la lecture, toute édition suivante BLOQUÉE jusqu'à un nouveau `read_file` (corrige le bug « édition à partir d'une représentation mentale stale ») ; un `record_write` sur un path sans mark est un no-op idempotent — testé `TestStrictMode` (3 cas).
- [ ] Critère 220 : « Newest mark wins » — deux reads successifs, le DERNIER gagne (hash le plus récent) — testé `test_newest_mark_wins`. Thread-safety — 20 threads × 50 stamps parallèles ne corrompent pas le dict interne — testé `test_concurrent_record_read_is_safe`.
- [ ] Critère 221 : `_GatedWriteTool` (proxy template SanitizedTool) — si `check_write` bloque, l'outil sous-jacent n'est JAMAIS appelé (vérifié par fichier disque INCHANGÉ), **lève `RuntimeError`** (message pédagogique citant `read_file` — comportement prod : smolagents remonte l'erreur d'outil au LLM, F-67) ; si ALLOW, délègue puis appelle `record_write` (Strict) ; copie name/description/inputs/output_type ; `__getattr__` délègue `to_code_prompt` ; chemin `forward` aussi gated — testé `TestGatedWriteTool` (`pytest.raises(RuntimeError, match="read_file")` + fichier inchangé).
- [ ] Critère 222 : `_ReadTrackingTool` — après délégation à `read_file`, stamp le hash du contenu COMPLET du fichier (re-lit le disque, peu importe l'offset/limit demandé = vision Deer Flow) ; skip le stamp si fichier absent ; retourne bien le résultat de read_file — testé `TestReadTrackingTool` (3 cas).
- [ ] Critère 223 : `wrap_tools_with_read_gate(tools, gate, enabled)` — `enabled=False` → no-op (même objet liste) ; `enabled=True` → wrap UNIQUEMENT `read_file` (+ les 4 outils d'écriture write_file/search_replace/edit_file/multi_replace ; `append_file` est VOLONTAIREMENT EXEMPTÉ — ajout-only, protégé par anti-doublon F-28 + idempotence F-43, le gater casserait la stratégie incremental F-28), laisse intacts les autres (list_directory, web_search/DuckDuckGo, MCP) ; préserve l'ordre — testé `TestWrapTools` (3 cas).
- [ ] Critère 224 : E2E scénarios Coder simulé — write sur fichier existant SANS read → BLOCK (fichier disque inchangé) ; read puis write → ALLOW (fichier modifié) ; write puis edit sans re-read → BLOCK (Strict, le cas #3857) ; message pédagogique cite le path ET `read_file` — testé `TestEndToEnd` (4 cas).
- [ ] Critère 225 : Branchement dans `execute_coder_node` (nodes.py) — `wrap_tools_with_read_gate` inséré ENTRE `wrap_screenshot_tools` et `sanitize_tools` (ordre : gate AVANT sanitizer → voit kwargs déjà typés), gated par `settings.read_before_write_enabled` — vérifié par lecture du code.
- [ ] Critère 226 : Config — `read_before_write_enabled: bool = True` dans la dataclass Settings + `load_settings` via `_get_bool("READ_BEFORE_WRITE_ENABLED", True)` + `.env.example` + `.env` — opt-out total `READ_BEFORE_WRITE_ENABLED=false` — testé via `load_settings()` (défaut True, env false → False).
- [ ] Critère 227 : La suite pytest complète passe (0 régression) — 651 passed / 0 failed (616 baseline + 35 nouveaux `test_read_gate.py`), 11 deselected (test_web_tester_functional = Chrome/npx live, hors périmètre).

## Critères F-57 — Skills à la demande / Coder lazy loading (Priorité 10, Phase 1)

- [ ] Critère 228 : `EAGER_SKILLS_CODER` est un `set` contenant exactement `{"file-creation", "coding", "context7-research"}` (3 skills critiques dont l'oubli provoque un failure mode fatal) — tous existent sur disque (`load_skill_body` non vide) — testé `TestEagerSkillsCoder`.
- [ ] Critère 229 : `_parse_frontmatter_yaml(text)` ne lève JAMAIS — YAML valide → dict (name, description) ; sans frontmatter → `{}` ; malformé → dict vide ou repli regex ; incomplet (split < 3) → `{}` — testé `TestParseFrontmatterYaml` (4 cas).
- [ ] Critère 230 : `parse_skill_meta(name)` lit UNIQUEMENT le frontmatter (pas le corps) — skill existant → `(name, description)` tuple de 2 ; absent → `None` — testé `TestParseSkillMeta` (4 cas, incluant tous les skills applicables ont un meta).
- [ ] Critère 231 : `build_skills_catalog(task_content)` — retourne metadata (name+description ~100 tokens/skill) de TOUS les skills applicables (EAGER + LAZY) ; marque chaque skill `(EAGER)` ou `(lazy)` ; contient la directive `load_skill` ; ne contient PAS le corps complet des skills LAZY ; NETTEMENT plus court que `build_eager_skills_block` (< 25% de sa taille) — testé `TestBuildSkillsCatalog` (6 cas).
- [ ] Critère 232 : `build_eager_skills_block(task_content)` — contient le corps complet des 3 skills EAGER (`### SKILL: file-creation`, `coding`, `context7-research`) ; ne contient PAS `frontend-design` ni `devtools-preview` (LAZY) ; contenu identique à l'ancien `build_skills_block` pour les EAGER — testé `TestBuildEagerSkillsBlock` (5 cas).
- [ ] Critère 233 : `@tool load_skill(skill_name)` (nouveau module `skill_loader_tool.py`) — retourne le corps complet du skill demandé ; skill inconnu → message d'erreur (ne lève jamais, fail-open) ; est un tool smolagents valide (expose `name`/`description`/`inputs`) — testé `TestLoadSkillTool` (4 cas, incluant noms bizarres/injection).
- [ ] Critère 234 : Non-régression — `select_skills_for_coder`, `build_skills_block` (déprécié mais conservé) et `load_skill_body` restent inchangés (tests existants `test_chrome_devtools_tool.py::TestSkillRouting`, `test_context7_tool.py` passent) — testé `TestNonRegression` (4 cas).
- [ ] Critère 235 : Config opt-out — `skill_lazy_loading_enabled: bool = True` dans la dataclass Settings + `load_settings` via `_get_bool("SKILL_LAZY_LOADING_ENABLED", True)` + `.env.example` + `.env` local — `false` → `execute_coder_node` fallback `build_skills_block` (eager complet, rétrocompat) ; `true` (défaut) → `build_eager_skills_block` + `build_skills_catalog` + outil `load_skill` ajouté à `coder_tools`.
- [ ] Critère 236 : La suite pytest complète passe (0 régression) — 678 passed / 0 failed hors 3 pré-existants `test_run_logging.py` (format log évolué, non liés à F-57, confirmés par `git stash`), 11 deselected (test_web_tester_functional = Chrome/npx live). 30 nouveaux tests `test_skill_lazy_loading.py`.
- [ ] Critère 237 : Gain mesuré (smoke test) — sur tâche web type, `len(build_eager_skills_block) + len(build_skills_catalog)` < `len(build_skills_block)` (ancien eager complet), réduction **-36.8%** du system prompt par step (17386 → 10988 chars).

### F-70 — Diff multi-fichiers pour le Judge + métriques de benchmark
- [x] Critère 238 : `graph_orchestrator/judge_diff.py` expose `build_judge_code_block(target_files, git_diff, *, full_max_chars=6000) -> str` (0 LLM, importe `truncate_output` de `feedback_utils`) — testé `TestIteration1FullFile` (3 cas : diff vide = full-file `--- {path} ---`, whitespace = iter1, target_files vide = "Code manquant").
- [x] Critère 239 : Comportement différencié iter 1 vs iter >1 — iter 1 (diff vide/whitespace) retourne le full-file concaténé SANS bloc diff (rétrocompat stricte, `test_diff_vide_renvoie_full_file`, `test_diff_whitespace_seul_traite_comme_vide`) ; iter >1 (diff présent) retourne bloc `=== DIFF MODIFIÉ (à juger EN PRIORITÉ — doctrine IN-DIFF ONLY) ===` + ```diff``` + `=== CODE COMPLET (contexte pour la vérification des exigences) ===` + full-file tronqué (`test_diff_present_injecte_bloc_diff_en_tete`, `test_ordre_diff_puis_full_file`).
- [x] Critère 240 : Tolérance fail-open — fichier cible absent/illisible → silencieusement sauté, le diff passe quand même (`test_fichier_absent_silencieux_fail_open`) ; multi-fichiers concaténés avec séparateurs `---` (`test_multi_fichiers_concatenes_dans_full_file`).
- [x] Critère 241 : Branchement `execute_code_judge_node` (`dspy_nodes.py`) — l'ancienne boucle de lecture full-file (l.799-805) remplacée par `build_judge_code_block(subtask.get("target_files", []), subtask.get("git_diff", ""))` ; import `from .judge_diff import build_judge_code_block` ; le kwarg `code` du `_run_dspy_node` reçoit le bloc — testé `test_execute_code_judge_node_injects_git_diff_block` (mock ChainOfThought, assert `code` contient `DIFF MODIFIÉ` + diff + `CODE COMPLET` + `IN-DIFF ONLY`) + `test_execute_code_judge_node_iter1_full_file_no_diff_block` (iter 1 = full-file, pas de bloc diff).
- [x] Critère 242 : `graph_orchestrator/judge_metrics.py` expose `canonicalize_finding(finding) -> str` (ID `"{location}|{category}|{severity}"` normalisé lowercase/whitespace, gère `Finding` Pydantic + dict + string, description EXCLUE = insensible à la paraphrase) — testé `TestCanonicalize` (6 cas).
- [x] Critère 243 : `compute_precision_recall(predicted, actual) -> {"precision","recall","f1"}` (set-match par ID canonique, empty/empty → 1.0, arrondi 4 dp) + `compute_mrr(correct, ranked) -> float` (1/rank 1-indexé, 0 si absent) + `judge_verdict_accuracy(predicted_approved, actual_should_approve) -> {"accuracy","false_positive","false_negative"}` — testés `TestPrecisionRecall` (6), `TestMRR` (4), `TestVerdictAccuracy` (4). Ports typés de `references/code-review-graph/code_review_graph/eval/scorer.py`.
- [x] Critère 244 : Non-régression — 0 régression confirmée par `git stash` (25 échecs préexistants strictement identiques avant/après F-70, tous E2E workflow + guard + read_gate + skill_lazy_loading, AUCUN lié au Judge). 32 nouveaux tests PASS (test_judge_diff 12 + test_judge_metrics 18 + test_dspy_nodes +2 Judge). Périmètre tenu : 0 modif workflows.py/git_snapshot.py/prompts.py ; (b) format structuré DÉJÀ COUVERT par Finding F-44 (template code-review-graph écarté = redondant) ; compute_risk_score écarté (P4 HORS-SCOPE) ; build_pr_diff_files écarté (fetcher GitHub UI).

### F-61 (itération) — Post-mortem run coding_d72dc8e36445c4b6 : 3 correctifs Coder
- [x] Critère 245 : Guard anti-`}` fermant (Correctif 1) — `run_with_retry` except Exception : détection INDEPENDANTE de la condition Syntax/parse (la branche `}` est testée en PREMIER car le message smolagents "Code parsing failed" ne contient pas "Syntax"/"parse" mais "parsing"). Condition : `is_code_agent and "}" in msg and ("parenthesis"|"match"|"closing"|"parsing" in msg.lower())`. Message SPÉCIFIQUE (Règle n°8 + exemple correct `search_replace(...)` fermé par `)`). Testé `test_brace_closing_error_emits_rule_8_message` (assert message R8 + exemple + pas de message générique) + `test_generic_syntax_error_keeps_decoupe_message` (assert message générique conservé pour erreurs sans `}`).
- [x] Critère 246 : CODER_MAX_STEPS configurable (Correctif 2) — config `coder_max_steps: int = 18` (avant 25 hardcoded) + `_get_int("CODER_MAX_STEPS", 18)` + `.env.example` + nodes.py `max_steps=settings.coder_max_steps`. Testé `TestCoderMaxStepsConfig` (défaut 18 + override env 25 + idle_breaker défaut 3).
- [x] Critère 247 : Circuit-breaker idle consécutifs (Correctif 3) — `run_with_retry` param `idle_breaker_threshold: int = 3`, compteur `consecutive_idle` incrémenté si `_detect_idle_step` non-None, reset si productif, break+return None si `>= threshold`. Config `idle_breaker_threshold` (défaut 3) + `.env.example`. Nœuds non-Coder (Worker/Judge/Synth/Adversary) reçoivent `idle_breaker_threshold=10**9` (jamais coupés — préservation comportement). Testé `test_idle_breaker_cuts_after_threshold_consecutive_idles` (coupe au 3e idle, pas au 5e retry) + `test_idle_breaker_resets_on_productive_run` (reset sur productif) + `test_idle_breaker_disabled_for_non_coder_nodes` (seuil élevé = pas de coupure).
- [x] Critère 248 : Non-régression — suite pytest 673 passed (+8 vs baseline 665) / 25 échecs préexistants strictement identiques (confirmés identiques à la baseline F-70), 0 régression. 8 nouveaux tests `test_coder_hardening.py`. py_compile OK (nodes.py + config.py).
- [x] Critère 249 : Périmètre tenu — pas de retry/backoff litellm pour Connection error (scope infra llama-server F-58, cycle dédié si récurrent) ; pas de modif worker_max_retries=3 (correctement dimensionné) ; F-70 non touché (validé, non impliqué dans l'échec).
- [x] Critère 250 : Découvertes de mock documentées — `type(agent).__name__` nécessite une vraie sous-classe dynamique (`type(name, (MagicMock,), {})`) car `MagicMock.__class__ = ...` et `spec=` ne changent PAS `type()` (seulement `__class__`) ; `agent.memory.steps` est purgé en fin d'attempt par run_with_retry → repeupler dans le mock pour simuler N idles consécutifs (sinon `_detect_idle_step` voit `[]` → None → branche non-idle → breaker ne déclenche jamais).

### F-61 (itération 3) — Post-mortem run coding_d72dc8e36445c4b6 : final_answer valide éjecté par LoopGuard
- [x] Critère 251 : Diagnostic — run E2E Bubble Sort multi-fichier du 2026-08-06 20:16, verdict graphe `failure` (Coder crash). Le Coder a atteint `max_steps=18` en itérant légitimement (write_file/search_replace rejoués après relecture — invariant read-before-write F-67). Il a émis un `final_answer({"status":"success",...})` **parfaitement valide** (matche CoderOutput) → MAIS verdict failure. Cause racine : bug logiciel `run_with_retry` lignes 296-297 `if loop_msg: pass` — le LoopGuard (F-36) scanne tout l'historique et comptabilise comme "répétition" l'itération de correction légitime, puis le bloc `if validated:` jette silencieusement le résultat (`pass` au lieu de `return`). Les 3 retries (`worker_max_retries=3`) reproduisent le même scénario → échec définitif. Les 3 fichiers générés étaient néanmoins fonctionnels (index.html 24l, script.js 172l, styles.css 164l).
- [x] Critère 252 : Correctif — `run_with_retry` bloc `if validated:` inversé : un `validated` réussi prime TOUJOURS sur `loop_msg`. Le `if loop_msg: pass` supprimé ; `loop_msg` reste ajouté au prompt de retry (ligne 279) pour guider le retry UNIQUEMENT quand `validated is None`. Message d'observabilité `[i] LoopGuard a signalé une répétition, mais le final_answer est valide → succès conservé (priorité au résultat sur le guard F-36).` émis pour tracer le chemin. Leçon : le LoopGuard (F-36) était conçu pour court-circuiter une vraie boucle, mais il ne doit jamais annuler un `final_answer` valide — si l'agent est arrivé au résultat, la "boucle" était soit terminée, soit une itération de correction légitime.
- [x] Critère 253 : Test de non-régression — `test_valid_final_answer_wins_over_loop_guard` (test_coder_hardening.py) : agent dont l'historique contient 3× le même `write_file` (déclenche `loop_guard.repeated_action()` non-None) + `final_answer` valide (CoderOutput success) → assert `result is valid_output` (pas None), `call_count == 1` (succès au 1er attempt, 0 retry consommé), `guard.repeated_action() is not None` (confirme qu'on teste bien le chemin LoopGuard). 9 tests test_coder_hardening.py PASS.
- [x] Critère 254 : Non-régression — suite pytest 674 passed (+1 vs baseline 673) / 26 échecs préexistants strictement identiques (confirmés par `git stash` : 26 failed avant ET après mon fix, tous E2E workflow + guard timeout + read_gate + skill_lazy_loading + prompt_refiner + escalation + feedback + output_dir, AUCUN lié au LoopGuard ou au bloc `if validated:`). py_compile OK (nodes.py + test_coder_hardening.py). 0 régression.

### F-65 — Pépites prompts différenciantes (scope A : tier prompts 5/5 + 3 quick wins)
- [x] Critère 255 : Invariant n°5 enrichi — `UNIVERSAL_INVARIANTS` contient "RÉVERSIBILITÉ" + "BLAST-RADIUS" + "PAR-ACTION" (grille de décision Codex 4-tier + Claude Code 3-tier, fiche 29). Plus "APPROVAL GATING PAR RISQUE" (renommage du titre).
- [x] Critère 256 : Invariant n°12 ajouté — `UNIVERSAL_INVARIANTS` contient "SELF-CORRECTION VÉRIFIABLE" + "promesse" (Claude Code « don't end with a promise » fiche 29). Les 12 patterns sont numérotés 1 à 12 (`test_all_patterns_present`).
- [x] Critère 257 : Role `router` enrichi — contient "CIBLES D'ÉCRITURE" + "CONTRAT PARTAGÉ" (write-lock parallel policy, Amp `amp-code.md` + Codex `codex-full.md` fiche 29).
- [x] Critère 258 : Role `architect` enrichi — contient "EARS" (format EARS, Kiro fiche 17).
- [x] Critère 259 : Roles `coder` + `coder_frontend` enrichis — contiennent "ENGINEERING MINDSET" + "CAS LIMITES" (VSCode gpt-5 fiche 17).
- [x] Critère 260 : Role `web_tester` enrichi — contient "REQUIREMENTS COVERAGE" + "DELTAS" (quality gates triage, VSCode gpt-5 fiche 17 — absent fiche 29).
- [x] Critère 261 : Role `judge` enrichi — contient "SELF-CORRECTION VÉRIFIABLE" (verdict sur vérifications effectives) + "file:start-end" (citation canonique, Devin+Cursor fiche 29).
- [x] Critère 262 : Role `security` enrichi — contient "RÉVERSIBILITÉ" (classification par réversibilité) + "{{secret_name}}" (canary, Warp fiche 29).
- [x] Critère 263 : Aucune modification de schéma — `models.py` inchangé (vérifié `git diff main...HEAD -- graph_orchestrator/models.py` = vide). `Finding.location` (F-44) réutilisé tel quel, seul le format attendu est standardisé en prompt.
- [x] Critère 264 : Aucune régression — suite pytest `tests/ --ignore=tests/test_static_tester.py` → 711 passed / 12 failed, **les 12 échecs sont strictement identiques à `main`** (comparatif `git stash` + checkout main : mêmes 12 failed). `test_prompts.py` seul → 52 passed / 0 failed.
- [x] Critère 265 : Attributions vérifiées — `tools_used=>update_emitted` attribué à Cursor fiche 17 (PAS fiche 29) ; `<cite>` balise attribuée à DeepWiki fiche 17 (n'existe pas en fiche 29) ; quality gates triage attribué à VSCode gpt-5 fiche 17 (absent fiche 29). Corrections confirmées par grep indépendant sur les deux corpus. Documentation dans `docs/NODES_AND_SKILLS.md` section F-65.

## Critères du Stall Detector — Anti-loop v2 (Priorité 3-bis — F-88, loopx)
- [ ] Critère 266 : L'enum `DeliveryOutcome` expose 3 valeurs canoniques (`accountable`/`progress`/`idle`) et `ACCOUNTABLE_OUTCOMES` est un frozenset contenant uniquement `ACCOUNTABLE` — testé `test_delivery_outcome_values` + `test_accountable_outcomes_contains_only_accountable`.
- [ ] Critère 267 : `classify_turn(tool_calls)` classifie correctement — liste avec un write = `ACCOUNTABLE` ; liste read/bash seulement = `PROGRESS` ; liste mixte (read + write) = `ACCOUNTABLE` (le write prime) ; liste vide/None = `IDLE` — testé `test_classify_turn_*` (4 cas).
- [ ] Critère 268 : `compute_material_fingerprint(tool_name, arguments)` produit un hash SHA256 stable de 16 hex chars pour les outils d'écriture (write_file/append_file/edit_file/search_replace/multi_replace) basé sur le CONTENU (content, ou old+new), et `""` pour les outils non-écriture (read/bash/screenshot) — testé `test_material_fingerprint_*` (6 cas dont stable, differs_on_content, empty_for_non_write, search_replace, multi_replace, string_arguments JSON, codeagent source line).
- [ ] Critère 269 : `StallDetector(threshold)` lève `ValueError` si `threshold < 1` (hard floor, miroir loopx `safe_threshold = max(1, int(threshold))`) — testé `test_stall_detector_rejects_threshold_below_1`.
- [ ] Critère 270 : `StallDetector.record(outcome, material_hash)` implémente la table de vérité — ACCOUNTABLE + hash *différent* du précédent → reset ; ACCOUNTABLE + hash *identique* → incrément (reproduction = stall, le cas F-36 rate) ; PROGRESS/IDLE → incrément — testé `test_stall_detector_progress_turns_trigger_stall` + `test_stall_detector_idle_turns_trigger_stall` + `test_stall_detector_new_material_resets` + `test_stall_detector_reproduction_triggers_stall`.
- [ ] Critère 271 : `StallDetector.is_stalled()`/`signal()` retournent True/message pédagogique (`CIRCUIT BREAKER` dans le message) au-delà du seuil, False/None sinon ; `signal()` est distinct du message du LoopGuard (cible la reproduction/absence de progression, pas la répétition de tool call) — testé `test_stall_detector_no_stall_initially` + `test_stall_detector_progress_turns_trigger_stall`.
- [ ] Critère 272 : `StallDetector(threshold=2, enabled=False)` est no-op — `record` n'incrémente rien même après 10 appels, `is_stalled()`/`signal()` retournent False/None — testé `test_stall_detector_disabled_never_triggers`. Threshold configurable (`threshold=3` → stall à 3) — testé `test_stall_detector_threshold_configurable`.
- [ ] Critère 273 : `StallDetector.reset()` remet le compteur ET `_last_material_hash` à zéro (après reset, un write identique à avant n'est PAS une reproduction) — testé `test_stall_detector_reset_clears_state`.
- [ ] Critère 274 : Thread-safety — 20 threads × 50 `record` concurrent → pas d'exception, état final cohérent — testé `test_stall_detector_thread_safe_concurrent_records`.
- [ ] Critère 275 : Branchement dans `run_with_retry` (nodes.py) — nouveau param `stall_detector: Optional[StallDetector] = None` (non-cassant) ; pour chaque step de `agent.memory.steps`, `classify_turn` + `compute_material_fingerprint` + `stall_detector.record` ; si `is_stalled()` ET `validated is None` → message au prompt ; `reset()` aligné sur `loop_guard.reset()` (après `agent.memory.steps = []`) — testé `test_run_with_retry_emits_stall_signal` (intégration, 2 steps PROGRESS dans un run → log stall).
- [ ] Critère 276 : Priorité `validated` conservée — un `final_answer` valide (CoderOutput success) prime sur le signal stall même si `is_stalled()` est True (succès retourné au 1er attempt, 0 retry) — testé `test_valid_final_answer_wins_over_stall_signal` (miroir de `test_valid_final_answer_wins_over_loop_guard`). Pas de stall_detector passé → no-op — testé `test_run_with_retry_noop_without_stall_detector`.
- [ ] Critère 277 : Non-régression F-36 — le contrat `test_run_with_retry_triggers_loop_breaker` (test_loop_guard.py) reste vert après chargement du module stall_detector (modules orthogonaux, LoopGuard intact) — testé `test_loop_guard_test_still_passes_with_stall_detector_module_loaded`. Config — `stall_detector_enabled: bool = True` + `stall_detector_threshold: int = 2` dans la dataclass Settings + `load_settings` via `_get_bool("STALL_DETECTOR_ENABLED", True)` / `_get_int("STALL_DETECTOR_THRESHOLD", 2)` + `.env.example` + `.env` — opt-out `STALL_DETECTOR_ENABLED=false`.
- [ ] Critère 278 : La suite pytest complète passe — 714 passed / 12 failed (les 12 échecs sont PRÉ-EXISTANTS confirmés via `git stash` : architect/escalation/guard-timeout/prompt_refiner(3)/read_gate(4)/skill_lazy_loading(2), AUCUN lié au stall detector), 11 deselected (`test_web_tester_functional`, nécessite Chrome/npx live), 1 error collection (`test_static_tester`, pré-existant sur main). 0 régression (26 nouveaux tests PASS, 0 échec lié à F-88).
- [ ] Critère 279 (HOTFIX post-run E2E 2026-08-08) : `compute_material_fingerprint` extrait le contenu d'un write_file CodeAgent — `extract_tool_calls_from_step` (loop_guard.py) retourne args = ligne source Python (pas un dict) pour un CodeAgent. `_extract_str` gère ce 3e cas via regex (valeur après `key=`, guillemets doubles/simples, DOTALL). Avant le fix : tous les writes CodeAgent → hash SHA256("") identique → le stall detector ne resetait jamais → faux stall à 17 turns sur du debug légitime → verdict failure en run E2E. Après le fix : deux writes au contenu différent → hashs différents → reset correct — testé `test_material_fingerprint_codeagent_source_line_differs_on_content` + `test_stall_detector_resets_on_codeagent_write_with_new_content` (end-to-end sur la vraie chaîne `extract_tool_calls_from_step` → `classify_turn` → `record`). 28 tests PASS (26 + 2 nouveaux). Validation run Coder isolé multi-fichier + draft (F-88 activé) : 0 stall sur 8 steps avec 43 tool calls d'écriture.

## Critères des Jeux de tests par nœud — Isolation LLM (Priorité 17 — F-89, MAX)
- [x] Critère 280 : 6 scripts d'isolation créés dans `debug/` — `run_router.py`, `run_prompt_refiner.py`, `run_architect.py`, `run_drafter.py`, `run_security.py`, `run_judge.py`. Chacun compile (`uv run python -m py_compile` OK sur les 6).
- [x] Critère 281 : Chaque script appelle la VRAIE fonction de production (0 mock, 0 duplication) — `execute_router_node` / `execute_prompt_refiner_node` / `execute_architect_node` / `execute_drafter_node` / `execute_security_reviewer_node` / `execute_code_judge_node` importées depuis `graph_orchestrator.dspy_nodes`. Le param `*_model` est passé à `None` (ignoré en prod, utilisé pour logging uniquement — le vrai modèle vient de `_run_dspy_node` → `model_lifecycle(spec)`).
- [x] Critère 282 : `run_router.py` — jeu de 5 prompts figés (Python mots-clés web piège / React JSX / HTML-CSS-JS vanilla / Rust / ambigu). Valide le bug F-56a (débordement vers javascript). Affiche language détecté vs attendu.
- [x] Critère 283 : `run_prompt_refiner.py` — 3 prompts figés (termes vagues / spec déjà structurée / minimaliste). Affiche ambiguities_detected + refined_prompt structuré.
- [x] Critère 284 : `run_architect.py` — spec Bubble Sort figée. Affiche ArchitectOutput (plan_id, global_architecture, sous-tâches avec task_id/description/target_files/stratégie/sections/skills).
- [x] Critère 285 : `run_drafter.py` — sub_dict Bubble Sort figé (id/content/strategy/target_files configurables). Sauvegarde le draft_markdown dans `debug/drafter_isolation_out/draft_isolation.md` pour réinjection dans `run_coder.py --draft`.
- [x] Critère 286 : `run_security.py` — 4 codes figés (propre/XSS-innerHTML/eval-URL/pickle.loads). Écrit les fixtures dans tempfile (le nœud lit target_files depuis le disque). Affiche is_secure + vulnerabilities + findings (severity/category/location).
- [x] Critère 287 : `run_judge.py` — 4 scénarios figés (correct+tests PASS / bug logique+tests FAIL / nit style+tests PASS / fail-closed security=None). Construit SecurityOutput Pydantic + test_res dict. Affiche is_approved + final_feedback + findings. Le scénario fail-closed valide le hard block SANS appel LLM (post-mortem run 123955).
- [x] Critère 288 : Chaque script accepte un input unique en CLI (`uv run python debug/run_X.py "<input>"` ou `@fichier`) EN PLUS du jeu de fixtures par défaut — permet de tester un cas ad hoc sans modifier le script.
- [x] Critère 289 : Chaque script affiche le contrat (modèle, backend, endpoint, timeout) + métriques (durée) — permet de comparer la vélocité d'un nœud isolé vs run E2E complet.
- [x] Critère 290 : Convention golden files documentée dans `debug/fixtures/golden/README.md` — nœuds déterministes (Linter, Static Tester) = golden possible (sortie stable) ; nœuds LLM = non-golden (entrées figées mais sorties non-déterministes, comparaison visuelle/heuristique).
- [x] Critère 291 : `debug/isolation/README.md` mis à jour — 2 familles documentées (méthodologies manuelles F-55 + scripts d'isolation LLM F-89) avec tableau des 8 scripts (Router/PromptRefiner/Architect/Drafter/Security/Judge/Coder/WebTester).
- [x] Critère 292 : Dossier `debug/` retiré du `.gitignore` — 37 fichiers d'outillage (scripts .py + méthodologies .md + fixtures) versionnés (F-55 croyait l'avoir fait, ne l'était pas en réalité) ; artefacts régénérables (coder_isolation_out, drafter_isolation_out, run dirs, __pycache__) restent ignorés via règles précises.
- [x] Critère 293 : Aucune régression — suite pytest `tests/ --ignore=tests/test_static_tester.py` → 729 passed / 12 failed (les 12 échecs sont PRÉ-EXISTANTS confirmés : architect/escalation/guard-timeout/prompt_refiner(3)/read_gate(4)/skill_lazy_loading(2), AUCUN lié à F-89 qui ne modifie aucun code de production). 11 deselected (`test_web_tester_functional`, nécessite Chrome/npx live). 0 code de production modifié (F-89 = outillage debug uniquement).
- [x] Critère 294 (F-68 Phase 1) : Modèles `ConsolidationAction` (kind update/delete/add, index 1-based, text) + `ConsolidationOutput` (entity_id, actions, summary) dans `models.py`. Convention additive (défauts []) — non-cassant pour les tests/helpers existants qui construisent `*Output()` sans ces champs.
- [x] Critère 295 : Méthodes KG `get_claims_by_run(run_id)` (JOIN claim+provenance sur run_id) + `get_entities_by_run(run_id)` (entités distinctes) — filtrent les claims par run pour la consolidation par entité (fidèle qm createKeyedQueue<ScopeId>).
- [x] Critère 296 : Méthode KG `update_claim_content(claim_id, content)` — UPDATE claim SET content + recalcul `dedup_key` (anti-réinsertion du vieux contenu). Retourne booléen (True si claim existait).
- [x] Critère 297 : Méthode KG `delete_claim(claim_id)` — CASCADE manuelle (DuckDB pas de FK enforced) : supprime provenance + edges (src OU dst) + claim. Ne lève jamais (claim absente = no-op, retourne False).
- [x] Critère 298 : Méthode KG `prune_old_claims(retention_days, preserve_kinds={"escalation","insight"})` — oubli par rétention temporelle (miroir `prune_idempotency`). Préserve les leçons durables (escalation+insight). Retourne le nombre de claims supprimées.
- [x] Critère 299 : Fonction module-level `apply_consolidation_actions(kg, numbered_claims, actions, entity_id, run_id)` — applier DÉTERMINISTE (0 LLM, port qm `applyConsolidationActions`). Ordre DELETE-d'abord puis UPDATE puis ADD. Index 1-based, invalide → skip fail-open. ADD via `add_claim` kind='insight' source='consolidation'. Retourne dict résumé {updated, deleted, added, skipped}.
- [x] Critère 300 : Nœud DSPy `ConsolidationSignature` (rôle judge + invariants F-44 via `with_invariants`) + `execute_consolidation_node(kg, run_id, settings)` — parcourt les entités du run, skip si `< memory_consolidation_after`, numérote 1-based, `truncate_output`, `_run_dspy_node(spec=no_think_spec, think=False)`, applique via `apply_consolidation_actions`. NodeMetrics par entité consolidée.
- [x] Critère 301 : Dégradation gracieuse — si LLM down, le KG reste INTACT (aucune mutation), retourne (None, None). L'appelant (workflows.py) replie silencieusement (try/except). Test `test_consolidation_llm_failure_leaves_kg_intact` valide (6 claims avant = 6 claims après).
- [x] Critère 302 : Branchement `workflows.py` fin de run (après boucle sous-tâches, avant `clear_checkpoint`) : `execute_consolidation_node` si `memory_consolidation_enabled` + `prune_old_claims(memory_retention_days)` TOUJOURS (indépendant du flag). Dégradation gracieuse try/except sur les deux. Opt-out `MEMORY_CONSOLIDATION_ENABLED=false`.
- [x] Critère 303 : 24 tests PASS `tests/test_consolidation.py` (applier 7 + KG méthodes 10 + nœud DSPy mocké 5 + E2E workflow 3) + script d'isolation `debug/run_consolidation.py` (3 scénarios doublons/mixed/clean, KG temporaire isolé). Suite pytest 763 passed / 13 failed (12 pré-existants + 1 flaky `test_e2e_resume_reuses_same_run_dir` qui passe en isolation). 0 régression F-68.

### F-68 Phase 2 — Recall cross-run du notebook de leçons durables
- [x] Critère 304 : `KnowledgeGraph.recall_lessons(kinds=None, limit=8)` — query SQL globale `SELECT content, kind, created_at FROM claim WHERE kind IN (insight,escalation) AND status='open' ORDER BY created_at DESC LIMIT ?`. PAS de filtre entity_id ni run_id (global). Kinds défaut `{"insight","escalation"}` (cohérent avec `prune_old_claims.preserve_kinds`). `kinds=set()` ou `limit<=0` → `[]` (défense). Retourne liste de dicts `{content, kind, created_at}`. — testé `TestRecallLessonsKG` (10 cas).
- [x] Critère 305 : Le recall IGNORE les kinds éphémères (`observation`, `refutation` = scratch) — seules les leçons durables (`insight`, `escalation`) sont rappelées. Seules les claims `status='open'` (pas `closed`). Les leçons d'autres runs sont visibles (cross-run = pas de filtre run_id). — testé `test_ignores_ephemeral_kinds` + `test_only_open_status` + `test_cross_run_visible`.
- [x] Critère 306 : `recall_lessons(kg, limit, kinds)` (wrapper module-level `lesson_recall.py`) délègue à `kg.recall_lessons`. `DEFAULT_LESSON_KINDS = {"insight","escalation"}` exposé comme constante. — testé `TestRecallLessonsModuleWrapper`.
- [x] Critère 307 : `build_lessons_block(claims, max_chars=1500)` formate les leçons en bloc markdown numéroté (`1. [LEÇON] ...`, `2. [ESCALATION] ...`) avec en-tête `### LEÇONS DE RUNS PRÉCÉDENTS (mémoire cross-run)` + note explicite « Applique celles pertinentes ; ignore les autres » (mitigation bruit global). Troncature via `truncate_output` (budget max_chars). Liste vide → `""` (injection conditionnelle). Content vide → ligne skippée. Kind inconnu → badge `[KIND]` uppercase. — testé `TestBuildLessonsBlock` (8 cas).
- [x] Critère 308 : Config `memory_recall_enabled` (défaut True) + `memory_recall_limit` (défaut 8) + `memory_recall_max_chars` (défaut 1500) dans la dataclass Settings + `load_settings()` via `_get_bool`/`_get_int` + `.env.example` + `.env` local. Opt-out `MEMORY_RECALL_ENABLED=false`. — testé `TestRecallConfig` (3 cas : défauts, opt-out env, override limit).
- [x] Critère 309 : Branchement `workflows.py` DÉBUT de run (après Router, avant boucle sous-tâches) — `lessons_block` calculé depuis `recall_lessons(kg, limit)` + `build_lessons_block(...)`, dégradation gracieuse try/except (« repli silencieux »). Pas de persistance checkpoint (cheap, refait à chaque run pour fraîcheur). `sub_dict["lessons"] = lessons_block` propagé au Coder. — testé E2E `TestRecallWorkflow`.
- [x] Critère 310 : Injection prompt Coder `nodes.py` — `task.get("lessons")` injecté après `original_content`, même pattern conditionnel (f-string vide si absent → pas de bloc). Ordre : Contexte global → Leçons → Rappel récence. — testé `TestCoderPromptInjection` (4 cas : vide/non-vide/coexistence/clé manquante).
- [x] Critère 311 : E2E cross-run — une insight pré-populée dans le KG sur disque (run antérieur) est rappelée et injectée dans `sub_dict["lessons"]` au run suivant (assertion sur le contenu). Opt-out `MEMORY_RECALL_ENABLED=false` → clé `lessons` vide. KG vierge → bloc vide. — testé `test_recall_populates_lessons_from_prior_run` + `test_recall_disabled_empty_lessons` + `test_recall_no_lessons_in_kg_empty_block`.
- [x] Critère 312 : 31 tests PASS `tests/test_lesson_recall.py` (recall_lessons KG 10 + wrapper 2 + build_lessons_block 8 + prompt injection 4 + E2E workflow 4 + config 3) + script isolation `debug/run_lesson_recall.py` (3 scénarios default/empty/scratch, KG temporaire isolé, vérification d'invariance scratch non rappelé). Suite pytest main final 785 passed / 11 failed (pré-existants confirmés identiques avant/après). 0 régression.

### F-76 — Contextualisation par package (AGENTS.md localisés)
- [x] Critère 313 : `execute_coder_node` lit un fichier `AGENTS.md` au niveau du dossier cible des `target_files` (commonpath des dirname), l'injecte dans le prompt Coder sous `### DIRECTIVES SPÉCIFIQUES AU COMPOSANT (AGENTS.md)`. Fichier absent → bloc vide (injection conditionnelle, pas de crash). — vérifié par lecture `nodes.py`.
- [x] Critère 314 : Défense path traversal (review Kilo) — `agents_md_path` est validé par containment realpath : `os.path.realpath(agents_md_path)` doit rester dans `workspace_root = os.path.realpath(os.getcwd())`. Un `../../etc/passwd` est rejeté silencieusement (fail-open). Smoke testé : chemin normal passe, traversal bloqué. — vérifié par lecture + smoke test.

### F-82 — Skill Finder (recherche/install sécurisée de skills depuis skills.sh)
- [x] Critère 315 : `parse_skills_find_output(raw_stdout)` strippe les codes ANSI et extrait les hits `owner/repo@skill` (regex `_HIT_RE`) ; détecte les marqueurs skills.sh dans une fenêtre autour de chaque hit (`safe`/`verified`/`audited` → `positive` ; `unsafe`/`malicious`/`deprecated`/`archived` → `negative`). Entrée vide/None → `[]`. — testé `TestParseFindOutput` (5 cas : extraction, ANSI, positive, negative, multiple/empty/None).
- [x] Critère 316 : `is_trusted(hit, trusted_authors)` applique la gate conservatrice : owner DOIT être dans l'allowlist (hard gate, spec « n'autoriser que les auteurs de confiance ») ET un marqueur négatif BLOQUE même un auteur de confiance (défense en profondeur) ; un marqueur positif est pur information. `parse_trusted_authors(csv)` parse une liste CSV lowercasée + repli défaut si vide. — testé `TestIsTrusted` (5 cas).
- [x] Critère 317 : `extract_trigger_keywords(description, query, hints, max_keywords=8)` fusionne query (toujours 1er) + hints Architect + termes significatifs extraits de la description (regex, stopwords FR/EN retirés, longueur ≥3), dédoublonne (insensible casse), plafonne à 8. `build_trigger_regex(keywords)` produit `r"\b(kw1|kw2)\b"` (`re.escape` par kw) ; liste vide → regex no-op (ne matche jamais). — testé `TestTriggerKeywords` (8 cas dont cap, dedupe, escape `c++`, no-op vide).
- [x] Critère 318 : `install_skill(hit)` exécute `npx -y skills add owner/repo@skill` via `subprocess.run([...], shell=False)` en **liste d'args** (anti-injection F-38/F-26) avec timeout (120s), `encoding="utf-8", errors="replace"` ; valide chaque composant par `^[A-Za-z0-9_.-]{1,64}$` AVANT tout appel subprocess → un composant malformé (`owner;evil`) est refusé sans lancer de subprocess. Retourne `InstalledSkill` ou `None`. Cross-plateforme (cmd.exe /c npx sur Windows où npx est un shim .cmd). Ne lève jamais. — testé `TestInstallSkill` (4 cas : arg-list/shell=False, bad component refusé sans subprocess, échec returncode, exception fail-open).
- [x] Critère 319 : `register_installed_skill(name, description, triggers, regex, source)` fait un upsert idempotent dans le **manifeste durable** `skills/installed-skills.json` (lazy summary : source/description/triggers/regex/installed_at). `load_dynamic_manifest()` retourne `[(regex, name), ...]` (standalone os+json, `[]` si absent). Pas de mutation du fichier source `skills_loader.py` (fini le scaffold qui salissait git). — testé `TestManifest` (round-trip, idempotent, durable, missing→empty).
- [x] Critère 320 : `search_and_install(query, author, triggers, settings)` orchestre parse→trust-gate→install→extract_trigger_keywords (via `parse_skill_meta` du skill installé)→register→refresh ; retourne un résumé consommé par l'Architect. **Fail-open absolu** : requête invalide → message, auteur non fiable fourni → rejeté avant `find`, erreur CLI/réseau → message, aucune exception propagée. — testé `TestSearchAndInstall` (5 cas : installe+manifeste+regex, auteur non fiable rejeté tôt, aucun hit de confiance, query invalide, erreur CLI fail-open).
- [x] Critère 321 : Intégration `skills_loader` — au démarrage, `DYNAMIC_SKILL_RULES.extend(load_dynamic_manifest())` (no-op si manifeste absent = fresh checkout → 0 régression) ; `register_dynamic_rule(pattern, name)` helper same-run dédoublonné ; `refresh_dynamic_rules_in_memory()` étend `DYNAMIC_SKILL_RULES` après install (idempotent). Le skill installé est ainsi visible des 3 chemins F-57 (`select_skills_for_coder` / `build_conditional_skills_block` / `build_skills_catalog`) « comme les autres ». — testé `TestSkillsLoaderIntegration` (refresh in-memory idempotent + visibilité via select_skills_for_coder).
- [x] Critère 322 : `@tool search_and_install_skill(query, author=None, triggers=None)` (tools.py) = wrapper mince sur `skill_finder.search_and_install` (lazy import) ; split CSV du param `triggers` en liste de hints ; récupère `settings` via lazy import config. `.forward(query, author, triggers)` utilisé par le ReAct Architect délègue correctement. Ne lève jamais. — testé `TestToolWrapper` (2 cas : délégation + split CSV + triggers=None).
- [x] Critère 323 : Config — `skill_finder_enabled: bool = True` (défaut ON, opt-out `SKILL_FINDER_ENABLED=false`) + `skill_finder_trusted_authors: str` (CSV, défaut `vercel-labs,microsoft,google-labs-code,clerk,greensock`) dans la dataclass Settings + `load_settings()` via `_get_bool`/`_get_str` + `.env.example` + `.env` local. Convention défaut-en-dataclass (ne casse pas les `Settings(...)` de test). — testé `TestConfigAndGate` (3 cas : défaut True, override false, override authors).
- [x] Critère 324 : Gate Architect — le bloc ReAct F-82 dans `execute_architect_node` (dspy_nodes.py) est encadré par `if settings.skill_finder_enabled:` (désactivable) ; le wrapper `_search_skill_wrapper` propage désormais `triggers` (hints mots-clés). Le ReAct `SkillResearchSignature` (rôle router + invariants F-85) et l'injection du `research_summary` dans `architect_input` sont conservés (statu quo « toujours ON »). — testé `TestConfigAndGate::test_architect_gate_and_wrapper_triggers_in_source` (regression guard par introspection source).
- [x] Critère 325 : `.gitignore` exclut `skills/installed-skills.json` (artefact runtime régénérable, git propre). Aucune autre mutation de source à l'exécution (le scaffold qui réécrivait `skills_loader.py` est supprimé). — vérifié par lecture `.gitignore` + diff tools.py (plus de `open(loader_path, "w")`).
- [x] Critère 326 : Suite pytest — `tests/test_skill_finder.py` 37 tests PASS (parsing 5, trust 5, keywords/regex 8, install 4, manifest 2, search_and_install 5, intégration skills_loader 2, wrapper 2, config+gate 4). 0 régression : 7 échecs pré-existants (`test_read_gate` ×4 + `test_skill_lazy_loading` ×3) confirmés strictement identiques au baseline via `git stash` (AUCUN lié au Skill Finder). — validé.

### F-97 — Durcir le Tester MA-5 (inline skill resources + normalisation chemins Windows)
- [x] Critère 327 : `normalize_tool_path(path)` (path_utils.py) — pur chaîne, SANS `os.path.abspath` (préserve les chemins relatifs `index.html` et le hashing read_gate). Strippe `file:///`, `file://`, `file:/` → chemin nu ; `/D:/...` → `D:/...` ; MSYS `/d/...` → `D:/...`. Fail-open : tout le reste inchangé (relatif, backslash `D:\...`, déjà-correct, non-str/vide). Jamais d'exception. — testé `tests/test_path_utils.py` (19 cas dont anti-corruption `src/a.js` inchangé, idempotence, 3 variantes de slashes).
- [x] Critère 328 : AUCUNE règle `^([a-zA-Z])/` (sans slash initial) — sinon un chemin relatif `src/a.js` deviendrait `S:/rc/a.js`. Vérifié par `test_relative_nested_unchanged`. — test de non-régression explicite.
- [x] Critère 329 : `read_file` et `list_directory` (tools.py) appliquent `normalize_tool_path` en tête (avant `open()`/`os.listdir()`). Un `file:///...` ne produit plus `[Errno 22]`/`[WinError 123]`. Défense en profondeur (bénéficie aussi au Coder). — testé `tests/test_tools.py` (2 cas d'intégration sur tmp_file/tmp_dir).
- [x] Critère 330 : `load_skill_body_resolved(name)` (skills_loader.py) — résout la progressive disclosure F-92 côté serveur : si `skills/<name>/resources/` existe, inline tous les `resources/*.md` sous `### <title>` (ordre tri déterministe) et RETIRE la section pointeur « ## Dynamic Resources » (sinon l'instruction contradictoire `view_file` reste). Pas de `resources/` → corps brut (rétro-compat strict avec `load_skill_body`). Skill absent → `""`. Jamais d'exception. — testé `tests/test_skill_resolve.py` (7 cas : sans/puis avec resources, ordre, absent, resources vide, intégration vraie skill web-tester).
- [x] Critère 331 : Le bloc résolu de la vraie skill `web-tester` ne contient plus « ## Dynamic Resources » NI « view_file » NI les liens `file:///.../resources/*.md` (le modèle ne peut plus les copier dans read_file). — testé par 2 tests d'intégration sur la skill réelle.
- [x] Critère 332 : `web_tester.py` utilise `load_skill_body_resolved` quand `settings.tester_inline_skill_resources` (défaut True), sinon `load_skill_body` (opt-out `TESTER_INLINE_SKILL_RESOURCES=false`). Règle prompt chemins réécrite : `file:///` RÉSERVÉ à `navigate_page` ; `read_file`/`list_directory` attendent un chemin simple (relatif ou `D:/GIT/...`), avec note que la normalisation auto rattrape une glissade. — vérifié par lecture `web_tester.py`.
- [x] Critère 333 : Racine générateur — `scripts/refactor_skills.py` émet désormais `read_file` (pas `view_file`) + « read on-demand, NOT all upfront » (corrige la source pour les futurs skills refactorés). — vérifié par lecture `refactor_skills.py:107`.
- [x] Critère 334 : Hygiène contenu — `skills/web-tester/resources/evaluation_criteria_verdict_decision.md` : « 12 max steps » (stale, cap réel = 8) → « TESTER_MAX_STEPS, par défaut 8 ». — vérifié par lecture.
- [x] Critère 335 : Config — `tester_inline_skill_resources: bool = True` dans la dataclass Settings + `load_settings()` via `_get_bool("TESTER_INLINE_SKILL_RESOURCES", True)` + `.env.example` + `.env` local. Convention défaut-en-dataclass (ne casse pas les `Settings(...)` de test). — vérifié par lecture config.py.
- [x] Critère 336 : Suite pytest — 28 nouveaux tests PASS (path_utils 19 + skill_resolve 7 + tools 2). Suite complète 892 passed / 8 failed (les 8 STRICTEMENT pré-existants `test_read_gate` ×4 + `test_skill_lazy_loading` ×3 + `test_guard` ×1, confirmés identiques via `git stash`). py_compile OK (9 fichiers). 0 régression. — validé.

### F-72 — Prompt Offloading (outils Python dédiés encapsulant snippets JS/algorithmes)
- [x] Critère 337 : Module `js_utils.py` — extraction DRY de `run_node_check(js_source) -> Tuple[int,str]` + `MAX_JS_CHARS` (200_000) depuis `static_tester.py`. `static_tester.py` importe en alias privé (`from .js_utils import run_node_check as _run_node_check, MAX_JS_CHARS as _MAX_JS_CHARS`) → code appelant (lignes 150-153) inchangé, imports morts `subprocess`/`tempfile` retirés. 0 changement comportemental (Static Tester Tier 1a intact). — testé `tests/test_js_utils.py` (5 cas : syntaxe OK/error skipif node + node absent mock + SubprocessError mock + constante).
- [x] Critère 338 : `@tool check_js_syntax(path)` (tools.py) — lit le fichier, applique `run_node_check`, retourne message ✅/❌ lisible. Détection `node` absent upfront (`shutil.which`) → message informatif (n'affirme JAMAIS 'valide' sans avoir vérifié, contrairement au tuple (0,'') ambigu de run_node_check). Troncature `MAX_JS_CHARS`. Jamais d'exception. — testé `tests/test_tools.py` +4 (valide, syntax error, fichier absent, node absent mock).
- [x] Critère 339 : `check_js_syntax` branché au Coder (`nodes.py execute_coder_node` : ajouté à l'import + à `coder_tools`) ET retrait de l'exemple subprocess `node --check` du prompt Coder (l.840 → remplacé par mention de l'outil `check_js_syntax(path=...)`). — vérifié par lecture + py_compile.
- [x] Critère 340 : Module `devtools_dom_tools.py` — 3 sous-classes `smolagents.Tool` (`clean_dom`, `add_visual_tags`, `fuzz_click_all_buttons`) qui wrappent `evaluate_script` (DevTools) via `function=<JS>`. Snippets JS corps préservés EXACTS (clean/add depuis l'ancien `web_tester.py:100,112`, fuzz depuis `nodes.py:722`), adaptation `script=`→`function=` ET retrait de l'enveloppe IIFE `(() => {...})()` → `() => {...}` (DevTools exige une fonction NON invoquée, l'invoque lui-même — une IIFE crasherait le CDP). Factory `build_devtools_helper_tools(cdt_tools)` fail-open (cherche `evaluate_script`, retourne `[]` si absent). — testé `tests/test_devtools_dom_tools.py` (10 cas : factory ×3, metadata, délégation ×3, contrat non-IIFE, préservation balises strip).
- [x] Critère 341 : WebTester — retrait du bloc mort `PuppeteerAddVisualTagsTool`/`PuppeteerCleanDomTool` (wrappaient `puppeteer_evaluate` = navigateur Puppeteer qui ne charge pas les `file://` locaux ; le prompt disait de NE PAS les utiliser) + branchement `tester_tools.extend(build_devtools_helper_tools(cdt_tools))`. Prompt Tester màj : `clean_dom`/`add_visual_tags`/`fuzz_click_all_buttons` (SANS préfixe puppeteer_) désormais DevTools-based et OK à utiliser. 2 skills `web-tester/resources/` màj cohérence (`puppeteer_clean_dom`→`clean_dom` ; note « À ÉVITER » → « OK sur le navigateur actif »). — vérifié par lecture + grep 0 réf stale fonctionnelle.
- [x] Critère 342 : Coder — branchement `coder_tools.extend(build_devtools_helper_tools(cdt_tools))` (après `coder_tools.extend(cdt_tools)`) ET retrait du snippet fuzzing inline du `preview_block` (étape 3 : `evaluate_script(function="() => document.querySelectorAll('button')...")` → `fuzz_click_all_buttons()`). Reste du workflow (navigate/screenshot/console) préservé. — vérifié par lecture.
- [x] Critère 343 : Centralisation doc DevTools — `DEVTOOLS_BASE_DOC` (chrome_devtools_tool.py) = signatures communes `navigate_page`/`list_console_messages`/`evaluate_script` + avertissement anti-IIFE CRITIQUE (single source of truth). `_DEVTOOLS_TOOLS_DOC` (Coder) = `DEVTOOLS_BASE_DOC` + note « 90% syntax check + Lighthouse réservé Tester ». `devtools_hint` (Tester, refactoré en if/else propre) = intro + `[DANGER FATAL filePath]` + `DEVTOOLS_BASE_DOC` + `take_snapshot`/`click`/`fill`/`take_screenshot` + `[VISUAL BUG ALERT]` + `[PYTHON BUILT-INS]` + priorité DevTools-first. Couverture point-par-point vérifiée (0 marqueur perdu vs les 6 points doc Coder + 13 points hint Tester initiaux). — vérifié par grep marqueurs + `test_prompts.py` PASS (n'assertait pas la doc DevTools, refactor sûr).
- [x] Critère 344 : Suite pytest — 19 nouveaux tests PASS (js_utils 5 + devtools_dom_tools 10 + tools +4). Suite complète 940 passed / 8 failed (STRICTEMENT pré-existants `test_read_gate` ×4 + `test_skill_lazy_loading` ×3 + `test_guard` ×1, documentés F-97/F-82, AUCUN lié à F-72 — ces modules ne touchent ni à js_utils/devtools_dom_tools/tools.check_js_syntax/nodes/web_tester/chrome_devtools_tool). py_compile OK (7 fichiers). 0 régression. — validé.

### F-93 — Grounding des findings Judge (anti-hallucination de localisation, P6)
- [x] Critère 345 : Module `graph_orchestrator/judge_grounding.py` (0 LLM, miroir `judge_metrics.py` F-70 : `from __future__ import annotations`, typage strict, défensif). Port simplifié du `WordAligner` de langextract — ÉCART CONSCIOUS : algorithme **legacy** `_fuzzy_align_extraction` (fenêtre glissante difflib, `resolver.py:591-715`) plutôt que le DP LCS `_best_lcs_span` (plus simple ; bornage `max_window = 2·len(needle)` implique densité ≥ ~0.5, un seul seuil coverage 0.75 suffit). `_normalize_token` (lowercase + plural stem), `_raw_tokens` (`re [^\W\d_]+|\d+`, ponctuation drop = bruit code), `fragment_is_grounded(source, needle, *, threshold=0.75) -> bool` (vide→True fail-open). Pure stdlib.
- [x] Critère 346 : `read_source_files(paths) -> dict` (fail-open, miroir `judge_diff.py`) ; `_resolve_file(fname, sources)` (basename puis substring) ; `_extract_file_line_refs` (`file.ext:NN`) ; `extract_code_fragments(text)` = spans backtick + chains `a.b.c` ONLY (PAS de prose, PAS de filenames via `_looks_like_filename`+`_FILE_EXTS` — anti faux-négatif/positif, testé `test_filename_exclu_des_fragments`).
- [x] Critère 347 : `ground_finding(finding, source_files) -> FindingGrounding` — (a) line-range check (`file:NN` hors-bornes→ungrounded ; NN valide→grounded) ; (b) fragments : aucun→fail-open grounded (prose-only), sinon grounded si ≥1 s'ancre (fichier résolu d'abord, puis TOUS en fallback indulgent). `ground_findings(findings, source_files) -> GroundingReport` ; source vide → tout grounded (fail-open global). Data models `FindingGrounding`/`GroundingReport` locaux (pas de churn `models.py`).
- [x] Critère 348 : Politique Option 1 (non-destructive, décision utilisateur) — `apply_grounding(verdict, report) -> CodeJudgeOutput` reconstruit via `model_copy(update={"findings": ...})` : chaque finding ungrounded rétrogradé d'un cran (`critical→high`, …, `low` inchangé) + `[ungrounded: <raison>]` appendé à `description` ; findings ancrés inchangés ; **`is_approved` JAMAIS changé**. No-op si 0 ungrounded (retourne le verdict à l'identique). Testé `TestApplyGrounding` (4 cas : critical→high+flag, low reste low+flag, no-op, is_approved True inchange).
- [x] Critère 349 : Branchement `execute_code_judge_node` (`dspy_nodes.py`) — post-LLM (entre `result = await _run_dspy_node(...)` et `return`), gardé par `settings.judge_grounding_enabled` ; `try/except Exception: pass` fail-open total (un bug grounding ne casse JAMAIS le verdict) ; log observabilité `N/M finding(s) non ancré(s)`. Le path fail-closed (security=None, retour avant LLM) est naturellement exclu. Imports lazy `from .judge_grounding import ...`.
- [x] Critère 350 : Config `judge_grounding_enabled: bool = True` (dataclass `config.py` + `_get_bool("JUDGE_GROUNDING_ENABLED", True)` dans `load_settings`) + `.env.example` (section dédiée) + `.env` local. Opt-out `JUDGE_GROUNDING_ENABLED=false`. Pas de config de seuil (constantes module, minimalisme F-70).
- [x] Critère 351 : Tests — `tests/test_judge_grounding.py` (34 tests : normalize 4, fragment_is_grounded 8 dont scattered-rejete, extract 6 dont filename-exclu, read/resolve 7, ground_finding 6, ground_findings 2, apply_grounding 4) + 2 intégration `test_dspy_nodes.py` (`test_execute_code_judge_node_grounds_ungrounded_finding` downgrade critical→high+flag is_approved inchangé ; `test_execute_code_judge_node_grounding_opt_out` finding inchangé). 36 nouveaux tests PASS.
- [x] Critère 352 : Scripts isolation — `debug/run_judge_grounding.py` (déterministe 0 LLM, 3 scénarios ancré/inventé/prose-only sur HTML Bubble Sort, valide les 4 invariants politique Option 1) + `debug/run_judge.py` étendu (affiche le flag `⚠️[ungrounded-rétrogradé]` sur les findings).
- [x] Critère 353 : Non-régression — suite pytest ciblée Judge 75/75 PASS. Suite complète 979 passed / 9 failed dont 8 STRICTEMENT pré-existants confirmés identiques via `git stash` (read_gate×4 + skill_lazy×3 + guard×1, AUCUN lié au grounding — modules non touchés) + 1 flaky non reproduit en isolation. py_compile OK (3 fichiers prod + 1 debug). 0 régression F-93.

### F-106 — Clôture du journal plat (récupération historique git → event stream DuckDB)
- [x] Critère 354 : `scripts/recover_log_history.py` — parcours de TOUTES les versions git du journal (47 commits, les deux ères du fichier via `--follow`), parsing `## [date] type | titre` (regex `[\w-]+` capte aussi `P8-bis`), union dédupliquée par (date, type, titre), sous-titres `##` non datés rattachés au corps de l'entrée courante, préambule capturé comme entrée `doc`.
- [x] Critère 355 : Import daté dans `run_event` (`run_id='legacy_md'`) avec `created_at` = date de l'en-tête (fallback : date du commit de première apparition) — CORRIGE la perte de dates de la migration F-75 (194 lignes toutes horodatées 2026-08-05) et 3 coquilles source `[2026-01-08]` → `2026-08-01`. Idempotent (clé event_type+titre), backup de la base avant mutation, remplacement documenté des lignes `legacy` non datées.
- [x] Critère 356 : `scripts/log_event.py` — CLI d'historisation assistant (`<event_type> "<message>"`, options `--run-id/--node/--date/--db`), même canal/table que l'outil `log_event` du graphe, vocabulaire recommandé non bloquant.
- [x] Critère 357 : Suppression définitive du journal plat (fichier + `scripts/migrate_log.py` obsolète) et purge de toutes les mentions dans les fichiers opératifs (AGENTS.md §2D réécrit avec règle « AUCUN JOURNAL PLAT » + interdiction de recréation, plan, contract, progress, feature_list, references_audit, nodes.py, log_event.py). Seules pierres tombales : AGENTS.md §2D, F-106, et recover_log_history.py (l'outil qui lit l'historique git).
- [x] Critère 358 : Validation — base finale 199 événements datés (2026-07-29 → 2026-08-14) ; grep final 0 mention hors tombstones ; py_compile OK ; tests `tests/test_log_recovery.py` PASS (parser, dedup multi-versions, insertion idempotente, remplacement legacy, git-walk sur repo tmp, CLI).

### F-99 — Goal Enforcement (anti-loop comportemental, qm goal.ts)
- [x] Critère 359 : Module `graph_orchestrator/goal_enforcer.py` (0 LLM, 100% Python natif) — `audit_completion` (preuves déterministes : écritures via extract_tool_calls_from_step, livrables sur DISQUE = état autoritaire, verify-after web), `goal_continuation_prompt` (discipline qm « NON PROUVÉE », objectif encapsulé `<objectif>` et échappé HTML anti-injection, preuves manquantes listées), `goal_cap_prompt` (wind-down), `GoalEnforcer.enforce` (table de vérité accept/continue/waive, RLock).
- [x] Critère 360 : Audit mode-aware — création (itération 1) exige ≥1 écriture + TOUS les target_files existants sur disque + verify-after (check_js_syntax OU list_console_messages) si web ; correction (itération > 1) se réduit au changement matériel. Le screenshot reste exigé séparément par F-50 (non dupliqué).
- [x] Critère 361 : Les 4 mécanismes qm portés — continuation prompt sur final_answer valide non prouvé ; MÊME impasse répétée `goal_blocked_min_rounds=3` → statut blocked ACCEPTÉ (résultat conservé, le Judge arbitre) ; `goal_waiver_stalled_rounds=5` rounds de continuation sans aucun tool call → auto-waiver anti-deadlock ; `goal_token_cap` (2M cumulés in+out) → wind-down unique puis arrêt accepté, jamais fausse complétion. L'état (impasse, streak, tokens) VIT à travers les retries (ne se reset PAS avec la purge mémoire).
- [x] Critère 362 : Branchement `run_with_retry` (param optionnel `goal_enforcer=None` non-cassant ; `record_tokens` à chaque attempt même échouée ; sur round de continuation le RAPPEL générique « JSON invalide » est SUPPRIMÉ — le final_answer était valide) + `execute_coder_node` (instancie GoalEnforcer : objectif=task['content'], target_files, iteration, `_is_web_task`).
- [x] Critère 363 : Config `goal_enforcement_enabled` (défaut True), `goal_blocked_min_rounds` (3), `goal_waiver_stalled_rounds` (5), `goal_token_cap` (2 000 000, 0=désactivé) dans config.py + load_settings + `.env.example` + `.env` local. Opt-out GOAL_ENFORCEMENT_ENABLED=false.
- [x] Critère 364 : Fix annexe + validation — `loop_guard.extract_tool_calls_from_step.known_tools` étendu (multi_replace/check_js_syntax/list_console_messages/take_screenshot/navigate_page/evaluate_script — sinon aucune preuve verify-after visible en chemin CodeAgent ; outils observationnels restent exemptés du comptage LoopGuard). 22 tests PASS (`test_goal_enforcer.py`), suite complète 1035 passed / 0 failed / 1 skipped. py_compile OK. 0 régression.

### F-99-bis — Durcissements Goal Enforcement après runs E2E #2/#3 (PR #74)
- [x] Critère 365 : Waiver dernier attempt — `run_with_retry` convertit CONTINUE en waiver quand `attempt == max_retries-1` (une continuation sans budget restant convertirait un final_answer valide en échec technique et priverait le Judge de son arbitrage ; la boucle graphe max_iterations reste l'enceinte externe). Testé `test_run_with_retry_dernier_attempt_waive`.
- [x] Critère 366 : Preuves CUMULÉES cross-attempts (`collect_evidence` + `_acc_writes/_acc_verify`) + observabilité (le reason CONTINUE liste les preuves manquantes). Testé `test_evidence_accumulee_cross_attempts` + `test_reason_liste_les_preuves_manquantes`.
- [x] Critère 367 : Hiérarchie des preuves autoritaires (run #3 : la compaction ampute memory.steps) — création = DISQUE (cible existante = livrable écrit, testé `test_enforcer_preuve_par_disque_memoire_compactee`) ; correction = modification git non commitée (`_disk_change`, F-53, testé `test_enforcer_correction_preuve_via_git`) en secours des writes cumulés ; verify-after retiré des bloqueurs du GoalEnforcer (redondant F-50 + Static Tester, testé `test_enforcer_verify_after_non_bloquant`). FAST_CONTEXT 32768→49152 dans `.env.example` (leçon run #1 : 400 exceed_context_size à 33-39k). 28 tests F-99 PASS, 87 guards. **Validation E2E run #3 : APPROUVÉ par le Judge** (2 continuations → production des fichiers → blocked-waive → arbitrage).

### F-108 — Fail-closed Judge + Tier 1c comportemental + persistance verdict (post-mortem run #3)
- [x] Critère 368 : Gate fail-closed TEST dans `execute_code_judge_node` (mirror Security F-61) — `test_res` en `failure` (CoderOutput), `timeout` (str TIMEOUT) ou absent (None) → `is_approved=False` SANS appel LLM, finding `critical/testing`, feedback reprenant les détails du Tester (guide l'itération Coder). Un test_res str bénin (« TESTS: SUCCESS ») passe vers le LLM (rétrocompat). Opt-out `JUDGE_RESPECT_TEST_FAILURE=false` (config.py + load_settings + .env.example + .env).
- [x] Critère 369 : Le STATUT du verdict est transmis au LLM Judge — `test_results` contient désormais `status=X | details` (avant : seuls les details, le mot failure n'était jamais visible du modèle).
- [x] Critère 370 : Static Tester Tier 1c `_check_behavioral_smells` (Tier 1, 0 LLM, 0 Chrome) — (a) compteur mort : variable au nom compteur initialisée à 0, affichée (textContent/innerHTML/template `${var}`), jamais incrémentée (`x++`/`x +=`/`x = x +`) → erreur [behavior] actionnable ; (b) animation instantanée : délai résoluble < 20 ms (`sleep(N)` toujours ; `setTimeout(fn, N)` seulement avec mot-clé d'animation — anti-FP deferral). Branché dans `static_check_html` Tier 1 (court-circuite le Tester LLM sur ces bugs, < 1s).
- [x] Critère 371 : Persistance du verdict Judge — `workflows.py` journalise chaque verdict dans l'event stream (`run_event`, node=judge, type=verdict, approved + feedback + nb findings), fail-safe try/except. Validation : le script.js EXACT du run #3 → 2 bugs Tier 1c attrapés ; version corrigée → 0 ; 12 tests judge + 8 Tier 1c ; suite complète 1052/0/1.

### F-109 — Audit visuel matérialisé + Tier 4 console (boucle Coder isolée)
- [x] Critère 372 : Outil `visual_check(criterion_number, verdict, observation)` (tools.py) + store module-level (`_VISUAL_AUDIT`/reset/get) — matérialise l'audit critère par critère après le screenshot.
- [x] Critère 373 : Enforcement `run_with_retry` (param `visual_criteria_count`) — après le check vision F-50, final_answer BLOQUÉ si checklist incomplète / verdict False / observation < 10 chars (`_visual_checklist_error`, messages actionnables). Branché `execute_coder_node` (reset + comptage des critères F-90, gated `visual_audit_enabled`).
- [x] Critère 374 : F-109-bis — RAPPEL INJECTÉ au boundary d'attempt (décompte exact des manquants) quand une tentative finit sans verdict et checklist incomplète. Testé (impression + prompt).
- [x] Critère 375 : Prompt — CHECKLIST OBLIGATOIRE explicite dans `build_visual_criteria_block` (numéros 1..N, refus si incomplet/False/creuse).
- [x] Critère 376 : Static Tester TIER 4 console (`_check_console_errors`) : navigate frais + clic primaire + `list_console_messages`, regex `Uncaught|TypeError|ReferenceError|...` → réfutation `[console]`. Validé LIVE : crash run #4 attrapé (is_valid False), run #5 corrigé = vrai négatif. Wire dans `static_check_html` (session DevTools partagée).
- [x] Critère 377 : Validation boucle Coder isolée — boucle 1 (avant fix-bis) : 0/6 audits, 2×40 steps sans conclusion ; boucle 2 : **6/6 audits + final_answer success en 1 tentative (503s)** ; livrable vérifié en direct (0 erreur console, compteur 0→15, 500ms/step, 50 barres, thème sombre). Tests 8 + 35 PASS.

### F-110 — Gardes compteur-rafraîchi + canvas-child + patron canonique (post-mortem run #6)
- [x] Critère 378 : Tier 1c-(c) `_check_behavioral_smells` — compteur affiché (assignment textContent/template existant) ET incrémenté (`X++`) mais sans rafraîchi d'affichage dans les 400 chars suivant l'incrément → flag `[behavior]` actionnable (compteur interne jamais affiché = hors périmètre). Testé sur fixture run #6 + contre-épreuve rafraîchie.
- [x] Critère 379 : `_check_canvas_children(html, js)` — `appendChild` sur un `getElementById` correspondant à un `<canvas id>` du HTML → flag (les enfants d'un canvas ne s'affichent JAMAIS ; canvas OU div, jamais les deux). Wire dans `static_check_html` Tier 1. Testé (anti-pattern / div-conteneur propre / ctx-drawing propre).
- [x] Critère 380 : Skill `coding` règle 5 (eager) — boucle d'animation CANONIQUE : exemple complet `comparisons++; counterEl.textContent = comparisons; draw(); await sleep(delay);` avec les contre-exemples ❌ (incrément sans rafraîchi, draw() seulement à l'init, divs dans canvas). Validation réelle : run6 → 2 flags exacts, loop2 sain → 0, suite complète 1065/0 ; run #7 : 3 rejets justifiés + escalade au diagnostic correct.

### F-103 — Budgets de guidance (gate deer-flow) + signaux prompt-audit greppables
- [x] Critère 381 : `scripts/check_agent_guidance.py` (0 LLM, déterministe) — port du gate deer-flow adapté aux surfaces du repo : racine `AGENTS.md` (12/16 Ko soft/hard), module `graph_orchestrator/prompts.py` (24/32 Ko), local `skills/*/SKILL.md` (40/48 Ko), chaîne Coder cumulée (80/96 Ko = prompts.py + corps des SKILL.md eager importés depuis `ALWAYS_SKILLS_CODER`, source de vérité unique, PAS une copie). Tailles normalisées CRLF→LF en octets UTF-8 (port fidèle `normalized_utf8_size`).
- [x] Critère 382 : Sémantique incrémentale deer-flow portée fidèlement — `--base-ref/--head-ref` : au-dessus du hard sans croissance vs base = warning (adoption sans casser l'existant), croissance = error ; soft dépassé + fichier touché = warning. Exit 1 si error OU (warning + `--strict-warnings`). Durcissement vs référence : repo sans commit (`git diff HEAD` en erreur) → repli sur les non-trackés au lieu de crasher.
- [x] Critère 383 : Signaux prompt-audit (Anthropic) greppables en opt-in `--audit-signals` — AG101 densité de pression (MUST/NEVER/CRITICAL/IMPORTANT/INTERDIT/JAMAIS/TOUJOURS…, seuil 4 occ/Ko calibré sur l'état réel max ~1.6) + AG102 hedges (« try to/si possible »), 1 finding AGRÉGÉ par fichier et par signal (pas par occurrence). CAVEAT systématique : la pression est LÉGITIME pour nos petits modèles locaux sous-déclencheurs — signal calibré pour modèles très littéraux, jugement requis avant suppression.
- [x] Critère 384 : Annotations GitHub CI (`--github-annotations` : `::error file=...,line=1,title=AG001::`) + découverte par `git ls-files --cached --others` (mode worktree) ou `git ls-tree/show` (mode refs).
- [x] Critère 385 : Validation — 37 tests PASS (sémantique budget 7 branches, chaîne 3, analyze 8, signaux 5, exit codes 4 dont basetemp git init réel, intégration repo réel 2) ; suite complète **1110 passed / 0 failed / 1 skipped** (0 régression) ; run LIVE : mode local = 29 surfaces, 1 error `AGENTS.md` 20 433 o > hard 16 384 (vrai signal consigné, réduction NON exécutée — décision utilisateur) ; mode incrémental vs origin/main = 0 error 0 warning exit 0 ; `--audit-signals` = 1 warning hedge agrégé (frontend-design-anthropic).

### F-105 — Durcissement sécurité : password managers (groupe 10) + redaction de secrets
- [x] Critère 386 : `bash_guard.py` enrichi du groupe 10 « password managers & OS keychain » (transpilation fidèle de `references/davidondrej-skills/hooks/dangerous-patterns.txt` l.54-65) : CLIs distinctifs (`bws|bw|lpass|keepassxc-cli|rbw|nordpass`) bloqués d'office ; `pass` bloqué en position de commande STRICTE avec argument (`_SEP_CMD` = début de ligne ou `;&|` + `re.M` pour les commandes multilignes — `echo "please pass the token"` et `const pass = getPassword()` passent) ; `op` bloqué avec ses VRAIS subcommands seulement (read/run/inject/item/document/vault/connect/service-account/events-api/signin — `op --version`/`op whoami` passent) ; `security find-generic-password|find-internet-password|dump-keychain` (flags avant subcommand gérés) ; bundles `.app` PM ; `~/.password-store` (`~`/`$HOME`/`${HOME}`/`/Users/x`) ; `open -a` PM ; `brew uninstall|remove|rm` PM (install autorisé) ; `gpg --export-secret-key(s|subkeys)` (`--export`/`--list-secret-keys` passent).
- [x] Critère 387 : Cas de `test-guard.sh` portés en pytest paramétré (~49 block + ~24 allow) — incl. commandes composées (`cd /tmp && pass show`, `echo ok; pass show`, `true | pass insert`), espaces de tête, multiligne (`cd /tmp\npass show`), et TOUS les faux positifs de la référence (bare `pass`, `npm run pass-tests`, `grep -R bypass`, `git commit -m "no op needed"`, `security find-certificate`, `gpg --export --armor`, `brew uninstall wget`, `open -a Safari`).
- [x] Critère 388 : Module `graph_orchestrator/redaction.py` (déterministe, 0 LLM, directive « Redact » mattpocock) — `redact_secrets(text)` remplace par `<REDACTED>` : blocs PEM privés (entier, publics intacts), identifiants d'URL (utilisateur conservé), tokens à préfixe réservé (`sk-`/`ghp_`/`github_pat_`/`xox[baprs]-`/`AKIA`/`AIza`/JWT `eyJ`), en-têtes `Bearer|Authorization`, affectations nommées (password/passwd/pwd/secret[_key]/api_key/access_token/auth_token/client_secret/token, nom pouvant être SUFFIXE d'identifiant : `PGPASSWORD`/`OS_PASSWORD` — nom complet conservé, séparateur `:` ou `=` et quotes préservés).
- [x] Critère 389 : POLITIQUE ANTI-CORRUPTION (fail-open documenté) — les valeurs code-like ne sont JAMAIS redactées : accès attribut (`request.body.password`), appel (`get_token()`), index (`config.keys[0]`), interpolation (`` `${user}_pw` ``), références env (`$VAR`, `%VAR%`), placeholders `<...>`, valeurs < 8 chars ; idempotence (`<REDACTED>` non re-touché). Priorité : lisibilité du code montré au Judge > couverture des secrets exotiques.
- [x] Critère 390 : Branchement `feedback_utils.truncate_output` (point de passage unique du feedback vers le LLM : Tester→Judge, Judge→Coder, bash_command) — redaction AVANT l'early-return court (un secret dans une sortie courte est masqué comme dans une longue) ; doctrine F-21 conservée (à la lecture ; DuckDB garde l'intégral pour audit). Opt-out `REDACTION_ENABLED` (défaut True) + `.env.example` + `.env`. Validation : 88 tests nouveaux (69 bash_guard + 19 redaction) PASS, suite complète **1198 passed / 0 failed / 1 skipped** (1110 baseline + 88, 0 régression), py_compile OK.

### F-100 — Recettes de vérification exécutable (port hermes-agent verify/)
- [x] Critère 391 : Package `graph_orchestrator/verify/` (Python pur, 0 LLM) — port quasi 1:1 de `references/hermes-agent/agent/verify/` : `recipes.py` (`Recipe` dataclass + `detect_recipe` ordre grok préservé : package.json→Node avec frameworks next/sveltekit/astro/remix/cra/vite + package manager par lockfile, Python Django/FastAPI/Flask/générique, Go/Rust/Maven/Gradle/Makefile/compose), `runner.py` (`run_verify` : phases séquentielles → start arrière-plan → boucle readiness HTTP → teardown), `environment.py` (manifeste `.verify/environment.json` qui PRIME sur la détection, tolérant aux fichiers corrompus → dégradation vers détection fraîche).
- [x] Critère 392 : Écarts consciencieux documentés — (a) détecteur **static-web** en dernier recours (cas dominant Prompt-Vault : vanilla HTML sans manifeste → `"{sys.executable}" -m http.server {port} --bind 127.0.0.1`, bind 127.0.0.1 anti popup firewall Windows, readiness `/` si index.html sinon `/<page>.html`) ; (b) `phases=None` (toutes) vs `()` (aucune) distingués, `phases` ne pilote QUE bootstrap/build/test et le start est piloté par `skip_start` (la référence encode `start` dans la liste des phases → « start seul » inexprimable) ; (c) substitution du placeholder `{port}` → port libre dynamique ; (d) teardown Windows `taskkill /F /T /PID` (kill de l'ARBRE, `proc.terminate()` ne tuerait que cmd.exe et laisserait le serveur orphelin), branche POSIX killpg = référence ; (e) sonde readiness via opener SANS proxy (127.0.0.1 hors HTTP_PROXY).
- [x] Critère 393 : Tier HTTP dans `execute_static_tester_node` (static_tester.py) — déclenché UNIQUEMENT si les Tiers 1-4 sont propres ; recette détectée au niveau du dossier du 1er target HTML (manifeste ou détection) ; start sur port LIBRE (`_find_free_port` bind-0) ; preuve `[http] Page servie : URL → HTTP 200 (recette kind/source, Xs)` concaténée au details success. Politique de verdict : readiness KO = **réfutation** (`[http]` + commande start + erreur + tail de sortie serveur) SAUF recette static-web (notre infrastructure http.server, pas le code du modèle → note de dégradation, jamais d'échec faux). Try/except total fail-open (ADR-0002).
- [x] Critère 394 : Config — `STATIC_TESTER_HTTP=0` coupe le tier (défaut 1), `STATIC_TESTER_HTTP_TIMEOUT` (défaut 10s) borne la boucle de readiness ; reportés dans `.env.example` + `.env` local (règle AGENTS.md §7). Script isolation `debug/run_verify.py` (déterministe 0 LLM, convention F-89) : dossier en arg, `--full` (bootstrap/build/test), `--port`, `--timeout`, verdict structuré JSON + exit code.
- [x] Critère 395 : Validation — **52 tests PASS** (Recipe dataclass/normalisation, détection toutes stacks + static-web + précédence node, sémantique phases None/(), VRAI http.server servi + readiness 200 + re-bindabilité du port après teardown, readiness KO, manifeste round-trip/corrompu/primauté, intégration Static Tester 6 : preuve success / opt-out / skip sur bug Tier 1 / réfutation start projet KO / dégradation static-web KO) ; suite complète **1250 passed / 0 failed / 1 skipped** (1198 baseline + 52, 0 régression) ; LIVE `debug/run_verify.py` sur golden run Bubble Sort : static-web détectée, HTTP 200 en 1.1s sur port libre 61250, teardown propre.

### F-102 — Checkpoint git par itération Coder pour le Judge (port open-swe turn_checkpoint)
- [x] Critère 396 : Module `graph_orchestrator/turn_checkpoint.py` (Python natif Windows, 0 LLM, 0 shell POSIX) — port de `references/open-swe/agent/utils/turn_checkpoint.py` : `record_turn_checkpoint(turn_key, repo_path)` snapshotte le worktree dans la base d'objets sous `refs/graph-orchestrator/turns/<key>` SANS contaminer HEAD, l'index réel ni le worktree (index scratch `GIT_INDEX_FILE` tempfile → `read-tree HEAD|--empty` → `add -A .` → `write-tree` → `commit-tree (-p HEAD)` → `update-ref`). Une REF (pas un tree nu) : survit à un `git gc` mi-run ; le namespace n'est jamais poussé.
- [x] Critère 397 : `read_turn_diff(base, head=None, repo_path, include_contents=True)` — diff STRUCTURÉ best-effort `{status: ready|missing|error, files, truncated}` : `git diff --numstat -z` + `--name-status -z` (statut added/removed/modified, +ajouts/-suppressions, binaire → unrenderable) + contenus base/head par fichier via `git cat-file --batch` natif (stdin, parsing binaire une passe, blob > 400 Ko → unrenderable, cap 200 fichiers → truncated). Parseurs purs `parse_numstat`/`parse_name_status`/`build_diff_files`/`_decode` = ports fidèles.
- [x] Critère 398 : Écarts consciencieux documentés — (a) `merge_checkpoint` (registry de thread) NON porté : son invariant « earliest wins » est encodé au niveau de la ref (`record_turn_checkpoint` n'avance JAMAIS une ref existante → une reprise après crash ne fait pas glisser la base du tour) ; (b) `_cd_repo` remplacé par `repo_path` explicite + garde d'isolation F-53 réutilisée (`_is_isolated` : refus de TOUTE écriture de ref si le repo découvert n'est pas le run dir — anti-pollution repo principal) ; (c) blobs octets bruts (pas de base64+JSON, pas de transport sandbox) ; (d) param `include_contents` AJOUTÉ (le Judge runtime n'a besoin que du résumé — sauter `cat-file` évite de lire 200 blobs pour rien).
- [x] Critère 399 : Intégration — (a) `workflows.py` : snapshot `<task_id>-iter<N>` au DÉBUT de chaque itération APRÈS le bloc Drafter (le `draft_*.md` ne compte pas comme changement de code du tour), gardé par `git_ok && settings.turn_checkpoint_enabled` ; post-Coder, `read_turn_diff(ref, include_contents=False)` → `summarize_turn_diff` → `sub_dict["turn_diff_summary"]` (best-effort, résumé vide si pas ready) ; (b) `judge_diff.build_judge_code_block(..., turn_diff_files=)` préfixe le bloc « CE QUE GIT DIT (turn checkpoint F-102) » — manifeste par fichier disponible DÈS l'itération 1 (le diff texte F-53 y est vide, <2 commits) et placé devant le bloc diff texte IN-DIFF en iter >1 ; absent/vide = comportement F-70 pur (rétrocompat stricte). Config `TURN_CHECKPOINT_ENABLED` (défaut true) + `.env.example` + `.env`. Script isolation `debug/run_turn_checkpoint.py` (déterministe 0 LLM, convention F-89).
- [x] Critère 400 : Validation — **34 tests nouveaux PASS** (29 `test_turn_checkpoint.py` : sanitization ref 3, record 7 dont non-contamination HEAD/index + reprise earliest-wins + anti-pollution parent, parsers 5, read_turn_diff 10 dont binaire/gros blob/truncated à 201 fichiers/diff entre deux refs/include_contents=False, summarize 2, intégration séquence production exacte 2 ; + 4 `test_judge_diff.py` bloc F-102/rétrocompat ; + 1 `test_config.py` défaut/opt-out) ; suite complète **1284 passed / 0 failed / 1 skipped** (1250 baseline + 34, 0 régression) ; py_compile OK ; LIVE `debug/run_turn_checkpoint.py` : **8/8 invariants** (ref créée, 0 commit HEAD, untracked intact, résumé iter 1 = 2 fichiers added alors que diff texte F-53 = 0 char, reprise ref non avancée, résumé iter 2 modified +1/-1, ordre manifeste→diff→code complet dans le bloc Judge).

### F-104 — Retry LLM v2 : pré-contenu vs mid-stream + init MCP non bloquante (openfox/opencode/crush/deer-flow)

- [x] Critère 401 : Module `graph_orchestrator/llm_retry.py` (Python pur, 0 dépendance) — `RetryPolicy` frozen (max_retries=5, base_delay_s=1.0, max_delay_s=30.0, jitter_factor=0.25, honor_retry_after=True — port des constantes opencode `RETRY_MAX_RETRIES`/`RETRY_JITTER_FACTOR`/cap 30s) ; `classify_llm_error` (fatal 4xx/auth/context-length AVANT retryable réseau/429/5xx, regex à frontières de mots — un code HTTP ne matche jamais un numéro de port, inconnu = fatal fail-fast) ; `extract_retry_after_ms` (headers `retry-after[-ms]` de `exc.response.headers` + regex message « try again in Ns ») ; `compute_delay` (retry-after PRIORITAIRE clampé au cap, sinon backoff `base·2^attempt` clampé, jitter décorrelé ±25 %) ; `with_llm_retry(fn, policy, on_retry, between_attempts, sleep injectable)` — sémantique openfox PRÉ-CONTENU : nos clients étant non-streamants, tout échec d'appel rejoue LA MÊME requête sans rien mettre à l'historique (transparent pour l'agent, 0 step gaspillé).
- [x] Critère 402 : Branchement smolagents — `LoggedOpenAIServerModel.__call__` (nodes.py) wrappe l'appel via `with_llm_retry` (kwarg optionnel `revive=`) ; `_between_attempts` = port openfox « re-résolution du client à chaque tentative » : appelle `revive()`, propage le NOUVEL api_base dans `client_kwargs["base_url"]`, re-crée `self.client` via `create_client()` (pool de connexions jamais empoisonné) ; retry interne SDK openai DÉSACTIVÉ (`max_retries: 0` dans `client_kwargs` des 4 sites : build_fast_model, build_reasoning_model, Coder dynamique nodes.py, WebTester dynamique web_tester.py) pour que RetryPolicy soit l'autorité unique. Sites dynamiques migrés `OpenAIServerModel` → `LoggedOpenAIServerModel(..., revive=srv.revive)`. Opt-out `LLM_RETRY_ENABLED=0` → comportement pré-F-104 (exception directe à `run_with_retry`).
- [x] Critère 403 : `model_lifecycle.revive()` (llama_server.py) + sonde `_port_healthy(port, timeout=2s)` — appelé UNIQUEMENT après l'échec d'un appel LLM (jamais de requête en cours tuée) : backend spawn + `/health` OK → no-op retour api_base courant (blip transitoire, retry sec) ; serveur mort/wedged (failure mode VRAM du post-mortem run #4) → `stop()` des restes + `_spawn()` complet sous `_spawn_lock` (nouveau port) + mise à jour des attrs exposés ; backend external/none → api_base courante. `_SpawnedServer`/`_spawn`/`_wait_for_health` inchangés (respawn = même chemin que l'entrée).
- [x] Critère 404 : Volet DSPy — `_configure_dspy` (dspy_nodes.py) passe `num_retries=settings.llm_transport_retries` à `dspy.LM` (retry transport litellm natif : backoff+jitter internes, honorant retry-after) — un blip transitoire ne tue plus le nœud DSPy (`except Exception → None, None` reste le dernier recours). Même budget de tentatives que le volet smolagents (setting partagé), opt-out via `LLM_RETRY_ENABLED=0` → `num_retries=0`.
- [x] Critère 405 : Init MCP non bloquante (crush : timeout PAR SERVEUR, un serveur pendu ne bloque jamais le run) — module `graph_orchestrator/mcp_connect.py` : `open_mcp_with_timeout(params, timeout_s, server_name)` entre le CM `ToolCollection.from_mcp` dans un thread DAEMON avec attente bornée (`done.wait(timeout)`), retourne `(cm, tools)` déjà ouvert (fermeture par `__exit__` direct — un @contextmanager ne se consomme qu'une fois), `(None, [])` au timeout (warning + dégradation gracieuse, thread abandonné daemon — même compromis zombie que le timeout de `run_with_retry`), relève l'erreur d'init. Branché : `chrome_devtools_tools()` (25s), `context7_tools()` + `fetch_context7_brief()` (15s), bloc Puppeteer `web_tester.py` (25s, ExitStack + `callback(cm.__exit__)` — le tester tourne avec DevTools+lecture si Puppeteer pendu). Avant : 30 s de blocage thread ET event loop (défaut mcpadapt), puis exception.
- [x] Critère 406 : Leçon deer-flow DanglingToolCallMiddleware (ne jamais émettre deux réponses pour un même id) — `repair_orphan_tool_results` (orphan_repair.py) voit désormais AUSSI la forme top-level `message["tool_calls"]` (sérialisation OpenAI/smolagents — réponse = message `role=tool` inséré juste après l'assistant, application en indices DÉCROISSANTS + séquence décroissante à indice égal pour préserver l'ordre d'appel c1→c2) et collecte les réponses existantes des messages `role=tool` ; dédup STRICTE par id entre les deux formes (un appel visible partout = EXACTEMENT une réponse) ; entrées sans id ou sans nom ignorées ; idempotent.
- [x] Critère 407 : Config — `llm_retry_enabled` (défaut True), `llm_transport_retries` (5), `llm_retry_base_delay_s` (1.0), `llm_retry_max_delay_s` (30.0), `llm_retry_jitter` (0.25), `chrome_devtools_connect_timeout_s` (25.0), `context7_connect_timeout_s` (15.0), `puppeteer_connect_timeout_s` (25.0) dans la dataclass Settings + `load_settings` + `.env.example` + `.env` local (patron convention : défaut dans la dataclass).
- [x] Critère 408 : Validation — **43 tests nouveaux PASS** (`test_llm_retry.py` 23 : classification 6 dont port-vs-code-HTTP + fatal-prime-sur-retryable, retry-after 5, délais 4 dont clamp + bornes jitter, wrapper 8 dont fatal fail-fast/érosion budget/between_attempts best-effort/retry-after prioritaire ; `test_mcp_connect.py` 9 : succès/timeout borné <2s/erreur relevée/dégradations consommateurs/defaults+override ; `test_llm_model_retry.py` 6 : retry transparent 3 appels, client re-créé, revive propage api_base, opt-out, 4xx fail-fast, kwarg optionnel ; `test_orphan_repair.py` +5 : top-level, ordre stable, id en double = 1 réponse, entrées invalides, idempotent). Suite complète **1315 passed / 0 régression** + 11 live `test_web_tester_functional` PASS (web_tester.py modifié) ; 1 flaky minute-boundary documenté (`test_e2e_resume_reuses_same_run_dir`, passe isolément, pré-existant F-53) ; py_compile 10 fichiers OK. Tests MCP existants re-patchés vers le point d'entrée réel `mcp_connect.ToolCollection`.

### F-101 — Compaction v2 : alignée petits modèles + anti-boucle de compaction (opencode + s08 + pi + claude-science + hermes)

- [x] Critère 409 : Module `graph_orchestrator/compaction_prompts.py` (Python pur, 0 LLM, dormant) — port des prompts de summarization opencode réécrits POUR PETITS MODÈLES : `SUMMARY_SYSTEM_PROMPT` (identité simple « You are a context summarization agent » + clauses anti-dérive « Do not continue the conversation » + « Respond in the same language as the conversation », verbatim de `packages/opencode/src/agent/prompt/compaction.txt`) ; `SUMMARY_TEMPLATE` (structure Markdown fixe 5 sections Objective/Important Details/Work State/Next Move/Relevant Files, sections vides « (none) », règles « Keep every section, even when empty » / « Preserve exact file paths, symbols, commands, error strings ») ; `SUMMARY_UPDATE_INSTRUCTIONS` (les 2 règles verbatim « anything you do not carry into the new summary is lost » + « the conversation wins ») ; `FOLD_KEY_RULES` (claude-science : valeurs porteuses nommées COMME DES SEARCH QUERIES + « never reconstruct a value from memory ») ; `build_summary_prompt(context, prior_summary)` (assemblage buildPrompt ordre fidèle : conversation → prior-summary → update instructions → template) ; `select_head_recent(entries, keep_chars)` (port de `select()` : parcours inverse, entrée ENTIÈRE jamais coupée en deux, None si tout tient).
- [x] Critère 410 : Module `graph_orchestrator/compaction_guards.py` (Python pur, 0 LLM) — (a) `is_context_overflow_error` (patterns provider : maximum context length / prompt too long / too many tokens / context window / input tokens exceeds, accepte exception ou str, jamais d'exception) ; (b) `OverflowGuard` (pi §3.9 overflowRecoveryUsed : `on_overflow()` arme le drapeau DANS la décision atomique de récupérer, un second overflow → failure_drain `is_drained()`, `on_new_user_input()` réarme — un tool-result seul ne réarme pas) ; (c) `CompactionBudget` (hermes : budget `charge()`/`refund_noop()`, latch `verify_cleared` armé SEULEMENT sur compaction commise avec progrès, verdict rendu UNE fois par `on_real_usage(prompt_tokens)` — le prompt provider réel est le seul juge, sous le seuil → remboursement attempts=0 + strikes reset, au-dessus + verdict pending → strike, breaker `blocked()` à 2 strikes ou 2 fallbacks consécutifs, seuil inconnu/usage absent → aucun verdict). Écarts consciencieux vs hermes : pas de persistance DB des compteurs (agent RAM mono-process séquentiel) ni de fenêtre de probation 300 s — documentés dans le module.
- [x] Critère 411 : `compaction.py` — archive disque perte-zéro (s08) : `archive_steps` (JSONL `transcript_<uuid4>.jsonl`, `open("x")` exclusif, une ligne par step sérialisé défensivement `_serialize_step`, échec → None fail-open) ; `apply_snip_compact(memory, max_steps, transcript_dir, frame)` place le marqueur s08 exact `[N messages archived at <path>]` (+ rétrocompat marqueur historique sans dir), REPORTe les chemins d'archives précédents (`Earlier archives kept:` — chaînage opencode : rien n'échappe aux compactions successives) et le FRAME scratchpad claude-science (`survit à la compaction`) ; `persist_large_output` (bloc `<persisted-output>` avec `Full output: <path>` + Preview, safe_id sanitisé `[A-Za-z0-9._-]`, fichier existant jamais réécrit, sous le seuil/fail-open → inchangé) ; `apply_tool_result_budget(..., outputs_dir)` persiste le contenu intégral AVANT de tronquer (le remplacement n'accepté que s'il réduit réellement) ; `apply_micro_compact` réutilise la ligne `Full output: ` → `[Earlier tool result saved at <path>]` (relisible via read_file) sinon placeholder historique ; `CompactingCodeAgent` gagne `context_frame: list[str]` et son `write_memory_to_messages` résout les dossiers depuis le cwd (run dir F-40 : `.transcripts/`, `.task_outputs/tool-results/`), avec le snip déplacé EN TÊTE du pipeline (l'archive contient les contenus originaux, pas des versions déjà réduites).
- [x] Critère 412 : Branchement anti-boucle overflow — `run_with_retry` (nodes.py) crée un `OverflowGuard` par exécution de nœud (un appel = un tour utilisateur logique, garde locale) ; dans le bloc `except Exception`, un overflow détecté consulte la garde : 1er → récupération (message « contexte débordé, mémoire purgée, reprends directement l'action utile » + purge mémoire existante de fin de boucle = notre compaction de récupération), 2e → **failure_drain** : `return None, last_metrics` immédiat (plus de retries brûlés sur une requête incompressible — system prompt + tools schemas + tâche), le graphe continue (Judge/itération). Opt-out `COMPACTION_OVERFLOW_GUARD=0` → comportement historique (tous les retries).
- [x] Critère 413 : Config — `compaction_archive_enabled` (défaut True) + `compaction_overflow_guard` (défaut True) dans la dataclass Settings + `load_settings` + `.env.example` + `.env` local (patron convention : défaut dans la dataclass).
- [x] Critère 414 : Validation — **63 tests nouveaux PASS** (`test_compaction_v2.py` : archive JSONL 6, snip v2 5 dont chaînage multi-archives + frame + rétrocompat, persisted-output 5, tool_result_budget 3, micro v2 3, agent frame pipeline + opt-out 2, prompts 9 dont ordre build + entrée entière + budget recent, OverflowGuard 4, détection overflow 16 paramétrés, CompactionBudget 10 dont remboursement-usage-vérifié + latch unique + breaker, intégration run_with_retry 3 dont drain à 2 appels vs 3 en opt-out) ; suite complète **1390 passed / 0 failed / 1 skipped** (1315 baseline + ~12 live + 63 nouveaux, 0 régression ; 1 flaky minute-boundary pré-existant documenté) ; py_compile 5 fichiers OK ; smoke live du snip archivé (marqueur + FRAME + 3 lignes JSONL) avant les tests.

### F-95 — Robustesse FS : transactions/rollbacks hardlinks + verrous coopératifs + cloisonnement IO (OpenKB)

- [x] Critère 415 : Module `graph_orchestrator/fs_locks.py` (Python pur, 0 LLM) — port `references/OpenKB/openkb/locks.py` : verrou OS advisory cross-process sur `<root>/.fs_tx/dir.lock` (POSIX `fcntl.flock` bloquant, Windows `msvcrt.locking` LK_NBLCK + retry borné 10 s puis `FsLockTimeout`), `_LocalRwLock` intra-process (plusieurs lecteurs OU un écrivain, condition), réentrance par thread (compteurs de profondeur, upgrade read→write refusé `RuntimeError`), `write_lock`/`read_lock`/`write_lock_held`, drain différé des journaux `fs_tx` à la PREMIÈRE acquisition exclusive (import différé casse le cycle locks↔fs_tx), écritures atomiques `atomic_write_bytes/text/json` (temp + `os.replace` + fsync fichier/dossier, mode existant préservé, umask pour fichier neuf — consommateur principal : journaux fs_tx). Écarts documentés : PAS de dépendance portalocker (stdlib pur = ce que portalocker enveloppe) ; sous Windows un read lock prend un verrou OS exclusif (msvcrt ne connaît pas les shared locks — sûreté conservée, concurrence lecteurs INTER-process sacrifiée sans objet pour nous, réelle INTRA-process via _LocalRwLock).
- [x] Critère 416 : Module `graph_orchestrator/fs_tx.py` (Python pur, 0 LLM) — port `references/OpenKB/openkb/mutation.py` : `MutationSnapshot` (journal `<root>/.fs_tx/journal/<id>.json` statuts active/committed/rolled_back + backup `<root>/.fs_tx/backup/<id>/`), `snapshot_paths` (fichiers copiés atomiquement, répertoires copiés OU hardlinkés via `_hardlink_or_copy` avec repli copy2 sur EXDEV/EPERM/EACCES — OneDrive/ACL Windows), `track_new` (artefacts créés post-snapshot, supprimés au rollback), `rollback` (enfants avant parents ; dossier hardlinké → `_restore_hardlinked_dir` O(touched) par diff d'inode (st_dev, st_ino) : intacts jamais recopiés, modifiés/supprimés restaurés, ajoutés retirés, dirs vides élagués), `recover_pending_journals` (active→rollback+discard, terminal→discard, corrompu→suppression bruyante sinon il re-échouerait à CHAQUE lock, rollback défaillant→retry borné `MAX_ROLLBACK_ATTEMPTS=5` puis GAVE UP), backup orphelin retiré sur snapshot partiel en échec. Écarts documentés : namespace `.fs_tx/` (vs `.openkb/`) ; `publish_staged_tree`/`_publish_staged_file` NON portés (staging raw/wiki-sources spécifique KB, aucun consommateur) ; AVERTISSEMENT contrat hardlink_dirs conservé (nos append_file écrivent in-place → l'appel prod passe hardlink_dirs=None).
- [x] Critère 417 : Module `graph_orchestrator/io_guard.py` (Python pur, 0 LLM) — port du pattern `references/OpenKB/openkb/agent/tools.py` (« Access denied: path escapes root. ») : allowlist de racines à contexte module-level (pattern `_scoped_idempotency` F-43 — `set/clear/get/scoped_allowed_roots`, restaure les racines précédentes à la sortie), `path_allowed` (résolution `os.path.realpath` complète → symlink + traversal `sub/../../evil.txt` détectés ; comparaison `normcase` Windows-safe ; frontière de répertoire stricte `run2` ∉ `run` ; jamais d'exception), `ensure_write_allowed`/`ensure_read_allowed` (messages pédagogiques orientant vers le chemin relatif, pattern read_gate/openfox). Branché sur les 9 outils FS de `tools.py` (write_file, append_file, edit_file, search_replace, multi_replace, read_file, read_python_skeleton, check_js_syntax, list_directory) — chacun normalise (`normalize_tool_path` F-97) puis vérifie AVANT toute IO. Fail-open garanti : sans racine enregistrée (tests, scripts debug/, usage direct) TOUT passe — 0 comportement historique cassé. `bash_command` NON couvert (chemins inextricables d'une commande shell — reste couvert par bash_guard F-38), écart documenté.
- [x] Critère 418 : Intégration `workflows.py` — (a) context manager `_scoped_run_guard(run_output_dir, settings)` composé dans le `with _scoped_chdir, _scoped_idempotency` existant (0 réindentation) : verrou exclusif cross-process du run dir + allowlist IO scopée à `[run_output_dir]`, les deux opt-out ; (b) la 1re acquisition exclusive DRAINE les journaux actifs (crash-recovery : rollback des mutations d'un process mort AVANT toute nouvelle mutation) ; (c) transaction fs_tx autour de CHAQUE appel Coder (`snapshot_paths` des target_files, opération `coder:<task_id>:iter<N>`) : retour NORMAL (même failure) → `mark_committed` (fichiers partiels conservés, sémantique historique du mode correction), EXCEPTION → rollback + discard + re-raise, CRASH PROCESS → journal « active » roulé back au prochain run (cohérent avec la granularité « début d'itération » du checkpoint F-24). `git_snapshot.init_run_git` écrit `.fs_tx/` dans `.git/info/exclude` (exclusions LOCALES, PAS de .gitignore worktree qui apparaîtrait lui-même comme fichier ajouté dans le 1er commit F-53 et le manifeste turn-diff F-102 — fix réactif au test de non-contamination), idempotent, best-effort, aussi sur le chemin de reprise (.git existant).
- [x] Critère 419 : Config — `run_dir_lock_enabled` / `fs_transactions_enabled` / `io_allowlist_enabled` (défauts True, opt-out individuels) dans la dataclass Settings + `load_settings` + `.env.example` + `.env` local (patron convention : défaut dans la dataclass, ne casse pas les helpers de test qui construisent Settings() à la main).
- [x] Critère 420 : Validation — **54 tests nouveaux PASS** (`test_fs_locks.py` 17 : atomic writes 5 dont remplacement intégral + pas de temp résiduel, RW lock 2 (lecteurs simultanés par barrière, écrivain exclusif), dir_lock 4 (lock file, réentrance, upgrade refusé, held), drain d'un journal actif au 1er verrou, verrou cross-process subprocess × 2 (timeout borné + read vs write croisé) ; `test_fs_tx.py` 16 : snapshot 3 dont échec partiel sans backup orphelin, rollback 5 (modifié/supprimé/créé/dossier copié/commit+discard), best-effort 1, hardlink 4 (inode partagé préservé + O(touched) + dégradation copie + repli EACCES), recover 6 (active/terminal/corrompu/retry borné→GAVE UP/noop/roundtrip journal seul) ; `test_io_guard.py` 17 : fail-open, autorisés/refusés, traversal, frontière préfixe, casse Windows, multi-racines, chemin absurde, lifecycle 2, opt-out settings (dataclasses.replace + patch à la source), intégration 9 outils ; `test_git_snapshot.py` +3 exclusion `.fs_tx/` (git status vide, exclude idempotent sans .gitignore worktree, ls-files sans .fs_tx)) ; suite complète **1441 passed / 0 failed / 1 skipped** (1390 baseline + 51 nouveaux à l'exécution + 3 exclusion, 0 régression ; le 1er run complet a fait réapparaître `test_e2e_resume_reuses_same_run_dir` = flaky minute-boundary PRÉ-EXISTANT documenté F-53, repassé isolément et au run final) ; py_compile 6 fichiers OK ; LIVE `debug/run_fs_safety.py` **3/3 démos** (crash-recovery end-to-end avec drain au verrou, verrou cross-process réel parent/enfant, cloisonnement IO avec fail-open).

### F-112 — Sonde animation instantanée multi-signal + résolution arithmétique des délais (post-mortem run #8)

- [x] Critère 421 : Diagnostic du run #8 (E2E FRESH_START 2026-08-16_2205, 1h47) consigné — livrable Bubble Sort avec tri INSTANTANÉ sorti de 3 itérations : (a) Tier 3 cécité : le découvreur de signal prenait le PREMIER élément numérique du DOM = `<span id="speedLabel">5</span>` (CONSTANTE placée avant le compteur) → t0=t1=t2 → « aucune progression » → skip silencieux (fail-open par design, jamais de FP — mais cécité totale) ; (b) Tier 1c cécité : le délai était une FORMULE (`sleep(320 - speed * 2)`, `speed=320` ms → −320) que la résolution littérale ne voyait pas ; (c) cause amont : le message pédagogique du Tier 1c suggérait LITTÉRALEMENT `const delay = 320 - speed * 28;` — le Coder 4B l'a greffée sur une variable en millisecondes → négatif.
- [x] Critère 422 : `_temporal_verdict` (pur, testable sans Chrome) — sonde MULTI-SIGNAL : TOUS les éléments numériques du DOM (clés par id), hash djb2 des pixels du premier `<canvas>` (toDataURL, tainted → null → signal ignoré fail-open), compte d'éléments à classes terminales (.sorted/.done/.visited/.finished/.completed). Verdict : ≥1 signal a progressé (t1≠t0) ET AUCUN ne bouge dans la fenêtre de stabilisation (t2==t1 partout) → animation instantanée (réfutation pédagogique citant les signaux observés et les 3 causes typiques : délai négatif/~0 clampé par setTimeout, algorithme en 1 tick sans await interne, rendu jamais appelé dans la boucle) ; rien n'a bougé → skip (jamais de FP) ; un signal bouge encore → progressive OK.
- [x] Critère 423 : `_evaluate_temporal` réécrite — protocole UN evaluate_script (pattern async documenté, jamais d'IIFE) : snapshot t0 tous signaux → clic bouton primaire → await OBSERVE (400 ms) → t1 → await STABILIZATION → t2 ; verdict délégué à `_temporal_verdict`. Fenêtre de stabilisation 50 → **350 ms** : à 50 ms une animation LÉGITIME à pas 100-300 ms (slider lent) semblait « stabilisée » entre deux pas → faux positif potentiel de l'ancienne version ; 350 ms garantit qu'un pas ≤ 350 ms est vu bouger. Skips préservés : pas de bouton primaire, outils DevTools absents, réponse illisible, exception → [] (fail-open intégral).
- [x] Critère 424 : `_resolve_delay_ms(arg, js)` — résolution ARITHMÉTIQUE des arguments de `sleep(...)`/`setTimeout(...)` : littéral → int ; substitution de CHAQUE identifiant lié à un littéral entier (`let|var|const name = <int>`) ; si résidu pur arithmétique (whitelist stricte `[0-9+\-*/(). ]` — jamais de nom, jamais d'appel, pas d'eval arbitraire) → évaluation sandboxée (`{"__builtins__": {}}`) ; échec/division par zéro/résidu alphabétique → None (fail-open). `sleep(320 - speed * 2)` avec `let speed = 320` → **−320** → flag au Tier 1c avec mention « valeur NÉGATIVE — setTimeout la clamp à 0 → instantané total ».
- [x] Critère 425 : Message pédagogique Tier 1c rewordé (anti greffe aveugle) — l'ancien suggérait la formule littérale `const delay = 320 - speed * 28;` (cause directe du bug du run #8) ; le nouveau porte l'INVARIANT : « le délai effectif par étape doit rester dans 50-300 ms pour TOUTE position du curseur — vérifie l'UNITÉ de ta variable (valeur slider 1-10 ≠ millisecondes) et évalue ta formule de bout en bout (une formule qui peut devenir négative est silencieusement clampée à 0 par setTimeout) ». Aucune formule copiable.
- [x] Critère 426 : Validation — **19 tests nouveaux PASS** (6 `_resolve_delay_ms` dont formule négative du run #8, formule positive légitime, var non liée fail-open, division par zéro fail-open, appel de fonction rejeté ; 3 behavioral dont formule −320 flaggée avec NÉGATIVE, absence de formule littérale dans le message + présence de l'invariant UNITÉ, formule positive propre ; 8 `_temporal_verdict` purs dont signatures exactes du run #8 (compteur progressif-then-stable + canvas, canvas seul, progressive OK, canvas progressive OK, signal constant seul skip, classes terminales stables, canvas absent, no-btn) ; 2 LIVE réplique exacte du run #8 — speedLabel numérique AVANT compteur + canvas + formule négative → RÉFUTÉE, version progressive (await 80 ms + draw() dans la boucle) → passe). LIVE sur le VRAI livrable `runs/2026-08-16_2205_bubble_sort_multifile_v6/index.html` : `is_valid=False` avec les DEUX réfutations (Tier 1c « −320 ms NÉGATIVE » + Tier 3 « compteur counter 0→214 ; canvas (pixels) »). Suite complète **1409 passed / 0 failed / 1 skipped** (1390 baseline, 0 régression) ; py_compile OK.

### F-113 — Fixes P0 post-mortem run #8 : api_base du sauvetage + prune vérifié + isolation runs/

- [x] Critère 427 : Fix sauvetage (cause racine prouvée par introspection) — propriété `api_base` sur `LoggedOpenAIServerModel` (retourne `client_kwargs["base_url"]`, donc TOUJOURS la valeur courante y compris après un `_between_attempts`/revive F-104) + helper `_resolve_agent_api_base(agent_model)` (attribut `api_base` direct s'il existe, sinon fallback `client_kwargs["base_url"]` — couvre les modèles non-Logged, None si rien) branché dans `run_with_retry` à la place du `getattr(agent_model, "api_base", None)` qui rendait TOUJOURS None (`smolagents.OpenAIServerModel` n'assigne pas cet attribut) → le sauvetage Pydantic tapait `settings.local_api_base` (port 8000, rien n'écoute en mode spawn) → `Connection error` déterministe 3/3 au run #8, serveurs dynamiques sains (3× `slot release` propre dans les logs llama).
- [x] Critère 428 : Fix prune — `_rmtree_verified(path, attempts=3)` : `shutil.rmtree` avec handler `onerror` qui retire le bit read-only (blobs git) puis retente, retries avec backoff court, retourne True SSI le dossier a disparu (vérification finale, jamais par défaut) ; `_prune_old_runs(runs_root, retention, grace_hours=6.0)` : le message « 🗑️ Ancien run supprimé » n'est émis QUE si le dossier a DISPARU (sinon warning bruyant « PRUNE PARTIEL … réessayé au prochain run ») + période de grâce : tout run dont le mtime date de moins de `grace_hours` n'est JAMAIS candidat (un run fraîchement terminé — ou poussé hors top-N par des dirs de tests — ne peut plus être détruit à chaud comme le run #8 à 00:21, 20 min après sa fin).
- [x] Critère 429 : Fix isolation — les 6 helpers E2E (`test_escalation`, `test_checkpoint`, `test_consolidation`, `test_lesson_recall`, `test_prompt_refiner._settings_full`, `test_feedback_integration`) passent `output_dir=tempfile.mkdtemp(prefix="e2e_runs_")` — avant, chaque exécution de la suite pytest créait 8+ vrais dossiers sous `runs/` (dirs `_t1`/`_task1` par dizaines, ~130 cumulés) qui poussaient les runs RÉCENTS hors top-N de la rétention → destruction par prune au run suivant.
- [x] Critère 430 : Tests — **17 nouveaux PASS** (`test_f113_p0_fixes.py` : résolution api_base 5 dont la régression exacte du run #8 (propriété == endpoint), propriété vivante après mutation client_kwargs (revive), fallback bare-model avec client_kwargs seul, attribut direct prioritaire, fail-open None ; `_rmtree_verified` 3 dont arbre `.git/objects` complet supprimé, blob READ-ONLY supprimé (pattern Windows du run #8), échec obstiné → False + dir survit ; prune grâce 3 dont run récent hors top-N PROTÉGÉ + jamais logué, rmtree défaillant → aucun « [🗑️] » + « PRUNE PARTIEL » émis, retention=0 no-op ; isolation 6 paramétrés : chaque helper produit un output_dir temporaire ≠ "runs").
- [x] Critère 431 : Validation — suite complète **1407 passed / 0 failed / 1 skipped** (1390 baseline + 17, 0 régression) ; py_compile 8 fichiers ; **vérification LIVE post-suite : 0 nouveau dossier créé sous `runs/`** (avant ce fix : 8+ à chaque exécution de la suite complète) ; les 5 suites E2E touchées (escalation/checkpoint/consolidation/lesson_recall/prompt_refiner/feedback) repassent au vert dans la suite complète.

### F-114 — Cap fullPage des screenshots + nudge checklist visual_check (post-mortem run #9)

- [x] Critère 432 : Cap `fullPage` — `_ScreenshotCapturingTool.forward` (`vision_callback.py`) force `fullPage=False` (et variante snake_case `full_page`) sur les clés UNIQUEMENT déjà présentes dans les kwargs (jamais d'injection d'argument inconnu vers le serveur MCP) quand `settings.vision_fullpage_cap` est actif. Motif run #9 : `take_screenshot(fullPage=True)` produisait des images jusqu'à 1265×9315 px → prompt processing vision de plusieurs minutes → `Request timed out` (600 s) tuant la tentative 2. Le strip `filePath` (F-50/F-90) reste actif (test de régression dédié).
- [x] Critère 433 : Nudge checklist contextuel — `make_screenshot_callback(capture_holder, visual_criteria_count=0)` : après le push de l'image, si `visual_criteria_count > 0` (Coder avec critères F-90 uniquement — Tester = 0 par défaut → inactif), `_build_checklist_nudge` compare les screenshots pris (`_SCREENSHOT_NUDGE_STATE["count"]`) aux critères audités (`tools.get_visual_audit()`) ; au seuil `_NUDGE_THRESHOLD=3` screenshots avec critères manquants, le rappel (« NE prends PAS un autre screenshot : appelle visual_check pour CHAQUE critère manquant [liste], puis final_answer ») est APPENDU à `memory_step.observations` — canal fiable car `_finalize_step` (step_callbacks) s'exécute APRÈS l'assignation des observations par l'agent (vérifié smolagents `agents.py:623`), indépendant du type de retour de l'outil (image PIL). Checklist complète → jamais de nudge ; sous le seuil → rien ; fail-open total (jamais d'exception).
- [x] Critère 434 : Lifecycle — `reset_screenshot_nudge()` appelé dans `execute_coder_node` au même site que `reset_visual_audit()` (une exécution de nœud = un cycle) ; le compteur traverse VOLONTAIREMENT les retries de `run_with_retry` (30 screenshots à la tentative 1 → nudge immédiat à la tentative 2, pas après 3 nouveaux) ; `visual_criteria_count` transmis au callback du Coder uniquement (`nodes.py`), le WebTester garde la signature par défaut (nudge inactif).
- [x] Critère 435 : Config — `vision_fullpage_cap: bool = True` (env `VISION_FULLPAGE_CAP`) dans `config.py` (champ + `load_settings`), documenté dans `.env.example` ET reporté dans `.env` local (convention §7 AGENTS.md). Le nudge n'a PAS d'opt-out séparé : il est inactif dès que `VISUAL_AUDIT_ENABLED=false` (critères = 0).
- [x] Critère 436 : Validation — **14 tests nouveaux PASS** (`tests/test_vision_nudge.py` : cap 6 dont clés absentes jamais injectées + opt-out via patch à la source (`dataclasses.replace` + `patch("graph_orchestrator.vision_callback.settings")`, convention `test_bash_guard`) + régression filePath ; nudge 8 dont seuil exact au 3e screenshot, critères manquants listés, checklist complète silencieuse, chemin Tester (0 critère) jamais nudgé, reset, observations existantes préservées, holder vide ne fait pas avancer le compteur) ; py_compile 3 fichiers modifiés. Suite complète (basetemp dédié, contournement symlink F-95) : **1493 passed / 1 failed / 1 skipped** — l'unique échec = `test_output_dir::test_e2e_resume_reuses_same_run_dir`, **flaky minute-boundary PRÉEXISTANT** (documenté F-53/F-95 : le test de ~41 s ne passe que si ses 2 runs tombent dans la même minute — dirs `_1640` vs `_1641` observés) ; vérifié échec IDENTIQUE sans les changements F-114 (stash des 3 fichiers source, base docs/audit-kilocode dont le code de prod est identique à main) → **0 régression F-114** ; durcissement du flaky toujours renvoyé à un cycle dédié.

### F-115 — Spec PromptRefiner en anglais (alignement petits modèles)

- [x] Critère 437 : `PromptRefinerSignature` (`dspy_nodes.py`) réécrite en anglais — instructions traduites + règle LANGUAGE explicite (« always write the refined spec in ENGLISH, whatever the input language » ; motif : pattern Kilo Code / Cline « Enhance Prompt », référence d'origine de F-39 — les petits modèles locaux Qwen/Ornith sont nettement plus forts sur l'anglais structuré) + sections canoniques anglaises EXACTES `## Objective` / `## Expected Features` / `## Technical Constraints` / `## Acceptance Criteria` ; descriptions des Input/OutputFields mises en cohérence (mention ENGLISH sur l'OutputField).
- [x] Critère 438 : `requirements_checklist.py` (F-51) parse les DEUX en-têtes de section — regex alternance `(?:Expected\s+Features?|Fonctionnalit[ée]s?\s+attendues?)` insensible casse (anglais prioritaire dans l'alternance, français conservé en rétro-compat pour les checkpoints F-24 repris et specs héritées) ; docstrings module + fonction mises à jour ; comportement fallback inchangé (section absente → [] → Tester texte libre).
- [x] Critère 439 : Tests — mock du test d'exécution passé en anglais (`## Objective`) + assertion `"Objective"` ; nouveau test doctrinal `test_signature_exige_sortie_anglaise` (marqueurs ENGLISH + 4 sections EN présents, section FR absente de la signature) ; +3 tests parseur (`test_spec_anglaise_complete`, `test_insensible_casse_anglais`, `test_retrocompat_francais_preservee`). **27 tests ciblés PASS** (11 prompt_refiner + 16 requirements_checklist) ; suite complète (basetemp dédié) : **1498 passed / 0 failed / 1 skipped** (1495 baseline + 4 nouveaux, flaky minute-boundary PASSÉ cette exécution, 0 régression).

### F-122 — ULTRA restreint à l'itération ≥ 3 (réactivation bornée)

- [x] Critère 440 : `_select_coder_spec` (`nodes.py`) — la condition d'escalade ULTRA devient `coder_ultra_correction AND no_think_model AND iteration >= 3` SEULE : les déclencheurs historiques « rejet déterministe à l'itération 2 » et « Coder mort techniquement » sont retirés (verdict run #8 : Ornith-9B no-think trop lent — 3.8 t/s, mega-blocs > timeout 600 s ; run #11 : le 4B sait corriger sur feedback qualitatif). Borne : AU PLUS UNE activation d'ULTRA par run (l'itération 3 = dernière chance avant escalade F-23). Docstring de l'échelle réécrite (F-111/F-122), les signaux `prev_deterministic_reject`/`prev_coder_died` restent calculés par `workflows.py` à titre d'observabilité.
- [x] Critère 441 : Config — `CODER_ULTRA_CORRECTION=true` réactivé dans `.env` ET `.env.example` avec commentaire F-122 (sémantique : uniquement it. 3, au plus une activation par run) ; le défaut dataclass reste `True` (inchangé).
- [x] Critère 442 : Tests — classe `TestCoderUltra` mise à jour : les 2 cas historiques INVERSÉS (`test_correction_rejet_deterministe_iteration2_reste_fast`, `test_coder_mort_iteration2_reste_fast` — is_ultra False, spec fast, max_tokens 4000), docstring de classe F-111/F-122 ; conservés : création it.1 fast, rejet qualitatif it.2 fast, it.3 Ultra, opt-out, sans spec no-think. **22/22 PASS** (`test_coder_hardening.py`) ; py_compile OK. Suite complète (basetemp dédié) : **1497 passed / 1 failed / 1 skipped** — échec unique = flaky minute-boundary `test_output_dir::test_e2e_resume_reuses_same_run_dir` PRÉEXISTANT (documenté F-53/F-95, échec identique en isolation, ZÉRO interaction avec `_select_coder_spec`/env ; il passe ou échoue selon la position de la minute — passé lors de la suite F-115) → **0 régression F-122**.

## Protocole d'Évaluation
* Tests unitaires : `uv run pytest tests/ -v` → zéro échec.
* Validation process : `uv run python -m graph_orchestrator.workflows` (WORKFLOW_MODE=coding) →
  vérifier que le workflow aboutit (Architect→Coder→Tester→Judge) et produit les 3 fichiers.
* Vérification livrable : `ls landing_page/` (3 fichiers) + inspection HTML (non corrompu).

